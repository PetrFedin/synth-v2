 Before/after counters require this slice to create no Selection, WholesaleOrder, SupplyCommitment, ActualCost or inventory movement. Automated evidence is split across `tests/product-commercialization-live-acceptance.test.mjs`, `tests/postgres/product-commercialization-live-acceptance.test.mjs` and `.github/workflows/product-commercialization-acceptance.yml`.
 
-This closes the missing executable acceptance harness for the current atomic-published P0.3 path, but P0.3 as a production contract remains PARTIAL because `COMM-LC-008` and `PRICE-009` below are still open. The new GitHub workflow has to pass on the exact PR head, and an intended live acceptance environment must still execute the command before the slice can be called `PROD-PROVEN`.
+PR #118 closed the missing executable harness and its exact head passed all required repository workflows listed in section 2.6. The current P0.4 slice additionally guards ProductSku-exact buyer pricing at domain/HTTP/OpenAPI/PostgreSQL boundaries and extends same-environment PostgreSQL acceptance with negative bypass checks. The overall production contract remains PARTIAL because `COMM-LC-008`, the remaining `PRICE-009` depth, buyer Selection convergence and later P0 slices remain open. An intended live acceptance environment must still execute the accepted gate before this slice can be called `PROD-PROVEN`.
 
 Supporting detail: `docs/architecture/product-readiness-commercial-projection-v2.md`, `docs/commercial-publication-linesheets.md`.
 
@@ -607,7 +616,11 @@ Showroom/linesheet is a buyer-facing commercial presentation of publication/cata
 
 ### 7.4 Selection / Color × Size matrix — PARTIAL
 
-Selection exists, but the target buyer matrix must preserve exact BuyerCatalogVersion, ProductSku, ordered size, price and commercial context. Draft context must not silently change when mutable catalog/master records change.
+The canonical matrix mutation (`replaceSelectionMatrix`) already requires a pinned rich BuyerCatalogVersion and resolves every selected cell to exact ProductSku/StyleVersion/Colorway/SizeValue identity plus frozen buyer price/currency/MOQ context. Client-supplied price, currency and catalog version are forbidden.
+
+A confirmed P0 compatibility seam remains in `createShowroomSelectionService`: when no canonical BuyerCatalog reader/buyer catalog is available, `createSelection` may still create a legacy selection with no BuyerCatalogVersion, and single-line `upsertSelectionLine` may then resolve price/currency/version from live `catalog_skus`. A pinned but historical non-rich BuyerCatalog can also fall back to live CatalogSku for ATS validation. This behavior is **DEPRECATED compatibility**, not canonical buyer truth, and is the immediate P0.5/PUB-005 convergence target. New canonical buyer selection must become fail-closed without an exact rich BuyerCatalogVersion and exact ProductSku cell; historical selections must remain readable without guessed ProductSku backfill.
+
+Draft context must never silently change when mutable catalog/master records change. P0.5 acceptance must prove BuyerCatalogVersion → exact Color × Size Selection → submit → WholesaleOrder → bilateral confirmation → OrderCommitSnapshot without any live-catalog reconstruction.
 
 ### 7.5 WholesaleOrder — IMPLEMENTED
 Order lifecycle and bilateral confirmation are executable. Order creation consumes frozen submitted Selection lineage. Currency is derived/validated against the frozen selection lines rather than accepted as an independent mutable client fact.
@@ -978,6 +991,14 @@ The reconciliation route requires `COST_MANAGE`, the exact latest PostCloseAdjus
 
 Public version remains `/v2` / OpenAPI `1.17.0`; these are invariant corrections and completion of the existing economics contract, not a new parallel API version.
 
+### 12.4 Canonical buyer pricing/catalog publication API — IMPLEMENTED/PARTIAL
+
+`POST /v2/commercial-publications/{publicationId}/buyer-catalogs` is the only current public mutation that creates a buyer-specific PriceListVersion and BuyerCatalogVersion pair. Required body fields are `showroomId` and `shopId`; optional `priceOverrides` is an array of exact `{ productSkuId, wholesalePriceMinor }` rows. Unknown fields, textual `sku`, client `unitPrice`, invalid identifiers, non-integer/negative/overflowing minor amounts and over-sized override arrays are HTTP 400. Unknown/duplicate ProductSku or mismatched immutable commercial lineage fails as domain validation (HTTP 422). Wrong role/organisation remains 403; reusing an idempotency key with a different fingerprint remains 409 `COMMAND_ID_CONFLICT`.
+
+The application revalidates the current brand capability and accepted/unexpired Showroom access before command replay or new execution. One successful command transaction inserts exactly one immutable PriceListVersion, one immutable BuyerCatalogVersion and the corresponding outbox events; exact replay returns the stored result without duplicate commercial facts. Neither request nor domain accepts an independently authoritative currency, major-unit price, current PLM value or textual-SKU join.
+
+The authoritative composed OpenAPI remains `/v2` version `1.17.0`; this is convergence of the existing endpoint, not a parallel API version.
+
 ---
 
 ## 13. PostgreSQL and migration contract
@@ -994,6 +1015,7 @@ Public version remains `/v2` / OpenAPI `1.17.0`; these are invariant corrections
 - Cost Allocation exact-lineage output requires no migration: `cost_allocation_run_snapshots` already persists `allocations`, `sku_economics` and the complete immutable snapshot `payload` as JSONB, so `orderLineNo`, `productSkuId` and `lineageMode` are retained without introducing redundant relational columns or rewriting prior snapshots.
 - PR #115 allocation→margin→readiness→close provenance required no migration because existing immutable economics tables already persisted complete snapshot payload JSONB while scalar accounting columns remained unchanged. The economics store reads an immutable `cost_allocation_run_snapshots.payload` by id for fail-closed validation; historical snapshots are not rewritten.
 - Migration `074_post_close_allocation_reconciliation.sql` creates immutable `post_close_allocation_reconciliation_snapshots`. One row is allowed per `post_close_adjustment_id`; one reconciled margin is pinned per reconciliation. Exact foreign keys and the integrity trigger bind order/commit/close/adjustment/pending margin/adjusted landed/allocation policy/allocation run/new current margin; the trigger also validates pending→current allocation status, scalar↔payload identity, exact orderVersion, timestamps/content hash and unchanged aggregate economics. UPDATE/DELETE is rejected by the common economics immutability trigger.
+- Migration `075_canonical_buyer_catalog_new_writes.sql` is forward-only PUB-005 enforcement. BEFORE INSERT guards require every fresh PriceListVersion/BuyerCatalogVersion to originate from a projection-backed V2 CommercialPublication, preserve exact projection/readiness/StyleVersion/ProductSku line and rich hierarchy, keep non-price commercial terms frozen, keep server-derived major/minor price consistent, and match relational scalar columns to immutable payload identity. BuyerCatalog additionally validates exact PriceList/publication/brand/shop/currency/Showroom/accepted-unexpired access context. Historical V1 rows are neither updated nor deleted; the guard never joins `catalog_skus` or current Product Master tables to invent ProductSku identity.
 
 ---
 
@@ -1038,7 +1060,7 @@ Product Identity + governed APPAREL/size MDM
 
 `npm run acceptance:product-readiness` first migrates and idempotently bootstraps the source-controlled operational MDM profile through the canonical MDM bootstrap, then executes both scenarios through supported authenticated `/v2` mutations. Scenario A intentionally omits category/measurement repository truth and passes only with exactly `category` + `measurements` blockers plus exact HTTP `422 / COMMERCIAL_PROJECTION_READINESS_BLOCKED` and zero exact projection rows before/after. Scenario B uses exact governed `APPAREL`, `INT_ALPHA`, `INT_M`, `CM` and `CHEST_CIRC` references, creates and publishes a canonical Measurement Chart, and passes only with a READY snapshot and zero blockers plus exact same-environment PostgreSQL lineage. Scenario A retains strict downstream commercial/warehouse/economic isolation. Scenario B additionally proves the intentional ProductSku registration effect from migration 065: exactly one same-brand zero balance identity is materialized while quantities, movement ledger and downstream commercial/economic facts remain unchanged. Repository tests validate both harnesses and failure semantics; until this command completes against the intended live acceptance environment, both #117 acceptance slices remain `IMPLEMENTED` rather than `PROD-PROVEN`.
 
-The current P0.3 branch additionally implements `npm run acceptance:product-commercialization`. It creates a fresh positive READY graph, continues it through exact Collection assortment/Showroom/buyer access, CommercialProductProjectionVersion, projection-backed CommercialPublication, PriceListVersion and BuyerCatalogVersion, and checks the immutable chain with both brand and shop actors against the same PostgreSQL target. A dedicated workflow now exists to run that command against the supported runtime process and PostgreSQL 17. The workflow definition, unit tests and PostgreSQL integration test are evidence of IMPLEMENTED coverage only until the exact workflow head succeeds; even a green CI execution does not by itself assert intended-environment `PROD-PROVEN` unless that environment is explicitly the accepted production-evidence target.
+PR #118 implemented `npm run acceptance:product-commercialization` and is merged. It creates a fresh positive READY graph, continues it through exact Collection assortment/Showroom/buyer access, CommercialProductProjectionVersion, projection-backed CommercialPublication, ProductSku-exact PriceListVersion and BuyerCatalogVersion, and checks the immutable chain with both brand and shop actors against the same PostgreSQL target. Its exact head passed the dedicated supported-runtime/PostgreSQL workflow and the required CI listed in section 2.6. The current P0.4 convergence extends that harness with exact ProductSku minor-price input plus PostgreSQL fail-closed new-write checks. Repository CI success remains `IMPLEMENTED` evidence, not an intended-environment `PROD-PROVEN` claim unless that environment is explicitly the accepted production-evidence target.
 
 ### 15.4 Required acceptance expansion order
 
@@ -1046,7 +1068,7 @@ Expand one business slice at a time, preserving explicit before/after invariants
 
 1. Campaign → Collection — IMPLEMENTED / PROD-PROVEN.
 2. Product Identity → Readiness — negative fail-closed **and** positive canonical READY harnesses IMPLEMENTED in #117; exact MDM/category/size/Measurement Chart PostgreSQL assertions are executable; actual intended live-environment execution is PENDING, so no `PROD-PROVEN` claim.
-3. Readiness → Projection → Publication → PriceList → BuyerCatalog — public-runtime harness IMPLEMENTED on `feat/acc004-ready-to-buyer-catalog-live`; exact-head workflow/live evidence is pending and the semantic production contract remains PARTIAL until `COMM-LC-008` and `PRICE-009` are closed.
+3. Readiness → Projection → Publication → PriceList → BuyerCatalog — public-runtime harness IMPLEMENTED and merged in #118; its exact head passed Verify/MDM/Syntha V2 CI/Product Commercialization Acceptance. Current P0.4 additionally converges ProductSku-exact pricing/new-write guards. Intended-environment `PROD-PROVEN` evidence is still pending and the semantic contract remains PARTIAL until `COMM-LC-008`, remaining `PRICE-009` depth and `PUB-005` downstream compatibility seams are closed.
