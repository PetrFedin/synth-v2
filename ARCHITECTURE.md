# Syntha V2 — Platform Master Specification

> **Canonical living specification / architecture / product requirements / UI contract**
> Status date: **2026-09-08**
> Baseline when this master specification was established: `main@4f5c452dede1ce9c8ffe518eddb8b6632cc89ad0`
> Document status: **AUTHORITATIVE / LIVING**

This file is the single authoritative map of what Syntha V2 is, how it works, what is implemented, what is incomplete, how modules are connected, what users see, and what every future change must preserve or update. Detailed documents under `docs/` remain supporting evidence and deep dives; they may add rationale and implementation detail, but they must not contradict this file.

If code, database, API, UI, workflow or another document disagrees with this master specification, the discrepancy is an architecture defect. A change is not complete until implementation, tests and this specification describe the same behavior.

---

## 0. Mandatory documentation governance

### 0.1 Non-negotiable synchronization rule

Every change that affects any of the following MUST update this file in the same pull request:

- business capability, workflow, lifecycle, state transition or action;
- entity, field, identifier, relation, ownership boundary or immutable lineage;
- API route, request/response field, command, error, idempotency or authorization rule;
- PostgreSQL table/column/index/constraint/trigger/migration or persistence semantics;
- UI screen, workspace, block, card, table, inspector, filter, field, button, menu item, icon, status, empty/loading/error state, confirmation or keyboard behavior;
- typography, colour, spacing, radius, elevation, responsive breakpoint or another design token;
- role/capability visibility, action availability or organisation isolation;
- runtime/startup/shutdown, deployment, environment variable, worker, health/readiness, observability or external integration;
- canonical business-chain relation, test/acceptance proof, architecture gap or production-readiness status.

A PR that changes runtime/product behavior without changing `ARCHITECTURE.md` is incomplete even if all unit tests pass. GitHub CI enforces this rule for governed source/runtime surfaces.

### 0.2 What must be recorded for a changed feature

The edited section must answer, where applicable:

1. **Purpose** — why the capability exists and who uses it.
2. **Entry point** — screen, route, command or upstream event that starts it.
3. **Inputs** — every material field, type, required/optional rule, source and validation.
4. **Actions** — buttons/commands and their prerequisites.
5. **State transition** — old state → action → new state; forbidden transitions.
6. **Relations** — upstream/downstream entities and exact immutable IDs/snapshots.
7. **Effects** — records/events/reservations/costs/publications that change or must not change.
8. **Permissions** — actor/capability/organisation boundary.
9. **UI contract** — block placement, fields, icons, labels, controls, states and responsive behavior.
10. **API contract** — route/method/request/response/error/idempotency.
11. **Persistence** — table/columns/constraints/transactional guarantees.
12. **Failure behavior** — fail-closed conditions, recovery/retry/correction path.
13. **Evidence** — tests, PostgreSQL integration and/or live acceptance proving the behavior.
14. **Status** — IMPLEMENTED / PARTIAL / PLANNED / GAP / DEPRECATED.
15. **Change reference** — PR/commit and affected sections in the change register.

### 0.3 Status vocabulary

| Status | Meaning |
|---|---|
| `IMPLEMENTED` | Canonical executable path exists in the named layer and is guarded by automated tests. |
| `PARTIAL` | Useful implementation exists, but one or more required layers/relations/UI/E2E proofs remain incomplete. |
| `PLANNED` | Intended canonical behavior is specified but implementation has not started or is not canonical. |
| `GAP` | A confirmed missing/broken/ambiguous path that can violate the intended end-to-end system. |
| `DEPRECATED` | Compatibility-only behavior retained temporarily; it must not be extended as a new source of truth. |
| `PROD-PROVEN` | The relevant path is exercised end-to-end through supported public/runtime boundaries against PostgreSQL and has operational evidence. |

A file, migration, mock screen or domain function alone does not qualify a capability as `PROD-PROVEN`.

### 0.4 Source hierarchy

When resolving ambiguity, use this order:

1. `ARCHITECTURE.md` — canonical platform contract and current status.
2. Executable invariant + migrations + public API/OpenAPI — actual enforceable behavior.
3. `docs/architecture/*`, governed domain docs, ODS and KPI documents — supporting deep specifications.
4. Tests — executable examples/evidence.
5. README/Cursor instructions — onboarding/operations summaries.
6. Legacy compatibility code or stale PRs — never a source of new semantics.

If levels 1 and 2 disagree, fix the discrepancy; do not silently choose one.

---

## 1. Platform purpose and canonical operating chain

Syntha V2 is a standalone fashion B2B operating platform for brands, retailers, distributors and production partners. It must operate as one connected product rather than a collection of screens.

### 1.1 End-to-end business chain

```text
DESIGN
→ PLAN
→ DEVELOP
→ SOURCE
→ SAMPLE
→ TECH PACK
→ PRODUCE
→ QUALITY
→ PRODUCT READINESS
→ COMMERCIAL PROJECTION
→ PUBLISH
→ COLLECTION / SHOWROOM / LINE SHEET
→ BUYER CATALOG
→ ASSORT / SELECTION / COLOR × SIZE MATRIX
→ WHOLESALE ORDER
→ ORDER COMMIT SNAPSHOT
→ SUPPLY COMMITMENT
→ SHIPMENT / RECEIPT
→ ACTUAL COST / LANDED COST
→ MARGIN ACTUALIZATION / CLOSE
→ SELL-THROUGH / KPI / SUPPLIER PERFORMANCE
```

No downstream module may reconstruct historical truth from mutable upstream masters when an immutable snapshot/version already exists.

### 1.2 Canonical immutable commercial/physical lineage

```text
ProductStyle
→ StyleVersion
→ Colorway
→ ProductSku
→ ProductReadinessSnapshot
→ CommercialProductProjectionVersion
→ CommercialPublication
→ PriceListVersion
→ BuyerCatalogVersion
→ Buyer Selection
→ WholesaleOrder
→ OrderCommitSnapshot
→ SupplyCommitment
→ Shipment / ShipmentLine
→ ActualCostLedgerEntry
→ LandedCostSnapshot
→ CostAllocationRunSnapshot
→ MarginActualizationSnapshot
→ CostCloseReadinessSnapshot
→ CostCloseSnapshot
```

When a late cost is recorded after close, the immutable continuation is explicit rather than a rewrite of the close:

```text
CostCloseSnapshot
→ PostCloseAdjustment
→ adjusted LandedCostSnapshot
→ MarginActualizationSnapshot(allocationStatus=pending-post-close)
→ exact new CostAllocationRunSnapshot
→ PostCloseAllocationReconciliationSnapshot
→ reconciled MarginActualizationSnapshot(allocationStatus=current)
```

Physical SKU identity after order commitment is not a display string. The canonical line identity is the exact immutable pair:

```text
orderLineNo + productSkuId
```

Textual `sku` remains useful for human display and compatibility but must not replace exact physical lineage where ambiguity is possible.

---

## 2. Repository, runtime and deployment architecture

### 2.1 Repository boundaries — IMPLEMENTED

- Repository: `PetrFedin/synth-v2`.
- Node.js: `>=22`.
- PostgreSQL: version 17 baseline.
- Cross-module imports are allowed only through a module `public.mjs` boundary.
- Applied SQL migrations are immutable and checksum-verified.
- Business mutations use durable command/idempotency identity and transactional outbox discipline where the bounded context mutates durable business state.
- Runtime dependencies stay minimal: `pg` only. Development dependencies are limited to the pinned type checker (`typescript`, `@types/node`) that backs the type contract gate.
- Organisation isolation is mandatory.

### 2.2 Supported process path — PROD-PROVEN for startup lifecycle

Supported entrypoint:

```text
npm start
→ scripts/start.mjs
→ runtime configuration
→ PostgreSQL connection
→ checksum/advisory-lock migrations
→ worker registration/start
→ HTTP listen
→ /health
→ /ready
→ SIGTERM
→ graceful HTTP/workers/PostgreSQL shutdown
```

`npm run smoke:runtime` runs this real process against `POSTGRES_TEST_URL`, disables external side effects, proves `/health` and migration/worker-aware `/ready`, then proves clean SIGTERM shutdown. It must never fall back to normal development or production data.

### 2.3 Local database topology — IMPLEMENTED

| Purpose | Variable | Default local endpoint |
|---|---|---|
| Development workspace | `SYNTHA_V2_DATABASE_URL` | PostgreSQL 17 on `127.0.0.1:5434` |
| Verification/tests | `POSTGRES_TEST_URL` | isolated PostgreSQL 17 on `127.0.0.1:5435` |

Verification may create durable fixtures; it must never point at workspace or production data.

### 2.4 Operational endpoints — IMPLEMENTED

- `GET /health` — process liveness.
- `GET /ready` — traffic readiness; includes PostgreSQL, migration state and operational worker registry.
- `GET /openapi.json` — current API contract.
- `/v2/*` — supported business API.
- `/` — standalone browser workspace.

### 2.5 Bootstrap owner — IMPLEMENTED

`npm run bootstrap:owner` is deterministic provisioning, not password reset. It preserves deterministic bootstrap identities, serializes concurrent calls with PostgreSQL advisory locking, replays only exact ownership/password/topology, and fails closed on mismatch or ambiguity.

### 2.6 Product commercialization acceptance runtime — IMPLEMENTED / LIVE EVIDENCE PENDING

`npm run acceptance:product-commercialization` is the P0.3 public-runtime acceptance entry point. It starts from a fresh positive Product Readiness graph created by the existing canonical READY harness, then continues through the current executable commercial boundary without direct SQL business mutation.

The acceptance setup may migrate the configured PostgreSQL target, bootstrap source-controlled governed MDM through the canonical MDM bootstrap, and ensure the reserved acceptance authentication identities. Business facts are then created only through authenticated `/v2` commands with `Idempotency-Key`.

The stable acceptance organisation/membership bootstrap is itself replay-safe command setup. Its durable command IDs always use the deterministic membership `createdAt=2026-08-31T00:00:00.000Z`; wall-clock time is forbidden from that command payload because replaying the same command ID with a different fingerprint must correctly fail with `COMMAND_ID_CONFLICT`. This deterministic timestamp is acceptance namespace metadata only and does not replace the real timestamps of business facts created through public `/v2` mutations. `tests/production-reference-bootstrap-replay.test.mjs` proves two consecutive bootstraps produce identical command fingerprints and references.

Two real actor contexts are required:

- reserved brand owner `syntha-acceptance-brand-owner`, authenticated with `SYNTHA_ACCEPTANCE_EMAIL` / `SYNTHA_ACCEPTANCE_PASSWORD` or optional short-lived `SYNTHA_ACCEPTANCE_TOKEN`;
- reserved shop owner `syntha-acceptance-shop-owner`, authenticated with `SYNTHA_ACCEPTANCE_SHOP_EMAIL` / `SYNTHA_ACCEPTANCE_SHOP_PASSWORD` or optional short-lived `SYNTHA_ACCEPTANCE_SHOP_TOKEN`.

`SYNTHA_ACCEPTANCE_BASE_URL` and the configured database URL must refer to the same environment; PostgreSQL is read after HTTP mutations to prove exact persistence and immutable lineage. Non-local execution remains fail-closed unless the target is HTTPS and `SYNTHA_ACCEPTANCE_ALLOW_REMOTE=true` is explicitly set. The dedicated GitHub workflow `Product Commercialization Acceptance` starts the supported `scripts/start.mjs` process against PostgreSQL 17, waits for `/ready`, executes this same command, terminates the process with SIGTERM and retains diagnostics.

PR #118 is merged as `main@0048d10c410231055d13436d61d0cf33db3e95b7`. Its exact pre-merge head `eb86a04b7ac9c6a0ba743882f59a6b33dfbfd113` passed Verify `33979864431`, Syntha V2 CI `33979864438`, MDM Reference Data `33979865154` and Product Commercialization Acceptance `33979864435`. That is repository/runtime CI evidence for the implemented slice; it is **not** an intended deployment-environment `PROD-PROVEN` claim. `PROD-PROVEN` remains forbidden until the specifically designated live acceptance environment completes the gate successfully.

---

## 3. Architecture invariants
### 3.1 One Product Master

Product Identity V2 is the only canonical Style/StyleVersion/Colorway/Size/SKU hierarchy. `catalog_skus` is **DEPRECATED compatibility state**, never a second Product Master. At the current P0.4 intermediate state some legacy technical/UI workspaces still have compatibility mutation/read paths, so it is inaccurate to claim that the table is already globally read-only. Those compatibility writes may preserve old workspace behavior only; they may not originate new Product semantics or new commercial truth.

Canonical new commercial writes are exclusively `Product Identity → ProductReadinessSnapshot → CommercialProductProjectionVersion → projection-backed CommercialPublication → ProductSku-exact PriceListVersion → BuyerCatalogVersion`. Migration 075 and the domain/API contract fail closed if a historical V1 flat CommercialPublication/PriceList is used to mint a fresh PriceListVersion/BuyerCatalogVersion. Historical V1 snapshots remain readable exactly as stored and never receive guessed ProductSku IDs. `PUB-005` therefore remains OPEN/PARTIAL until `/v2/catalog/skus` legacy mutations/UI dependencies and the remaining Selection legacy fallback are retired or explicitly bounded as read-only history.

### 3.2 Technical truth is not commercial truth

The only formal PLM-to-commerce handoff is:

```text
ProductReadinessSnapshot
→ CommercialProductProjectionVersion
→ CommercialPublication
```

Buyer/catalog/order code must not bypass this handoff by reading mutable live PLM/MDM fields to reconstruct an already published commercial fact.

### 3.3 Historical facts freeze versions

Readiness, projection, publication, buyer catalog, selection, order commitment, supply, actual cost, allocation, margin, close, post-close adjustment and post-close allocation reconciliation records preserve exact source versions/IDs. Current master labels cannot retroactively redefine historical facts. A reconciliation adds a new immutable fact and a new current margin; it never edits the CostClose, PostCloseAdjustment, pending margin or prior allocation run.

### 3.4 Physical lineage

Canonical supply/physical execution uses exact `ProductSku` plus committed order line. New exact paths must not join by fuzzy SKU/name/date when immutable identifiers exist.

### 3.5 Inventory

Inventory truth is centralized around location/balance/reservation/allocation/movement. ATS is derived centrally from canonical balance semantics. Mutation paths require idempotency, locking and reconciliation; a buyer/order screen must not maintain a second stock balance. Migration 065 deliberately materializes exactly one canonical `product_sku_inventory_balances` identity row when a new ProductSku is inserted, keyed by that ProductSku and same brand with `available_quantity=0`, `reserved_quantity=0` and `version=1`. Creating this zero balance identity is ProductSku→Inventory identity registration, not an inventory movement, receipt, reservation, allocation or stock increase; movement-ledger and quantity deltas must remain unchanged until a real inventory business event occurs.

### 3.6 Pricing

Pricing exposed to buyers is server-authoritative and snapshot/version based. Client-calculated price is presentation only and cannot become order truth.

### 3.7 Corrections, not rewrites

Immutable commercial, physical and economic ledgers are corrected through append-only reversal/replacement/adjustment/reconciliation semantics. Historical records are not edited in place to make current totals look correct.

---

## 4. Identity, authentication, roles and capabilities

### 4.1 Authentication — IMPLEMENTED

Syntha owns authentication; no external identity provider is canonical. Browser session stores the bearer access token under `syntha-v2-session` in `sessionStorage`. Login uses `/v2/auth/login`; authenticated identity is verified via `/v2/auth/me`; logout calls `/v2/auth/logout` and clears local session state.

### 4.2 Organisation context — IMPLEMENTED

Workspace boot loads user identity, workspace data and notifications, then drains membership and organisation pages before declaring the workspace foundation complete. Failure to fully load membership/organisation context fails with `WORKSPACE_FOUNDATION_INCOMPLETE` instead of rendering a potentially cross-organisation partial state.

### 4.3 Capability rule

A visible mutation control is forbidden unless all of the following exist together:

```text
visible action
+ capability/role check
+ handler
+ public API route
+ application method
+ domain validation
+ persistence transaction
+ automated interaction contract
```

Destructive actions require explicit confirmation or a reason form. Client validation mirrors domain boundaries but never replaces backend validation.

---

## 5. Product and development domain model

### 5.1 Product Identity V2 — IMPLEMENTED/PARTIAL UI

#### ProductStyle
Stable product identity. Owns lifecycle identity; versioned technical content belongs to `StyleVersion`, not mutable fields on style.

#### StyleVersion
Immutable exact technical version. It preserves predecessor/version continuity, bilingual title/content references, governed MDM references and content identity. Downstream readiness refers to one exact StyleVersion.

`StyleVersion.categoryRef` is the canonical governed category reference for a new technical fact. The compatible dictionary family is exactly `assortment.category`; a new reference pins the exact MDM `(entryId, version)` and is accepted only when that exact version exists, is the current entry version for the new write, belongs to the compatible dictionary, is active, is approved when approval is required, is effective at the business write time, and is global or belongs to the permitted organisation scope. Unknown, stale-version, wrong-dictionary, inactive, unapproved or out-of-effect references fail closed before the StyleVersion is created. The resolved reference is frozen through the immutable MDM usage/version snapshot so later MDM changes cannot redefine historical Product Identity meaning.

#### Colorway
Immutable variant bound to exactly one StyleVersion and brand context; may pin an exact governed colour reference/version.

#### SizeScale / SizeScaleVersion / SizeValue
Stable size system plus ordered immutable version/value structure. Ordered size truth feeds variant publication and Color × Size ordering.

#### ProductSku
Canonical immutable sellable/physical identity:

```text
exact StyleVersion
+ exact Colorway
+ same-brand ordered SizeValue
```

A ProductSku must never be replaced by a plain textual SKU string in canonical physical lineage. PostgreSQL migration 065 also creates that ProductSku's single zero-initialized canonical InventoryBalance identity at insert time; this does not create a second inventory truth and does not itself represent warehouse stock or movement.

#### Product media
Immutable media/order references can be scoped to StyleVersion/Colorway and selected into readiness/projection. Buyer gallery depth remains PARTIAL.

Supporting detail: `docs/architecture/product-identity-v2.md`.

### 5.2 PLM/development workspaces

Current executable workspace assets include Planning, Styles, Materials, BOM, Measurements, Samples, Sourcing, Tech Packs, Production Orders, Production Executions and Final Quality. Their visual contract is ODS v1. Their business truth must converge on Product Identity rather than create a second style/SKU model.

| Workspace | Core purpose | Current status | UI/ODS status |
|---|---|---|---|
| Planning | collection/line planning and development readiness context | PARTIAL canonical integration | shared ODS primitives, remaining compatibility debt |
| Styles | development-facing style work | PARTIAL canonical Product Identity convergence | PARTIAL ODS migration |
| Materials | material master/development facts | IMPLEMENTED/PARTIAL integration | PARTIAL ODS migration |
| BOM | component/material composition | IMPLEMENTED | PARTIAL ODS migration |
| Measurements | POM/spec/revision workflow | IMPLEMENTED | PARTIAL ODS migration |
| Samples | sample lifecycle/evidence | IMPLEMENTED | ODS-native |
| Sourcing | RFQ/quote/supplier sourcing | IMPLEMENTED/PARTIAL depth | next ODS debt-burn-down target |
| Tech Packs | frozen technical package/readiness dependencies | IMPLEMENTED | ODS-native |
| Production Orders | production commitment/order view | IMPLEMENTED | ODS-native |
| Production Executions | execution/milestones/progress | IMPLEMENTED | ODS-native |
| Final Quality | final QC evidence/disposition | IMPLEMENTED | ODS-native |

The master specification must be expanded whenever one of these workspaces gains/removes a field, action, state, panel or dependency. Existing module-specific docs remain supporting evidence; they are not permission to omit changes here.

#### Canonical Measurement Chart — IMPLEMENTED / live execution evidence pending

The canonical Measurement Chart is repository-authoritative measurement evidence for Product Identity and Product Readiness. Its immutable identity context is the exact same-brand tuple `StyleVersion + Colorway + SizeScaleVersion`; it never derives canonical identity from a textual SKU. The chart pins the exact governed MDM version of its `measurement.unit` and every `measurement.point`, preserves the ordered Product `SizeValue` matrix, and is the only measurement-chart family eligible to satisfy the canonical Product Readiness `measurements` gate.

Supported public API contract:

```text
POST  /v2/measurements/canonical
GET   /v2/measurements/canonical/:chartId
PATCH /v2/measurements/canonical/:chartId
POST  /v2/measurements/canonical/:chartId/publish
```

There is deliberately no canonical collection `GET /v2/measurements/canonical` and no canonical `PUT` update route. OpenAPI must describe exactly the supported methods; introducing either absent route as documentation-only behavior is forbidden.

Create accepts exactly these top-level fields and rejects unknown fields:

```text
styleVersionId
colorwayId
sizeScaleVersionId
measurementUnitEntryId
baseSizeValueId
sizes
points
notes
```

Each `sizes` row is exactly `{ sizeValueId }`. Each `points` row is exactly `{ pointEntryId, description, toleranceMinus, tolerancePlus, measurements }`, and every measurement value is exactly `{ sizeValueId, value }`. `notes` is a required request key and may normalize to null/empty semantics according to the domain optional-text rule; callers may not omit the key. The update request is the exact editable set above without the three identity fields plus required optimistic-concurrency `expectedVersion`. Publication accepts exactly `{ expectedVersion }`.

All three canonical mutations require authenticated actor context, the existing measurement-management capability/brand ownership checks and `Idempotency-Key`. The service resolves the exact StyleVersion, Colorway, SizeScaleVersion and SizeValues; resolves the current effective MDM entries for `measurementUnitEntryId` and each `pointEntryId`; and fails closed for missing/mismatched Product Identity, wrong brand, wrong dictionary, inactive/unapproved/not-effective MDM entries, unexpected fields, stale `expectedVersion`, duplicate size/point identities or invalid/incomplete matrices. Russia-first canonical garment measurement units must be governed `measurement.unit` length entries using the metric system.

The canonical runtime MDM truth is the exact immutable `mdm_entry_versions.snapshot` captured by PostgreSQL from the normalized `mdm_entries` row (`to_jsonb(NEW)`), not the source-registry JSON shape used before bootstrap. The operational bootstrap persists localized names as string values in `translations = { ru, en }` and localized descriptions inside the same versioned `attributes` object (`descriptionRu` / `descriptionEn`). Canonical Measurement point localization therefore reads `pointRef.snapshot.translations.ru`, `pointRef.snapshot.translations.en` and RU description from `pointRef.snapshot.attributes.descriptionRu`; semantic properties such as `dimension` come from that same `snapshot.attributes`. Source dataset aliases such as `name_ru`, `name_en` and `description_ru` are bootstrap inputs only and are forbidden as runtime fallbacks. Missing/invalid normalized runtime localization fails closed with `MEASUREMENT_POINT_NAME_INVALID` rather than consulting a second source truth. `tests/measurement-runtime-mdm-snapshot.test.mjs` pins the exact persisted snapshot shape and source-alias-only rejection.

Lifecycle is append/history preserving:

```text
DRAFT → PUBLISHED
PUBLISHED --PATCH with expectedVersion--> new DRAFT revision
```

A PATCH to a published chart archives the published revision before saving the new DRAFT revision; it never destructively mutates published historical evidence. Publication requires at least one governed point and a complete ordered value matrix for every selected Product SizeValue, including the base size. Readiness reads the latest exact PUBLISHED canonical chart for the Product Identity context and retains the exact chart/version/MDM evidence rather than reinterpreting it from current master data.

The older SKU-oriented `/v2/measurements`, `/v2/measurements/:sku` and `/v2/measurements/:sku/publish` routes remain a **DEPRECATED compatibility surface for Product Identity/readiness semantics** until consumer/UI migration is audited. They must not be used as canonical evidence for new ProductReadiness writes and must not acquire new Product Identity semantics. PR #117 synchronizes the previously missing canonical runtime routes into the existing measurement OpenAPI module rather than creating a second API truth; `tests/measurement-canonical-openapi.test.mjs` pins exact methods, request fields, idempotency/auth, absence of canonical collection-GET/PUT and continued legacy compatibility. The separate positive READY acceptance harness now consumes this exact public canonical route set and asserts the PUBLISHED chart/MDM/size matrix through same-environment PostgreSQL. The runtime/OpenAPI/acceptance contract is `IMPLEMENTED`; actual intended live acceptance execution is still pending and therefore is not `PROD-PROVEN`.

### 5.3 Operational MDM reference profile — IMPLEMENTED/PARTIAL taxonomy depth

The source-controlled `mdm/reference/*.json` files form the bootstrap operational profile `RU_FASHION_CORE`. They are modular datasets, not independent copies of one monolithic reference file. A modular dataset must still declare RU market, RU/EN language coverage, a registered source, governed dictionary metadata and valid entry governance, but it is not required to duplicate unrelated size/unit dictionaries merely to exist.

`npm run validate:mdm` loads all operational reference datasets first and then validates the required Russia-fashion core across the complete loaded profile. The required size-system codes are `RU_APPAREL_NUMERIC`, `INT_ALPHA` and `EU_FOOTWEAR`; required operational units are `CM`, `MM`, `M`, `G`, `KG` and `PCS`. Those mandatory profile checks are global across `mdm/reference`, while semantic cross-entry references that are deliberately dataset-local remain local: seeded size values/footwear sizes must resolve `size_system_entry_id` to a `size.system` entry in the same dataset, and a seeded `measurement.point` must resolve `default_unit_entry_id` to a `measurement.unit` entry in the same dataset. Universal size-conversion fields remain forbidden; explicit governed brand/market conversion records are required instead.

PR #117 adds `mdm/reference/russia-fashion-assortment-core.json` as an operational governed category seed from registered source `syntha_operational_master`. It defines dictionary:

```text
id: mdm-dictionary:assortment-category
code: assortment.category
data_class: classifier
scope_model: global
hierarchy_enabled: true
effective_dated: true
approval_required: true
```

and the initial active entry:

```text
id: mdm-entry:assortment-category:apparel
version: 1
code: APPAREL
RU: Одежда
EN: Apparel
effective_from: 2026-08-31T00:00:00.000Z
hierarchy_level: category
product_family: apparel
```

`APPAREL` is intentionally only the governed top-level apparel category. It does **not** stand in for a subcategory or `assortment.product_type`, and it must not be overloaded with gender, collection, season or arbitrary style attributes. More detailed category/product-type taxonomy is added only through separately governed MDM versions and registered source/effective-date semantics.

This source-controlled seed makes an exact governed category reference available to the positive Product Readiness path; it does not by itself make Product Readiness or downstream commercialization `PROD-PROVEN`.

---

## 6. Product readiness and commercialization

### 6.1 ProductReadinessSnapshot — IMPLEMENTED

Purpose: freeze whether a specific StyleVersion is commercially releasable based on authoritative route-aware technical evidence and commercial preparation.

Rules:

- one snapshot is immutable history;
- repository-authoritative evidence is fail-closed;
- external evidence cannot override a missing/failed repository gate when a canonical repository source exists;
- exact Product Identity/PLM source versions are retained;
- blocked snapshots remain historical evidence and are not rewritten to READY later.

#### ACC-004 Product Identity → Readiness dual acceptance harness — IMPLEMENTED / LIVE EVIDENCE PENDING

PR #117 implements two independent Product Identity → Product Readiness acceptance scenarios under the existing `npm run acceptance:product-readiness` entry point. The runner uses the same acceptance environment/origin/authentication controls as `acceptance:collection`, migrates the configured PostgreSQL target, idempotently bootstraps every source-controlled `mdm/reference/*.json` dataset through the canonical `bootstrapMdmReference` infrastructure path, bootstraps only the stable acceptance organisations/roles, and performs all business mutations through authenticated public `/v2` API calls. The MDM bootstrap is controlled environment/master-data setup, not a direct SQL business mutation.

**Scenario A — fail-closed, preserved unchanged.** It creates:

```text
ProductStyle
→ StyleVersion(no categoryRef)
→ Colorway
→ SizeScale
→ SizeScaleVersion
→ SizeValue
→ ProductSku
→ ProductMedia(hero)
→ ProductReadinessSnapshot(BLOCKED)
→ rejected CommercialProductProjectionVersion attempt
```

The scenario deliberately uses `developmentRoute=READY_GOODS`, omits governed `StyleVersion.categoryRef`, and creates no canonical Measurement Chart. This remains a controlled negative fixture even though `RU_FASHION_CORE` contains exact governed `assortment.category / APPAREL` data. It supplies immutable external evidence only for `sourcing`, `purchase_or_production_commitment`, `quality` and `compliance`, plus valid commercial media/content/terms/availability. The readiness result must be **blocked** with exactly `category` and `measurements`, with blocked count exactly `2`. Any READY result, missing blocker, extra blocker or changed blocker count fails closed.

After PostgreSQL confirms that blocked snapshot, Scenario A calls `POST /v2/product/readiness/:readinessId/commercial-projection` with `expectedLatestVersionNo=0`. The only accepted outcome is HTTP `422` with `COMMERCIAL_PROJECTION_READINESS_BLOCKED`; exact StyleVersion/brand `commercial_product_projection_versions` rows must be zero both before and after the rejected command. This negative scenario is never replaced by the happy path.

**Scenario B — positive canonical.** A separate graph is created through supported public API:

```text
ProductStyle
→ StyleVersion(categoryRef=APPAREL v1)
→ Colorway
→ SizeScale
→ SizeScaleVersion(size.system=INT_ALPHA v1)
→ SizeValue(M / mdm-entry:size:int-m v1)
→ ProductSku
→ ProductMedia(hero)
→ canonical Measurement Chart DRAFT
   measurement.unit=CM v1
   measurement.point=CHEST_CIRC v1
   exact SizeValue matrix
→ canonical Measurement Chart PUBLISHED
→ ProductReadinessSnapshot(READY, blockers=0)
```

Scenario B does not use external evidence for the canonical `category` or `measurements` gates. It uses repository-authoritative StyleVersion MDM usage plus the exact PUBLISHED canonical Measurement Chart and uses external immutable evidence only for the same route-aware `sourcing`, `purchase_or_production_commitment`, `quality` and `compliance` gates that have no canonical repository source in this READY_GOODS acceptance fixture. Every business mutation is authenticated and idempotent; acceptance command IDs are bounded to the public `Idempotency-Key` maximum of 128 characters even with the maximum accepted 80-character runId.

The positive same-environment PostgreSQL assertion joins the exact StyleVersion category columns and `mdm_usage_snapshots`, Colorway, SizeScaleVersion, SizeValue, ProductSku, ProductMedia, canonical Measurement Chart, chart-size/point/value matrix and ProductReadinessSnapshot. It pins exact `APPAREL`, `INT_ALPHA`, `INT_M`, `CM` and `CHEST_CIRC` entry versions; exact same-brand Product Identity; exact chart StyleVersion/Colorway/SizeScaleVersion/base SizeValue; PUBLISHED chart revision; exact measurement value; and READY with zero blocked dimensions. Before/after isolation requires exactly one intentional inventory identity delta caused by ProductSku registration: `inventory_balance_rows +1` for the exact new ProductSku, whose persisted balance must be same-brand, `available=0`, `reserved=0`, `version=1`. Aggregate inventory quantities and the inventory movement ledger must remain unchanged, as must CommercialPublication, PriceListVersion, BuyerCatalogVersion, Selection/Order, SupplyCommitment and ActualCost state. Any second balance row, non-zero quantity, movement-ledger delta or downstream commercial/economic mutation fails the READY acceptance. The standalone `npm run acceptance:product-readiness` command deliberately stops this Scenario B at READY; the separate P0.3 `acceptance:product-commercialization` command creates its own fresh positive READY graph and continues that exact graph downstream without altering Scenario A or weakening the readiness gate.

Automated evidence is split so one scenario cannot weaken the other: `tests/product-readiness-live-acceptance.test.mjs` pins Scenario A, `tests/product-readiness-ready-live-acceptance.test.mjs` pins Scenario B including wrong-actor and MDM/published-chart drift failures, `tests/mdm-assortment-category-reference.test.mjs` pins the operational category dataset/validator contract, `tests/measurement-canonical-openapi.test.mjs` pins the canonical measurement API/OpenAPI contract, and `tests/measurement-runtime-mdm-snapshot.test.mjs` prevents source-registry localization aliases from reappearing as a second runtime MDM truth. These tests and the executable harness establish `IMPLEMENTED` status, not `PROD-PROVEN`. `PROD-PROVEN` requires a successful `npm run acceptance:product-readiness` execution against the intended live runtime and same PostgreSQL environment. The full Product → Margin `ACC-004` gate remains OPEN/PARTIAL.

### 6.2 CommercialProductProjectionVersion — IMPLEMENTED / positive acceptance harness added

Created only from an eligible READY ProductReadinessSnapshot. It is an immutable commercial projection, not a live PLM read model. Projection continuity/version lineage is preserved and the current executable constructor persists it directly with immutable `status=published`.

The P0.3 acceptance runner calls `POST /v2/product/readiness/:readinessId/commercial-projection` with `expectedLatestVersionNo=0` against the exact positive READY snapshot, then reads the result through `GET /v2/product/commercial-projections/:projectionId`. The gate requires version `1`, status `published`, exact `readinessSnapshotId`, exact `styleVersionId` and stable `contentHash`; same-environment PostgreSQL must show the same values. Scenario A from #117 continues to prove the inverse fail-closed boundary: a blocked snapshot cannot create any projection row.

This positive public-runtime harness is `IMPLEMENTED`. A successful intended-environment execution is still required before any `PROD-PROVEN` claim.

### 6.3 CommercialPublication — IMPLEMENTED atomic published snapshot / PARTIAL staged lifecycle

The authoritative current runtime does **not** implement the previously documented staged lifecycle. The executable canonical V2 path today is:

```text
READY ProductReadinessSnapshot
→ CommercialProductProjectionVersion(status=published)
→ POST /v2/commercial-publications
→ CommercialPublication(formatVersion=2, status=published)
```

The V2 CommercialPublication is an immutable projection-backed snapshot. Creation requires the exact brand-owned CommercialProductProjectionVersion and an exact Collection assignment to the same `StyleVersion`; it freezes projection id/version/content hash, readiness snapshot id, StyleVersion id, currency, exact ProductSku line terms and the Style → Colorway → ordered SizeValue/ProductSku hierarchy. PostgreSQL independently checks projection/collection/style/currency/payload lineage and published snapshots are not mutated in place.

The desired production lifecycle remains a required P0 target:

```text
DRAFT → READY → PUBLISHED → SUPERSEDED / ARCHIVED
```

No canonical V2 DRAFT/READY/SUPERSEDED/ARCHIVED CommercialPublication state transitions or public mutation routes exist at this baseline. Therefore that lifecycle is `GAP`, not IMPLEMENTED. It must be added without creating a second publication model, or the target contract must be explicitly revised if product governance chooses a different single canonical lifecycle. Historical flat-catalog publication compatibility remains DEPRECATED migration debt and may not receive new Product semantics.

### 6.4 PriceListVersion — IMPLEMENTED core / PARTIAL production pricing depth

The current V2 PriceListVersion is an immutable server-authored `published` snapshot created from one exact projection-backed CommercialPublication for one buyer shop. It freezes Publication id, exact commercial projection id/version/content hash, readiness snapshot id, StyleVersion id, brand/shop identity, currency and per-ProductSku wholesale price, RRP, MOQ, delivery window and availability terms. BuyerCatalogVersion pins this exact PriceListVersion, and downstream commercial truth must not read a later mutable price value after freeze.

Canonical buyer-specific price adjustment is now ProductSku-exact. `POST /v2/commercial-publications/:publicationId/buyer-catalogs` accepts optional `priceOverrides[]` rows with exactly `{ productSkuId, wholesalePriceMinor }`. Textual `sku` is not an override identity and client-supplied `unitPrice` is rejected. The server validates a unique ProductSku that exists in the frozen publication, validates a positive bounded integer minor amount, derives `unitPrice = wholesalePriceMinor / 100`, and preserves source RRP/MOQ/delivery/availability and exact projection lineage. Duplicate, unknown, malformed or textual-SKU override attempts fail closed before persistence.

A historical `formatVersion=1` CommercialPublication can still be read for compatibility but cannot originate a new PriceListVersion. A PriceListVersion used for a new BuyerCatalog must itself preserve exact projection-backed ProductSku lines and hierarchy. PostgreSQL migration 075 independently enforces these invariants for direct/internal insert bypasses without rewriting old rows.

Current pricing-depth gaps are explicit rather than hidden:

- PriceListVersion has no canonical explicit `market` field at this boundary;
- it has no price-list-level `effective_from` / `effective_to` contract;
- governed tax-treatment/customer-eligibility depth beyond the already frozen shop/access context remains to be specified where the business contract requires it.

The textual-SKU pricing identity seam is CLOSED in this P0.4 slice, but `PRICE-009` remains OPEN/PARTIAL until market/effective-period and any required tax/eligibility semantics are complete.

### 6.5 BuyerCatalogVersion — IMPLEMENTED rich backend / PARTIAL buyer-facing depth

The V2 BuyerCatalogVersion is immutable and `published`. It freezes the exact projection-backed CommercialPublication, exact ProductSku-price PriceListVersion, projection/readiness/StyleVersion lineage, brand/shop identity, open Showroom context, accepted invitation/access grant and currency. Its rich payload preserves Style → Colorway → ordered SizeValue/ProductSku hierarchy and exact ProductSku commercial lines rather than requiring a live PLM reconstruction.

Fresh BuyerCatalogVersion writes are fail-closed on non-V2/legacy publication sources, a PriceListVersion from another publication/brand/shop/currency/lineage, changed frozen non-price terms, changed rich hierarchy, closed/mismatched Showroom or invalid buyer access. Historical V1 BuyerCatalogVersion records remain readable as immutable compatibility history; they are not silently upgraded or allowed to seed a new canonical buyer catalog.

The P0.3 acceptance reads the same BuyerCatalogVersion as the brand actor, the authorized shop actor and through the Showroom access route, then joins the exact catalog/price/publication/projection/readiness chain in the same PostgreSQL environment. Browser Showroom/Linesheet and Color × Size UX remain PARTIAL and must consume this frozen rich truth rather than a flat mutable catalog.

### 6.6 P0.3 READY → BuyerCatalog acceptance — IMPLEMENTED / LIVE EVIDENCE PENDING

`npm run acceptance:product-commercialization` executes one fresh namespace-isolated public-runtime slice:

```text
positive Product Identity + governed MDM + canonical Measurement Chart
→ ProductReadinessSnapshot READY
→ CommercialProductProjectionVersion
→ Campaign / Collection
→ exact Collection × StyleVersion assignment while Collection is DRAFT
→ Collection PUBLISHED
→ Showroom OPEN
→ active brand ↔ shop relationship
→ accepted Showroom invitation
→ projection-backed CommercialPublication
→ PriceListVersion
→ BuyerCatalogVersion
```

Every business mutation is made through authenticated `/v2` routes and carries an idempotency key. The brand owner and shop owner are separate real actors so relationship and invitation acceptance cannot be silently performed under one unrestricted context. PostgreSQL is used only for governed setup/persistence proof, not to substitute a business mutation.

Acceptance requires exact continuity of `StyleVersion`, `ProductSku`, ProductReadinessSnapshot, CommercialProductProjectionVersion id/version/content hash, Collection assignment, CommercialPublication, PriceListVersion and BuyerCatalogVersion; it also verifies currency, wholesale price, RRP and MOQ from the frozen positive fixture. The buyer-catalog mutation now exercises the public ProductSku-exact price input `{ productSkuId, wholesalePriceMinor }` (using the same canonical amount so this slice tests identity without changing fixture economics). Brand read, shop read and Showroom buyer-catalog access must resolve the same immutable BuyerCatalogVersion.

Before/after counters require this slice to create no Selection, WholesaleOrder, SupplyCommitment, ActualCost or inventory movement. Automated evidence is split across `tests/product-commercialization-live-acceptance.test.mjs`, `tests/postgres/product-commercialization-live-acceptance.test.mjs` and `.github/workflows/product-commercialization-acceptance.yml`.

PR #118 closed the missing executable harness and its exact head passed all required repository workflows listed in section 2.6. The current P0.4 slice additionally guards ProductSku-exact buyer pricing at domain/HTTP/OpenAPI/PostgreSQL boundaries and extends same-environment PostgreSQL acceptance with negative bypass checks. The overall production contract remains PARTIAL because `COMM-LC-008`, the remaining `PRICE-009` depth, buyer Selection convergence and later P0 slices remain open. An intended live acceptance environment must still execute the accepted gate before this slice can be called `PROD-PROVEN`.

Supporting detail: `docs/architecture/product-readiness-commercial-projection-v2.md`, `docs/commercial-publication-linesheets.md`.

---

## 7. Wholesale commerce and order commitment

### 7.1 Browser flow already represented in the executable shell

The application defines the commercial stage sequence:

```text
campaign
→ collection
→ showroom
→ selection
→ order-builder
→ order
→ confirmation
→ deal-space
```

### 7.2 Campaign / Collection — IMPLEMENTED and live-accepted slice

`npm run acceptance:collection` exercises the public authenticated API and same-environment PostgreSQL through:

```text
Campaign draft
→ Campaign open
→ Collection draft
→ Collection published
```

The gate proves that this slice does **not** silently mutate downstream commercial publication, buyer catalog, selection/order, ProductSku inventory, warehouse movement, SupplyCommitment or ActualCost state.

### 7.3 Showroom / Linesheet — IMPLEMENTED/PARTIAL Product Identity depth

Showroom/linesheet is a buyer-facing commercial presentation of publication/catalog truth. It must not reconstruct mutable PLM truth. Variant-rich Product Identity-backed style detail remains an area to complete.

### 7.4 Selection / Color × Size matrix — PARTIAL

The canonical matrix mutation (`replaceSelectionMatrix`) already requires a pinned rich BuyerCatalogVersion and resolves every selected cell to exact ProductSku/StyleVersion/Colorway/SizeValue identity plus frozen buyer price/currency/MOQ context. Client-supplied price, currency and catalog version are forbidden.

A confirmed P0 compatibility seam remains in `createShowroomSelectionService`: when no canonical BuyerCatalog reader/buyer catalog is available, `createSelection` may still create a legacy selection with no BuyerCatalogVersion, and single-line `upsertSelectionLine` may then resolve price/currency/version from live `catalog_skus`. A pinned but historical non-rich BuyerCatalog can also fall back to live CatalogSku for ATS validation. This behavior is **DEPRECATED compatibility**, not canonical buyer truth, and is the immediate P0.5/PUB-005 convergence target. New canonical buyer selection must become fail-closed without an exact rich BuyerCatalogVersion and exact ProductSku cell; historical selections must remain readable without guessed ProductSku backfill.

Draft context must never silently change when mutable catalog/master records change. P0.5 acceptance must prove BuyerCatalogVersion → exact Color × Size Selection → submit → WholesaleOrder → bilateral confirmation → OrderCommitSnapshot without any live-catalog reconstruction.

### 7.5 WholesaleOrder — IMPLEMENTED
Order lifecycle and bilateral confirmation are executable. Order creation consumes frozen submitted Selection lineage. Currency is derived/validated against the frozen selection lines rather than accepted as an independent mutable client fact.

### 7.6 OrderCommitSnapshot — IMPLEMENTED

Immutable deal snapshot used by supply/economics. Physical/economic facts must refer to the committed order truth, not a subsequently edited buyer/cart view.

---

## 8. Supply, shipment, inventory and physical execution

### 8.1 SupplyCommitment — IMPLEMENTED

Supply allocations preserve committed order lineage. Canonical ProductSku supply lineage uses exact `orderLineNo + productSkuId + sku(display)` rather than textual SKU alone.

### 8.2 Shipment / receipt / supplier recovery — PARTIAL end-to-end depth

Shipment, receipt, claim resolution and supplier recovery capabilities exist. Canonical shipment/claim lines are expected to preserve exact committed order line/ProductSku identity. `POST /v2/receipt-claim-resolutions/:resolutionSnapshotId/supplier-recoveries` accepts either no line identity for aggregate recovery or the exact `orderLineNo + productSkuId` pair for SKU-specific recovery; optional `sku` is display/consistency only. The recovery service resolves the requested exact pair against immutable claim lines before creating the physical ActualCost entry and refuses textual-SKU-only inference. Legacy PostgreSQL fixtures whose committed order lineage predates ProductSku remain valid only for aggregate physical cost/recovery and must not synthesize a ProductSku from display SKU.

### 8.3 Inventory — IMPLEMENTED core

Canonical inventory model includes location, balance, reservation, allocation and movement concepts with centrally derived ATS and transaction/idempotency safeguards. Inventory state must reconcile rather than be independently edited by buyer/order UI.

---

## 9. Actual cost, landed cost and margin

### 9.1 Canonical cost identity

Order-level aggregate ActualCost is allowed without ProductSku. SKU-specific physical cost must resolve to exact physical lineage.

Canonical SKU-specific cost lineage is:

```text
orderId
+ orderCommitId
+ supplyCommitmentId
+ shipmentId
+ orderLineNo
+ productSkuId
+ sku (optional display/consistency)
+ physicalLineageVersion = 2
```

`shipmentLineId` remains shipment execution detail but is not a mandatory ActualCost identity component: one committed `orderLineNo + productSkuId` may be split across more than one shipment row inside the same shipment notice, and the cost scope deliberately resolves that exact immutable committed ProductSku line without inventing a second identity from textual SKU.

### 9.2 Canonical ActualCost write contract — IMPLEMENTED

PR #112 closes the P0 new-write bypass with one contract across HTTP, OpenAPI, application resolution and PostgreSQL:

- `POST /v2/orders/:orderId/actual-costs` is aggregate-only. New generic request bodies do not accept `sku`;
- `POST /v2/orders/:orderId/cost-close/adjustments` is also aggregate-only and cannot introduce SKU scope after close;
- `POST /v2/shipment-notices/:shipmentNoticeId/actual-costs` accepts either no line identity for an aggregate physical cost or the exact pair `orderLineNo + productSkuId` for a SKU-specific cost;
- `POST /v2/receipt-claim-resolutions/:resolutionSnapshotId/supplier-recoveries` follows the same identity rule: aggregate recovery omits line identity; SKU-specific recovery requires exact `orderLineNo + productSkuId` pinned to one immutable claim issue line;
- supplying only `sku`, only `orderLineNo`, or only `productSkuId` is invalid. Optional `sku`, when present with the exact pair, is only a display/consistency assertion and must match immutable shipment/claim lineage;
- physical corrections may omit line identity and inherit the immutable original physical line; if a caller supplies identity, the exact pair is mandatory and correction cannot move to another ProductSku line or between aggregate and SKU-specific scope;
- the composed authoritative `/v2` OpenAPI contract preserves version `1.17.0` without version drift while exposing `orderLineNo` and `productSkuId` on physical ActualCost and supplier-recovery inputs, removing `sku` from generic new-write/post-close inputs, and retaining it on historical correction input;
- migration `073_actual_cost_exact_physical_lineage.sql` installs the forward-only `actual_cost_000_canonical_write_guard_trigger` before the existing physical lineage gate. It blocks fresh generic textual-SKU writes and incomplete physical-v2 SKU identity without rewriting historical rows;
- generic correction of a historical legacy textual-SKU entry remains legal only as append-only reversal + replacement preserving the original SKU/order/commit/supply/brand/shop scope. It cannot introduce SKU scope onto an aggregate row, remove it from a legacy SKU row, move it to another SKU, or route a physical-v2 correction through the generic path;
- legacy physical execution fixtures that lack canonical ProductSku lineage remain aggregate-only. A later exact path must be created from real ProductSku-bearing committed lineage rather than a guessed backfill from textual SKU;
- permissions and organisation scope are unchanged: supported writes continue through the existing authenticated/capability-scoped route/application boundaries and database lineage checks.

Supported-path failures are fail-closed before persistence where possible (`HTTP_BODY_FIELD_UNKNOWN`, `HTTP_BODY_FIELD_INVALID`, `PHYSICAL_ACTUAL_COST_EXACT_IDENTITY_REQUIRED`, `SUPPLIER_RECOVERY_EXACT_PRODUCT_SKU_LINEAGE_REQUIRED`, `SUPPLIER_RECOVERY_PRODUCT_SKU_LINE_NOT_CLAIMED`, `SUPPLIER_RECOVERY_SKU_MISMATCH`); PostgreSQL independently protects bypasses with stable lineage errors including `ACTUAL_COST_LEGACY_SKU_NEW_WRITE_FORBIDDEN`, `ACTUAL_COST_EXACT_PRODUCT_SKU_IDENTITY_REQUIRED` and `ACTUAL_COST_LEGACY_CORRECTION_LINEAGE_MISMATCH`.

Automated evidence includes `tests/product-sku-physical-cost-lineage.test.mjs`, `tests/actual-cost-canonical-lineage-contract.test.mjs`, `tests/supplier-recovery-routes-openapi.test.mjs`, `tests/postgres/actual-cost-canonical-lineage-integrity.test.mjs`, `tests/zr-postgres-fulfillment.test.mjs` and `tests/zt-postgres-supplier-recovery.test.mjs`. The latter two deliberately preserve their legacy pre-ProductSku fixtures as aggregate-only evidence. Live ProductSku-bearing Shipment → ActualCost → Margin acceptance remains a separate production-evidence gate and is not implied by these tests.

### 9.3 Corrections — IMPLEMENTED

ActualCost corrections remain append-only reversal/replacement entries linked to the original entry/correction identity. New physical-v2 SKU-specific corrections preserve exact `orderLineNo + productSkuId`; historical non-physical textual-SKU corrections preserve their original legacy scope exactly. No correction edits an immutable historical entry in place.

### 9.4 Landed cost / allocation / margin / close — IMPLEMENTED

The order-level economics spine is immutable and version-pinned:

```text
ActualCostLedgerEntry
→ LandedCostSnapshot
→ CostAllocationRunSnapshot
→ MarginActualizationSnapshot
→ CostCloseReadinessSnapshot
→ CostCloseSnapshot
→ PostCloseAdjustment
→ adjusted LandedCostSnapshot
→ MarginActualizationSnapshot(allocationStatus=pending-post-close)
→ exact new CostAllocationRunSnapshot
→ PostCloseAllocationReconciliationSnapshot
→ reconciled MarginActualizationSnapshot(allocationStatus=current)
```

`LandedCostSnapshot` remains the order-commit-level frozen total derived from the exact active ActualCost entry IDs. `MarginActualizationSnapshot` and Cost Close remain aggregate order-commit economics; exact ProductSku allocation is provenance and detailed economics evidence, not a silent conversion of those aggregate snapshots into per-SKU documents.

PR #114 closes the confirmed ProductSku-lineage loss inside Cost Allocation:

- canonical allocation mode is `product-sku-v2`; every committed allocation target is the exact `orderLineNo + productSkuId`, while `sku` is display/consistency only;
- two committed lines with the same textual `sku` but different ProductSku IDs remain distinct allocation/economics rows and are never merged by display SKU;
- physical-v2 ActualCost with exact line identity is allocated directly only to the matching exact committed pair; a forged/missing pair fails closed rather than falling back to textual SKU;
- an old textual-SKU ActualCost without exact IDs on an otherwise canonical ProductSku order is treated as aggregate policy-distributed cost, never as a direct ProductSku match;
- aggregate ActualCost is distributed over exact ProductSku order-line targets using the approved immutable policy basis (`unit`, `net_value` or `custom`);
- canonical `custom` allocation uses `customLineWeightsByCostEntryId`, whose rows require `orderLineNo + productSkuId + weight`; optional `sku` is only a consistency assertion. The older `customWeightsByCostEntryId` textual-SKU map is legacy-only and is rejected for canonical ProductSku custom allocation;
- allocation rows and compatibility-named `skuEconomics` rows both carry `orderLineNo`, `productSkuId` and `sku`; exact identifiers are non-null in `product-sku-v2` mode and null in explicit `legacy` mode;
- pre-ProductSku OrderCommit snapshots keep their historical textual-SKU grouping in `legacy` mode and never receive inferred/backfilled ProductSku IDs;
- immutable allocation snapshots continue to persist the richer rows in existing `cost_allocation_run_snapshots.allocations`, `sku_economics` and full `payload` JSONB. No migration is required because the persistence envelope already stores the complete immutable JSON structure;
- Cost Allocation authorization remains `COST_MANAGE` for mutation and `MARGIN_READ` for reads, with existing command/idempotency and transactional-outbox behavior unchanged.

PR #115 binds the immutable allocation run into downstream aggregate economics without changing aggregate arithmetic:

- canonical ProductSku margin actualization requires an explicit immutable `costAllocationRunSnapshotId`; the application loads that run and the domain validates exact order, order version, OrderCommitSnapshot, LandedCostSnapshot, currency, active cost-entry set, allocation total and `lineageMode=product-sku-v2`;
- a canonical `MarginActualizationSnapshot` records `allocationStatus=current`, `costAllocationRunSnapshotId`, `costAllocationRunContentHash`, `costAllocationPolicyVersionId`, `costAllocationLineageMode=product-sku-v2` and `aggregateContentHash`; its new `contentHash` commits to the previous aggregate hash plus immutable allocation provenance;
- explicit pre-ProductSku legacy margin uses `allocationStatus=legacy-not-applicable` and null allocation pins. Legacy rows never receive inferred ProductSku identity;
- `CostCloseReadinessSnapshot` and `CostCloseSnapshot` must pin the same allocation id/hash/policy/mode as their canonical margin basis. Missing, pending or mismatched canonical allocation lineage fails closed;
- generic post-close ActualCost remains aggregate-only. Because a new exact allocation may require new approved custom weights, the late-cost path creates aggregate landed/margin facts with `allocationStatus=pending-post-close` rather than fabricating ProductSku economics; the adjustment preserves the allocation id/hash frozen by the original close;
- `pending-post-close` is not close-ready and never reuses the closed allocation as if it described the new landed-cost basis;
- existing immutable snapshot tables persist these provenance fields in their full JSONB payloads; the economics store reads immutable allocation runs by exact id for fail-closed validation;
- outbox margin/readiness/close/post-close events expose allocation status and pin fields so projections can distinguish `current`, `legacy-not-applicable` and `pending-post-close` states.

PR #116 completes the explicit exact post-close reconciliation contract:

- entry point: `POST /v2/orders/{orderId}/cost-close/adjustments/{postCloseAdjustmentId}/allocation-reconcile` with an exact `costAllocationRunSnapshotId` and normal command/idempotency identity;
- the caller must first create a new immutable CostAllocationRunSnapshot for the exact adjusted LandedCostSnapshot. Reconciliation never searches for or auto-selects a “latest” allocation run;
- only canonical `product-sku-v2` commits require this ProductSku reconciliation; legacy economics remains explicit and is not upgraded by guessed ProductSku IDs;
- the requested PostCloseAdjustment must be the latest adjustment for that CostClose and must not already have a reconciliation. An older reconciled adjustment remains immutable history but cannot override a later adjustment in the effective economics position;
- the exact CostClose, latest adjustment, adjusted LandedCostSnapshot, pending MarginActualizationSnapshot and supplied CostAllocationRunSnapshot must agree on order, order version, OrderCommitSnapshot, currency, active cost-entry set, allocation total and landed basis;
- success creates one immutable `PostCloseAllocationReconciliationSnapshot` plus one new `MarginActualizationSnapshot(allocationStatus=current)` in the same economics transaction;
- reconciliation is provenance-only for aggregate economics: `aggregateContentHash`, net revenue, landed cost, contribution margin amount and contribution margin percent must exactly equal the pending margin. A reconciliation that changes aggregate arithmetic fails closed;
- `PostCloseAllocationReconciliationSnapshot` freezes close id, adjustment id, pending margin id, adjusted landed id, exact allocation run id/hash/policy/mode, new current margin id, previous/resulting allocation status, timestamp and content hash;
- a later PostCloseAdjustment makes its own pending margin the effective economics basis until that newer adjustment receives its own exact allocation and reconciliation; earlier reconciliations are not rewritten or deleted;
- `GET /v2/orders/{orderId}/economics-position` exposes `postCloseAllocationReconciliationSnapshotId`, `allocationStatus`, exact allocation id/hash/policy/mode and chooses provenance only from the latest effective close/adjustment/reconciliation chain. A pending latest adjustment returns null current allocation pins rather than falling back to the original close;
- successful reconciliation emits `margin.actualized` for the new current margin and `cost-close.allocation-reconciled` for the immutable reconciliation fact;
- authorization is `COST_MANAGE` for reconciliation and existing `MARGIN_READ` for the economics-position read model.

The composed `/v2` OpenAPI version remains `1.17.0`. `CostAllocationRunInput` exposes exact `customLineWeightsByCostEntryId`; `CostAllocationRow`, `SkuEconomics` and `CostAllocationRunSnapshot` expose exact lineage and `lineageMode`. Margin actualization exposes `costAllocationRunSnapshotId`; PR #116 additionally exposes the post-close reconciliation route/schemas and effective economics-position allocation/reconciliation fields without creating a parallel API version.

Automated evidence includes `tests/cost-allocation-product-sku-lineage.test.mjs`, `tests/econ003-allocation-margin-close-lineage.test.mjs`, `tests/econ003-post-close-allocation-reconciliation.test.mjs`, `tests/econ003-post-close-allocation-reconciliation-migration.test.mjs` and `tests/order-economics-position.test.mjs` plus the default/PostgreSQL verification suites. PR #116 implementation head passed repository Verify run `33345573039` and Syntha V2 CI run `33345572980`, including PostgreSQL verification. The known `ECON-003` code/runtime lineage gap is therefore CLOSED at the implementation-contract level; this documentation-sync head must still pass the same required gates before merge. Live ProductSku-bearing economics acceptance remains separately governed by `ACC-004` and is required for `PROD-PROVEN` status.

Supporting detail: `docs/architecture/order-supply-cost-margin-baseline.md`, `docs/architecture/order-margin-bridge.md`, `docs/architecture/econ003-allocation-margin-close-lineage.md`.

---

## 10. UI/UX master contract — Omnidata Design System v1

### 10.1 Source and rule

ODS v1 is the only current visual/interaction system. New page-local visual dialects and new `omnidata-vN` layers are forbidden. Compatibility layers may remain only while their semantic coverage is migrated and validated.

Canonical assets:

- `public/omnidata-v14-role-system.css` — exact tokens and role styling;
- `public/modules/omnidata-v14-role-system.js` — semantic role/part mapping and runtime audit;
- `public/modules/omnidata-v14-components.js` — component classification;
- `public/modules/omnidata-v14-module-adapters.js` + CSS — compatibility migration primitives;
- `docs/omnidata-design-system-v1.md` — supporting visual-system rationale and migration detail.

### 10.2 Exact typography

| Token | Value |
|---|---|
| Family | `Arial, "Helvetica Neue", Helvetica, sans-serif` |
| XS | `11px` |
| SM | `12px` |
| MD / body | `13px` |
| LG | `16px` |
| XL / H1 | `22px` |
| Base line-height | `1.42` |
| H1 weight/line-height | `600 / 1.18` |
| H2 weight/line-height | `600 / 1.25` |
| H3 | `13px`, weight `700` |
| H4 | `12px`, weight `700` |

### 10.3 Exact colour tokens

| Meaning | Value |
|---|---|
| workspace | `#f2f4f5` |
| sidebar | `#303640` |
| sidebar deep | `#282e37` |
| surface | `#ffffff` |
| soft surface | `#f7f8f9` |
| hover surface | `#fff8f4` |
| primary text | `#222831` |
| soft text | `#56606a` |
| muted | `#69727d` |
| border | `#d9dee3` |
| strong border | `#c7cdd3` |
| primary accent | `#ff5b22` |
| accent hover | `#e84b15` |
| success | `#477052` |
| warning | `#8c661f` |
| danger | `#a94349` |
| info | `#466b8d` |

Status backgrounds/borders are semantic, not module-specific: success `#f0f7f2/#c9dbce`, warning `#fff8e8/#ead8ab`, danger `#fff2f2/#e5c3c6`, info `#f0f6fb/#c8d7e4`.

### 10.4 Spacing and geometry

| Token/primitive | Value |
|---|---|
| spacing scale | `4 / 8 / 12 / 16 / 24 / 32 px` |
| control height | `34px` |
| standard table row | `42px` |
| radii | `3 / 5 / 8 px` |
| max content width | `1760px` |
| inspector width | `360px` |
| expanded sidebar | `232px` |
| compact desktop/tablet sidebar | `208px` |
| collapsed/icon rail | `68px` |
| topbar minimum height | `50px` |
| brand block minimum height | `56px` |
| table minimum width | `720px` |
| table header height | `36px` |
| status chip min height | `22px` |
| textarea min height | `88px` |

### 10.5 Seven permitted visual roles

Exactly seven top-level ODS roles are canonical:

```text
table
filterbar
card
status
inspector
button
field
```

Structural meaning uses `data-ods-part` (page header, toolbar, tabs, pagination, surface, metric, master-detail, form, list, definition grid, timeline, progress, alert, toast, etc.). A module owns business semantics; ODS owns visual language.

### 10.6 Field behavior

- text/select/textarea ODS fields fill available width;
- control min-height `34px`;
- horizontal padding `10px`;
- border strong token, radius `3px`;
- field text `12px` weight 500;
- focus border uses accent and `0 0 0 2px rgba(255,91,34,.1)` ring;
- checkbox/radio canonical geometry `15×15px`, accent colour orange;
- textarea resizes vertically and starts at least `88px` high;
- invalid client state may guide the user but backend remains authoritative.

### 10.7 Table behavior

- wrapper owns horizontal overflow;
- table header is sticky, uppercase, 10px/700, strong bottom border;
- normal rows are 42px and 12px text;
- hover uses `#fff8f4`;
- selected row uses `#fff0ea`;
- numeric content should use tabular numerals;
- last row removes redundant bottom border;
- inspector/detail must not overlap the registry/table.

### 10.8 Status/alert behavior
- status uses rounded pill (`999px`) and semantic tone;
- success/warning/danger/info meaning must not change by locale;
- alert/toast expands to full width of its containing semantic surface and supports multiline content;
- destructive/error meaning uses danger semantics; never encode critical state only by colour without textual state.

### 10.9 Inspector and responsive behavior

Desktop inspector is sticky at `top:62px`, max-height `calc(100vh - 78px)` and scrolls internally. At `<=920px`, master-detail becomes one column and inspector becomes static. Content padding decreases at `<=1080px`; page header/filterbar/metrics reflow at smaller breakpoints including `620px`.

### 10.10 Role-system runtime invariants

`normalize()` in `public/modules/omnidata-v14-role-system.js` runs under its own
MutationObserver, so it must never leave the DOM dirty in a way that observer
watches. Two invariants hold it:

1. **A pass may not re-trigger itself.** Every observer-driven pass detaches its
   own observer for the duration of the pass (`takeRecords()` then
   `disconnect()`, re-observing in a `finally`), and writes an attribute or a
   text node only when the value actually changes. A redundant `classList.add`
   still queues a mutation record, and an unconditional `textContent =`
   still replaces the text node; without both guards the pass re-schedules
   itself through `queueMicrotask` and the microtask chain starves rendering —
   the page never paints a frame.

   This holds for all three observer-driven layers, each of which had the fault
   independently:

   | Layer | Observer watches | What re-dirtied it |
   |---|---|---|
   | `omnidata-v14-role-system.js` | `class` and five other attributes | `classList.add` of classes already present |
   | `omnidata-v14-components.js` | `class`, `aria-selected`, `aria-pressed` | `classList.add('omnidata-v14')` already present |
   | `omnidata-v14.js` | added nodes | `translateBrand()` / `translateRole()` assigning `textContent` unconditionally |
2. **A button role is never assigned to a descendant of a button.** Buttons
   cannot nest. The heuristic class matcher would otherwise promote internals
   such as `.button-label` on the `button-` prefix, drawing a second control
   inside its parent; the audit likewise ignores button internals so they do not
   count as unclassified and drive the retry timer.

Navigation items carry the `button` role for semantics and audit, but are laid
out by the `navigation-item` part, not by the generic button chrome: left
aligned, no border, inherited typography, centred only when the sidebar is
collapsed or at the narrow breakpoints.

The same separation applies wherever an element carries a role for semantics but
is not a standalone control of that role. Each of these is styled by role and
part in the contract stylesheet — never by a module namespace, which
`validate-design-system` rejects:

| Element | Role it carries | Why the generic chrome is wrong |
|---|---|---|
| Search wrapper | `field` / `field-group` | It is one horizontal control (icon + input in one border), not a label stacked above an input |
| Command-bar spacer | `filterbar` | `aria-hidden` layout filler (`flex:1`); it must never paint |
| Status dot | `status` | A 9px dot whose border colour encodes state, not a pill |
| Toast host | `status` / `toast` | A permanently mounted positioning container; the `.notice` inside carries the card |
| Dashboard mini-table | `table` | Two or three summary columns in a half-width panel; the 720px grid floor only forces a scrollbar |
| Status chip label and count | `status` | They sit inside a status card, so the pill was drawn again nested inside the chip they belong to; they are text |
| Section count | `card` | A soft count pill beside a section title, not a card nested in a card |
| Help button | `button` | A topbar control on the same baseline as the bell, not the full-height topbar tab the v7 layer sized it to |

Neither classifier may give a box role to an inline element. A card, an inspector, a
table or a filter bar is a box with a border and a ground; a `<span>`, `<label>` or
`<small>` is text. Because the class matcher works by substring, `.ls9-metric-label`
was read as another metric and drawn as a second bordered tile inside the tile it
labels, the same height as its parent and hanging 13px below it. A badge and a
control legitimately are inline, so `status` and `button` are excluded from the rule.

A table cell keeps table layout: `display:grid` on a `<td>` voids its `colspan`, which
collapsed a seven-column empty-state message into the first column.

There is one more writer of the page name than the resolvers above: the v8 layer
rewrites `.breadcrumb strong` after every render from its own context table, and for a
view that table does not list it writes the dashboard's name. That is why five sections
showed `Рабочий стол` as the page you were on even after the resolvers were corrected.
It asks the navigation first now. A view that a bridge activates rather than the
navigation declaring it must still name itself in the navigation data, or the path
resolves one render late.

Both classifiers enforce this. `omnidata-v14-components.js` refuses a container
role to a descendant that already has it, and sweeps the tree once more after the
last pass because assignment order across passes is not guaranteed; the role
system does the same for `button`, `filterbar` and `inspector`. Without both, one
classifier re-creates what the other refused: an inspector panel was drawn seven
deep, each `.od-inspector-*` child painted as another panel with its own border,
white ground, 12px padding and scrollbar.

The breadcrumb and the page header take a view's name from the rendered sidebar
(`SynthaOmnidataV7.viewMeta`), which is the one place that knows every reachable
view and its group, whichever module contributed it. Before that, four PLM modules
each replaced `viewSectionName` with a string of their own and the linesheets layer
wrote a fifth straight into the breadcrumb, so one group was named several ways in
two languages, and five sections showed `Рабочий стол` as their path. A view that
opens through a bridge rather than a nav entry must still name itself on the button
(`dataset.view`), or it falls back the same way. A breadcrumb step that repeats the
next one is dropped.

A nested role is not automatically wrong — a card list holds cards, a table wrap
holds a table, a toast host holds a notice. What is wrong is an element that
carries a role because of where it sits and then paints that role's chrome a
second time inside its own parent. Check a suspect by comparing an element's
role with the nearest ancestor carrying the same role, and by whether it paints
a border or background of its own.

The v12 rail tooltip (`.nav-item[data-v12-label]::after`) belongs to the
icon-only rail and is shown only while the sidebar is collapsed; with labels
visible it repeats the label beside it and covers the item above.


### 10.11 Shell and navigation

Current main shell top-level navigation is generated from a single `NAV_GROUPS` structure:

| Group | View | Icon name |
|---|---|---|
| — | Overview | `overview` |
| DEVELOPMENT | Catalog | `catalog` |
| WHOLESALE COMMERCE | Showrooms | `showrooms` |
| WHOLESALE COMMERCE | Partners | `partners` |
| WHOLESALE COMMERCE | Selections | `selections` |
| WHOLESALE COMMERCE | Orders | `orders` |
| MANAGEMENT | Calendar | `calendar` |
| MANAGEMENT | Notifications | `notifications` |

Sidebar footer actions and icons:

- Refresh — `refresh`;
- Logout — `logout`, danger semantic;
- Collapse/Expand — `collapse` / `expand`;
- RU/EN locale switcher.

Topbar contract:

- breadcrumb with `back` icon and `SYNTHA / section / view` hierarchy;
- current-section search with `search` icon and `⌘K` hint;
- notifications button with `bell` icon and unread count;
- active organisation with `building` icon;
- user avatar initials + display name/email + role.

The sidebar collapsed preference is stored in `localStorage` under `syntha-v2-sidebar-collapsed` after the one-time readable-shell migration.

### 10.12 Login/startup states

Login screen contains locale switcher, brand block, description, email, password and Sign In action. Password field has a minimum client length of 12; server policy remains authoritative. Startup hydration failure shows an explicit error plus Retry and Sign out; it does not silently render a partial workspace.

### 10.13 Required state coverage for every interactive screen

Every new/changed data screen must specify and implement as applicable:

```text
initial/loading
empty
populated
filtered/no-results
selected row/card
validation error
permission denied/disabled action
server/domain error
network/transient error
mutation busy/idempotent replay
success confirmation/toast
stale/version-conflict handling
responsive narrow layout
```

A button without an implemented end-to-end handler is forbidden.

---

## 11. Browser loading and localization contract

Visual layers that observe the DOM must not react to their own writes, and must not schedule that work as a microtask. Four layers (`omnidata-v13`, `omnidata-v14`, `omnidata-v14-components`, `omnidata-v14-role-system`) run a `MutationObserver` whose callback assigns the same attributes the observer watches — `class`, `role`, `aria-*`, `data-od14-*`. Their writes are unconditional, and writing an attribute with an unchanged value still queues a mutation record, so each pass re-arms the observer. Because observer callbacks are delivered as microtasks and the layers scheduled their work with `queueMicrotask`, the re-arm happened inside the same microtask checkpoint: the event loop never regained control and the workspace never became interactive.

Each layer therefore applies with its observer detached, discards the records its own pass produced (`takeRecords`) before reconnecting, and schedules through `requestAnimationFrame`, falling back to `setTimeout` and only then to a microtask where neither exists. The observed option sets are unchanged, including `characterData` in the role system, because language enforcement depends on text changes.

Static workspace assets are served with negotiated content encoding. Text responses above 1 KiB are compressed with Brotli, or gzip when Brotli is not acceptable, chosen from `Accept-Encoding` including its quality values; binary types and small responses are always served as identity. Every static response carries `Vary: Accept-Encoding`, and a compressed representation carries its own ETag, so a cache holding one representation can never answer a request that only accepts the other. A compression that fails to shrink the asset is discarded and the identity bytes are served instead, without an encoding header. Encoded bodies are memoised under the content hash that already backs the ETag, so a changed file misses the memo by construction.

The shell currently loads 96 blocking assets. Measured on the supported runtime: 1165 KiB as identity, 288 KiB with gzip, 272 KiB with Brotli. Because the visual layer is deliberately `no-store`, that transfer repeats on every navigation, which is what makes the encoding contract load-bearing rather than cosmetic.


### 11.1 Loading order

The standalone shell loads localization first, then shared DOM/API/pagination/capability/validation foundations, then business workspace modules, then final ODS role/component runtimes, with `app-start.js` last. Changing this dependency order is an architecture change and must be documented here.

### 11.2 Languages

Russian and English are mandatory. User-facing strings route through the shared localization runtime. Business data is not translated by the visual normalizer. Established abbreviations such as PLM, BOM, SKU, POM, MOQ, ATS, RFQ, PO, ERP, WMS, PIM, OMS, QC, QMS and API may remain unchanged.

---

## 12. API and mutation contract

A `DomainError` that describes a fault on the platform side — a reader that broke its contract, a failed clock or RNG — is answered with HTTP 500, not a 4xx that blames the caller. `src/http/error-status.mjs` classifies these by suffix (`_RESULT_INVALID`, `_READER_REQUIRED`, `_READER_UNAVAILABLE`, `_CLOCK_INVALID`) plus `AUTH_RANDOM_SOURCE_INVALID`, so a newly introduced reader/result/clock code is classified on arrival. Unclassified domain validation errors deliberately remain 422; that default is contractual and covered by test.

Transport adapters carry no request policy of their own. `src/http/pipeline.mjs` owns routing, Bearer authentication, the `Idempotency-Key` requirement, body decoding/limits and error mapping once; `src/http/api.mjs` (node:http) and `src/http/fetch-api.mjs` (Web Fetch) only translate a transport request into the shared request view `{ method, url, header(name), readBody(limit) }` and render the returned result. A new request rule is therefore added in one place and cannot be present on one transport and missing on the other. `readBody` must enforce the limit while the body is consumed: a request without `Content-Length` is otherwise bounded only after it has been buffered in full. HTTP status mapping lives in `src/http/error-status.mjs`, re-exported from `src/http/api.mjs` for existing callers.

### 12.1 General rules

- API namespace: `/v2`.
- The composed authoritative `/v2` OpenAPI `info.version` remains `1.17.0`; module/schema corrections inside the current v2 namespace must not independently create version drift.
- Auth: bearer access token for protected routes.
- Business mutation retries reuse the original idempotency key.
- Transport failures may be retried according to client policy; domain HTTP errors must not be blindly retried with a new command.
- Request body validation is strict where route contracts define allowed fields.
- Domain validation is authoritative even if the UI prevents invalid input.
- OpenAPI must change in the same PR as a public request/response change.

### 12.2 Error design

Domain failures use stable semantic error codes. New fail-closed invariants should surface a domain-level error before a raw database-trigger error whenever the supported application path can detect the condition. The database still protects direct/internal bypasses.

### 12.3 Cost allocation and margin lineage API — IMPLEMENTED

`POST /v2/orders/:orderId/cost-allocation-runs` accepts an exact `landedCostSnapshotId`, approved `policyVersionId`, optional legacy `customWeightsByCostEntryId`, and optional canonical `customLineWeightsByCostEntryId`.

- `customLineWeightsByCostEntryId[costEntryId]` is an array of `{ orderLineNo, productSkuId, weight, sku? }`; duplicate exact pairs, malformed identifiers, negative/non-finite weights and unknown row fields are rejected at HTTP boundary;
- for canonical ProductSku commits the domain accepts exact custom line weights only; textual-SKU custom maps are legacy-only;
- response `CostAllocationRunSnapshot.lineageMode` is `product-sku-v2` or `legacy`;
- every `CostAllocationRow` and compatibility-named `SkuEconomics` response row includes `orderLineNo`, `productSkuId` and `sku`; exact identifiers are null only for explicit legacy commits.

`POST /v2/orders/:orderId/margin/actualize` accepts required `landedCostSnapshotId` and optional-at-transport `costAllocationRunSnapshotId`. For a canonical ProductSku OrderCommit the latter is domain-required and must identify the immutable allocation run matching the exact landed basis. For explicit legacy OrderCommit lineage the field is omitted and no ProductSku allocation reference is synthesized.

`POST /v2/orders/{orderId}/cost-close/adjustments/{postCloseAdjustmentId}/allocation-reconcile` accepts exactly one business field, required `costAllocationRunSnapshotId`, plus the normal `Idempotency-Key` header. It is the only supported mutation that promotes a canonical post-close `pending-post-close` margin back to `current` detailed ProductSku allocation provenance.

The reconciliation route requires `COST_MANAGE`, the exact latest PostCloseAdjustment and an exact immutable allocation run for that adjustment's adjusted LandedCostSnapshot. It returns `{ reconciliation, marginActualization }`; duplicate reconciliation, non-latest adjustment, mismatched landed/allocation basis or changed aggregate economics fail closed. No route auto-selects an allocation run by timestamp.

`GET /v2/orders/{orderId}/economics-position` exposes the effective `postCloseAllocationReconciliationSnapshotId`, `allocationStatus`, allocation run id/hash/policy/mode and effective landed/margin IDs. If the latest adjustment has no reconciliation, allocation status remains `pending-post-close` and current allocation pins are null; an older reconciliation cannot override a newer adjustment.

Public version remains `/v2` / OpenAPI `1.17.0`; these are invariant corrections and completion of the existing economics contract, not a new parallel API version.

### 12.4 Canonical buyer pricing/catalog publication API — IMPLEMENTED/PARTIAL

`POST /v2/commercial-publications/{publicationId}/buyer-catalogs` is the only current public mutation that creates a buyer-specific PriceListVersion and BuyerCatalogVersion pair. Required body fields are `showroomId` and `shopId`; optional `priceOverrides` is an array of exact `{ productSkuId, wholesalePriceMinor }` rows. Unknown fields, textual `sku`, client `unitPrice`, invalid identifiers, non-integer/negative/overflowing minor amounts and over-sized override arrays are HTTP 400. Unknown/duplicate ProductSku or mismatched immutable commercial lineage fails as domain validation (HTTP 422). Wrong role/organisation remains 403; reusing an idempotency key with a different fingerprint remains 409 `COMMAND_ID_CONFLICT`.

The application revalidates the current brand capability and accepted/unexpired Showroom access before command replay or new execution. One successful command transaction inserts exactly one immutable PriceListVersion, one immutable BuyerCatalogVersion and the corresponding outbox events; exact replay returns the stored result without duplicate commercial facts. Neither request nor domain accepts an independently authoritative currency, major-unit price, current PLM value or textual-SKU join.

The authoritative composed OpenAPI remains `/v2` version `1.17.0`; this is convergence of the existing endpoint, not a parallel API version.

---

## 13. PostgreSQL and migration contract

- Every schema change is a new numbered migration.
- Applied migrations are never edited.
- Migration checksum is stored in `schema_migrations`.
- Advisory locking serializes concurrent migrators.
- Referential/lineage invariants that must survive direct SQL/service bypass should be protected at the database boundary where practical.
- Historical immutable rows are not rewritten just to conform to a newer lineage version; use versioned semantics and forward-only enforcement.
- Append-only ledgers require corrections/reversals/adjustments rather than mutable overwrite.
- ActualCost migration 073 is forward-only: generic fresh writes are aggregate-only; new SKU-specific physical writes require exact `order_line_no + product_sku_id`; historical non-physical textual-SKU rows remain immutable and may only be corrected by preserving their original lineage through reversal + replacement.
- Legacy rows/tests that lack ProductSku remain legal only in aggregate cost scope. Neither migration 073 nor application compatibility code may infer/backfill ProductSku from textual SKU.
- Cost Allocation exact-lineage output requires no migration: `cost_allocation_run_snapshots` already persists `allocations`, `sku_economics` and the complete immutable snapshot `payload` as JSONB, so `orderLineNo`, `productSkuId` and `lineageMode` are retained without introducing redundant relational columns or rewriting prior snapshots.
- PR #115 allocation→margin→readiness→close provenance required no migration because existing immutable economics tables already persisted complete snapshot payload JSONB while scalar accounting columns remained unchanged. The economics store reads an immutable `cost_allocation_run_snapshots.payload` by id for fail-closed validation; historical snapshots are not rewritten.
- Migration `074_post_close_allocation_reconciliation.sql` creates immutable `post_close_allocation_reconciliation_snapshots`. One row is allowed per `post_close_adjustment_id`; one reconciled margin is pinned per reconciliation. Exact foreign keys and the integrity trigger bind order/commit/close/adjustment/pending margin/adjusted landed/allocation policy/allocation run/new current margin; the trigger also validates pending→current allocation status, scalar↔payload identity, exact orderVersion, timestamps/content hash and unchanged aggregate economics. UPDATE/DELETE is rejected by the common economics immutability trigger.
- Connection timeout policy is split by workload. The application pool binds request-path traffic with `statement_timeout`, `lock_timeout` and `idle_in_transaction_session_timeout` (`SYNTHA_DB_STATEMENT_TIMEOUT_MS`, `SYNTHA_DB_LOCK_TIMEOUT_MS`, `SYNTHA_DB_IDLE_IN_TRANSACTION_TIMEOUT_MS`; `0` leaves the server default). Two workloads legitimately exceed those bounds and opt out explicitly rather than inheriting them: startup migrations issue session-level `SET ... = 0` before touching the schema and release their connection with destroy so the relaxed session never returns to the pool, and retention maintenance issues transaction-scoped `SET LOCAL` (`SYNTHA_DB_MAINTENANCE_STATEMENT_TIMEOUT_MS`, default `0`) because its sweeps are unbounded bulk deletes. Timeout values are validated integers; they are never interpolated from unvalidated input.
- Migration `075_canonical_buyer_catalog_new_writes.sql` is forward-only PUB-005 enforcement. BEFORE INSERT guards require every fresh PriceListVersion/BuyerCatalogVersion to originate from a projection-backed V2 CommercialPublication, preserve exact projection/readiness/StyleVersion/ProductSku line and rich hierarchy, keep non-price commercial terms frozen, keep server-derived major/minor price consistent, and match relational scalar columns to immutable payload identity. BuyerCatalog additionally validates exact PriceList/publication/brand/shop/currency/Showroom/accepted-unexpired access context. Historical V1 rows are neither updated nor deleted; the guard never joins `catalog_skus` or current Product Master tables to invent ProductSku identity.

---

## 14. Events, workers and side effects

`outbox_events` carries more than one event envelope shape. Domain events written through `domainEvent()` expose `id` and `type` inside the JSON; MDM reference and Product Identity events expose `eventId` and `eventType` instead. The notification projection key is therefore the outbox row primary key, never a field read out of the envelope: the row id is `NOT NULL`, and it is already what the reader's anti-join against `notification_projections` and the claim table use. Records handed to the projection service carry `eventId` and `eventType` taken from the row, alongside the untouched envelope.

Outbox publication follows the same rule and for the same reason: the claim query carries `outbox_events.id` and `event_type` onto the record, and the publisher identifies, acknowledges, reschedules and dead-letters by that row key. The HTTP publisher separately enforces its own wire contract — a delivered payload must carry `id` and `type` — so an event whose envelope cannot satisfy it becomes a bounded delivery failure that retries and then dead-letters under its own id. It must never become an exception thrown before the per-record try/catch, because `processInAggregateOrder` does not guard individual records and one such throw fails the entire batch, blocking delivery of every well-formed event behind it.

Only `selection.submitted`, `order.terms-accepted` and `deal-space.opened` produce notifications. Every other event type is projected as a no-op that marks the row processed, so reference-data events neither create user-visible noise nor remain pending forever.

Retention maintenance and startup migrations run outside the request-path PostgreSQL timeout policy defined in section 13; every other worker inherits it.

Business mutations that publish integration effects use the transactional outbox pattern. Verification/runtime smoke disables external webhook side effects. A live acceptance scenario must identify what downstream state is allowed to change and explicitly prove important state that must remain unchanged.

---

## 15. Verification and production-evidence ladder

### 15.1 Default gate

`npm run verify` includes architecture boundaries, PostgreSQL static contract, isolation, UI, ODS, i18n, MDM, governed KPI methodology, the type contract and application tests.

The type contract (`npm run validate:types`, `scripts/validate-types.mjs`) runs TypeScript over the JavaScript sources with `checkJs` and compares the result against the per-file baseline in `ops/type-baseline.json`. It fails when a file gains type errors, and equally when a file loses them without the baseline being re-recorded, so the recorded debt can only ratchet down. `npm run validate:types -- --update` re-records it. The baseline exists because the codebase carries pre-existing findings that cannot be resolved in one change; it is a ratchet, not an accepted permanent state. TypeScript and `@types/node` are pinned exactly, since the baseline is only meaningful against a fixed compiler.

### 15.2 PostgreSQL release-candidate gate

`npm run verify:postgres` adds PostgreSQL integration coverage and the real runtime process smoke.

### 15.3 Live acceptance

All three acceptance commands are executable and are exercised by CI against the live runtime stack. Two of them previously could not run at all: `scripts/acceptance-collection.mjs` and `scripts/bootstrap-production-reference.mjs` imported `pg` with a named binding, which throws at load time because `pg` is CommonJS and publishes no `exports` map. The test suite could not detect this, because tests resolve `pg` through `tests/pg-test-facade.mjs`, which re-exports the named bindings; operational entry points get no such facade. `tests/script-runtime-contract.test.mjs` now asserts the import contract directly.

Creating a ProductSku legitimately initialises exactly one zero-quantity row in `product_sku_inventory_balances`. Both Product Readiness scenarios therefore permit that single identity delta and nothing else: every other counter, available and reserved quantities included, must be unchanged, which is what proves the new row carries no stock. The rule is defined once in `assertReadyProductInventoryIsolationDelta` and shared by both scenarios, because the BLOCKED scenario previously asserted strict equality and could never pass live.

`SYNTHA_ACCEPTANCE_RUN_ID` pins idempotency keys, so two commands sharing one run id against one environment replay the same mutations instead of creating new state. The commercialization gate internally executes the READY readiness scenario, so a readiness command reusing its run id is a replay and legitimately creates no new ProductSku identity row. Acceptance steps that must prove fresh creation therefore require their own run id; CI gives each acceptance step a distinct one. This is a property of idempotent replay, not a reason to relax the single-identity-delta assertion.

The commercialization scenario currently assumes a first run against its reserved organisations: replaying it against an environment that already holds an active acceptance brand↔shop relationship fails with `RELATIONSHIP_NOT_RENEWABLE`, which is the domain behaving correctly. Repeat-run support against a persistent environment is open work.


Operational `PROD-PROVEN` live acceptance coverage remains deliberately narrow at the currently evidenced baseline:

```text
Campaign → Collection
```

It uses public authenticated HTTP plus same-environment PostgreSQL verification. It is non-destructive outside its reserved acceptance namespace and compares downstream invariant counters.

PR #117 additionally implements two executable Product Readiness scenarios:

```text
Scenario A:
Product Identity → ProductReadinessSnapshot(blocked: category, measurements)
→ rejected CommercialProductProjectionVersion attempt

Scenario B:
Product Identity + governed APPAREL/size MDM
→ canonical Measurement Chart PUBLISHED
→ ProductReadinessSnapshot(READY, blockers=0)
```

`npm run acceptance:product-readiness` first migrates and idempotently bootstraps the source-controlled operational MDM profile through the canonical MDM bootstrap, then executes both scenarios through supported authenticated `/v2` mutations. Scenario A intentionally omits category/measurement repository truth and passes only with exactly `category` + `measurements` blockers plus exact HTTP `422 / COMMERCIAL_PROJECTION_READINESS_BLOCKED` and zero exact projection rows before/after. Scenario B uses exact governed `APPAREL`, `INT_ALPHA`, `INT_M`, `CM` and `CHEST_CIRC` references, creates and publishes a canonical Measurement Chart, and passes only with a READY snapshot and zero blockers plus exact same-environment PostgreSQL lineage. Scenario A retains strict downstream commercial/warehouse/economic isolation. Scenario B additionally proves the intentional ProductSku registration effect from migration 065: exactly one same-brand zero balance identity is materialized while quantities, movement ledger and downstream commercial/economic facts remain unchanged. Repository tests validate both harnesses and failure semantics; until this command completes against the intended live acceptance environment, both #117 acceptance slices remain `IMPLEMENTED` rather than `PROD-PROVEN`.

PR #118 implemented `npm run acceptance:product-commercialization` and is merged. It creates a fresh positive READY graph, continues it through exact Collection assortment/Showroom/buyer access, CommercialProductProjectionVersion, projection-backed CommercialPublication, ProductSku-exact PriceListVersion and BuyerCatalogVersion, and checks the immutable chain with both brand and shop actors against the same PostgreSQL target. Its exact head passed the dedicated supported-runtime/PostgreSQL workflow and the required CI listed in section 2.6. The current P0.4 convergence extends that harness with exact ProductSku minor-price input plus PostgreSQL fail-closed new-write checks. Repository CI success remains `IMPLEMENTED` evidence, not an intended-environment `PROD-PROVEN` claim unless that environment is explicitly the accepted production-evidence target.

### 15.4 Required acceptance expansion order

Expand one business slice at a time, preserving explicit before/after invariants:

1. Campaign → Collection — IMPLEMENTED / PROD-PROVEN.
2. Product Identity → Readiness — negative fail-closed **and** positive canonical READY harnesses IMPLEMENTED in #117; exact MDM/category/size/Measurement Chart PostgreSQL assertions are executable; actual intended live-environment execution is PENDING, so no `PROD-PROVEN` claim.
3. Readiness → Projection → Publication → PriceList → BuyerCatalog — public-runtime harness IMPLEMENTED and merged in #118; its exact head passed Verify/MDM/Syntha V2 CI/Product Commercialization Acceptance. Current P0.4 additionally converges ProductSku-exact pricing/new-write guards. Intended-environment `PROD-PROVEN` evidence is still pending and the semantic contract remains PARTIAL until `COMM-LC-008`, remaining `PRICE-009` depth and `PUB-005` downstream compatibility seams are closed.
4. BuyerCatalog → Selection → OrderCommit — PLANNED.
5. OrderCommit → Supply → Shipment — PLANNED.
6. Shipment → SKU-specific ActualCost → exact ProductSku allocation → MarginActualization → Cost Close → post-close adjustment/reconciliation — PLANNED live proof. Exact ActualCost is implemented in #112, exact allocation in #114, pre-close allocation→margin/close provenance in #115, and explicit exact post-close reconciliation in #116. PR #116 implementation head passed required repository Verify and Syntha V2 CI including PostgreSQL verification. Live public-API business proof remains under `ACC-004`.
7. Full connected Product → Margin golden path — final P0 proof.

A direct service/domain test is not a substitute for a live public API acceptance gate. Aggregate legacy PostgreSQL flows may verify compatibility but do not count as proof of the ProductSku-specific acceptance slice. Likewise, a unit-tested acceptance harness is not `PROD-PROVEN` until the harness itself completes against the intended runtime/PostgreSQL acceptance environment.

---

## 16. Capability and completeness register

This is the current high-level master status. Supporting detail is kept in this file’s domain sections and `docs/architecture/syntha-v2-live-capability-register.md`; when status changes, this table must change in the same PR.

| Capability | Status | Main unresolved gate |
|---|---|---|
| Runtime clean-clone/start/readiness/shutdown | PROD-PROVEN | deployment-specific acceptance remains environment responsibility |
| Authentication + organisation foundation | IMPLEMENTED | continue role/capability coverage audits |
| Operational RU fashion MDM reference profile | IMPLEMENTED/PARTIAL taxonomy depth | governed `assortment.category / APPAREL` bootstrap and modular core validation implemented; expand category/product-type taxonomy only as canonical flows require it |
| Product Identity V2 | IMPLEMENTED | exact governed category references supported; UI/legacy catalog convergence plus intended live readiness evidence remain |
| PLM Planning/Styles | PARTIAL | converge all semantics on Product Identity |
| Materials/BOM/Measurements/Samples | IMPLEMENTED/PARTIAL | canonical Measurement Chart runtime/OpenAPI plus positive READY harness are synchronized on exact Product Identity + governed MDM; actual live execution and ODS/remaining workspace convergence remain |
| Sourcing/Tech Pack | IMPLEMENTED/PARTIAL | supplier/sourcing depth + ODS debt for Sourcing |
| Production/Final Quality | IMPLEMENTED | continue physical lineage/readiness E2E proof |
| ProductReadinessSnapshot | IMPLEMENTED | #117 contains separate fail-closed and positive READY public-runtime harnesses; intended live-runtime evidence remains |
| CommercialProjection | IMPLEMENTED | #118 exact-head repository acceptance passed; intended live-environment evidence remains |
| CommercialPublication | IMPLEMENTED/PARTIAL | projection-native immutable atomic published snapshot exists; V1 commercial history cannot seed fresh PriceList/BuyerCatalog after migration 075; staged lifecycle remains `COMM-LC-008`; legacy CatalogSku workspace debt remains `PUB-005` |
| PriceListVersion | IMPLEMENTED/PARTIAL | exact ProductSku price override + server-derived minor/major consistency implemented; market/effective dates and any required tax/eligibility depth remain `PRICE-009` |
| BuyerCatalog/Linesheet | IMPLEMENTED/PARTIAL | fresh writes require exact projection-backed Publication + ProductSku PriceList; historical V1 is read-only; complete variant-rich Showroom/Linesheet/Selection UX |
| Color × Size matrix | PARTIAL | exact immutable buyer matrix completion |
| WholesaleOrder/OrderCommit | IMPLEMENTED | expand live acceptance |
| SupplyCommitment | IMPLEMENTED | expand physical acceptance |
| Shipment/Receipt/Supplier Recovery | PARTIAL | exact ProductSku recovery contract implemented; live exact-lineage recovery acceptance remains |
| Inventory | IMPLEMENTED core | continuous reconciliation/golden-path proof |
| Generic aggregate ActualCost | IMPLEMENTED | live economics acceptance; new generic/post-close writes remain aggregate-only |
| SKU-specific ActualCost | IMPLEMENTED | live Shipment → ActualCost → Margin acceptance |
| Cost Allocation | IMPLEMENTED | exact pre/post-close ProductSku allocation contracts and automated PostgreSQL verification complete; live acceptance remains `ACC-004` |
| Landed cost / Margin / Close | IMPLEMENTED | pre-close allocation pin merged in #115; exact post-close reconciliation implemented and verified in #116; live Product → Margin proof remains `ACC-004` |
| KPI governance/methodology | PARTIAL production connection | complete exact runtime/persistence/reconciliation |
| ODS v1 | IMPLEMENTED with compatibility debt | burn legacy layers down; never add new dialect |
| Full Product → Margin live acceptance | PARTIAL | #117 readiness + #118 READY→BuyerCatalog repository-runtime slices implemented; P0.4 pricing guard extension in progress; intended-live evidence and downstream slices 4–7 remain |

---

## 17. Known architecture gaps and debt register

| ID | Priority | Gap | Required outcome | Status |
|---|---|---|---|---|
| `AC-LINEAGE-001` | P0 | Generic ActualCost accepted textual SKU without exact physical lineage | aggregate-only generic path; exact ProductSku physical path; DB fail-closed guard; preserve legacy corrections | CLOSED in #112 |
| `AC-HTTP-002` | P0 | Physical ActualCost resolver supported exact IDs but HTTP/OpenAPI did not expose them | request + response include `orderLineNo` and `productSkuId`; generic SKU scope removed | CLOSED in #112 |
| `ECON-003` | P0 | ActualCost → landed/allocation/margin/close/post-close path could lose or leave unproven ProductSku lineage | exact allocation line identity plus reproducible aggregate margin/close and explicit exact post-close reconciliation from frozen lineage | CLOSED by #116 at code/runtime lineage level — #114 fixed exact allocation, #115 bound pre-close allocation→margin/readiness/close provenance, #116 adds latest-adjustment exact post-close reconciliation plus effective-position provenance; implementation-head Verify `33345573039` and Syntha V2 CI `33345572980` succeeded; live Product → Margin proof remains separately OPEN as `ACC-004` |
| `ACC-004` | P0 | Live acceptance does not yet prove the connected Product → Margin spine | progressively prove canonical Product → Margin spine through public runtime/PostgreSQL slices | OPEN/PARTIAL — #117 implements both Product Readiness scenarios; #118 merged READY→Projection→projection-native Publication→PriceList→BuyerCatalog with separate brand/shop actors and green exact-head repository acceptance. Current P0.4 extends exact pricing/new-write protection; intended-live evidence and BuyerCatalog→OrderCommit→physical/economics slices remain open |
| `PUB-005` | P0 | Some historical CatalogSku/UI/Selection compatibility behavior remains flat-catalog oriented | projection-only variant-rich publication/buyer catalog; no new flat-catalog product/publication truth | OPEN/PARTIAL — fresh PriceList/BuyerCatalog can no longer originate from V1 flat commercial history; canonical price override is exact `productSkuId + wholesalePriceMinor`; migration 075 guards DB bypass and preserves V1 history unchanged. Remaining P0.4 debt: `/v2/catalog/skus` mutation/workspace dependencies, legacy technical readers/adapters and Selection fallback without pinned rich BuyerCatalogVersion |
| `COMM-LC-008` | P0 | `ARCHITECTURE.md` previously described a staged CommercialPublication lifecycle that the canonical V2 runtime does not actually implement | add one canonical fail-closed `DRAFT → READY → PUBLISHED → SUPERSEDED/ARCHIVED` lifecycle with API/DB/idempotency/tests, or formally revise the single lifecycle contract; no parallel publication truth | OPEN/GAP — runtime currently creates immutable V2 CommercialPublication directly as `published` |
| `PRICE-009` | P0 | PriceListVersion still lacks explicit market/effective-period and any business-required tax/eligibility depth | canonical ProductSku-exact pricing, market, effective_from/effective_to, server validation and immutable BuyerCatalog pin | OPEN/PARTIAL — ProductSku-exact override, minor-unit server validation and immutable BuyerCatalog pin are implemented; market/effective-period/tax-depth remain |
| `UI-006` | P1 | Legacy Omnidata CSS/JS compatibility layers remain loaded | migrate semantics to ODS v1 and remove debt only after validation | OPEN/PARTIAL |
| `UI-013` | P0 | Four Omnidata layers observed the DOM, wrote the attributes they observed, and scheduled that work with `queueMicrotask`, so the feedback re-armed inside one microtask checkpoint and the workspace never became interactive | apply with the observer detached and schedule through the event loop | CLOSED — before the fix a screenshot of the shell timed out in two independent browsers with "page is busy"; after it the login view renders, exposes its controls and reports no console errors |

| `NOTIF-011` | P0 | Notification projection read the projection key out of the event envelope, so every event whose JSON lacked `id` failed with PostgreSQL `23502`, could not be claimed, checkpointed, retried or dead-lettered, and reported `eventId: null` | key the projection on the outbox row primary key | CLOSED — proved live: 61 of 63 events were permanently unprojectable before the fix, 105 of 105 project afterwards with zero worker failures and zero notifications created |
| `NOTIF-012` | P0 | Outbox publication identified records from the event envelope, so one event whose JSON lacked `id`/`type` threw before the per-record try/catch and failed the whole publication batch; no well-formed event behind it was ever delivered | identify publication records by the outbox row key so failures are bounded and attributable | CLOSED — proved live against a real webhook receiver: before the fix 63 events stayed pending with zero deliveries, after it the two domain events are delivered and the 61 foreign-envelope events dead-letter |


| `ACC-REPLAY-010` | P1 | Commercialization acceptance cannot be replayed against an environment that already holds an active acceptance relationship | make the reserved-organisation setup converge on existing active state instead of requiring a first run | OPEN — surfaced by executing the gate twice against one live environment; the domain correctly rejects renewing an active relationship with `RELATIONSHIP_NOT_RENEWABLE` |
| `ACC-REPLAY-010` | P1 | Commercialization acceptance could not be replayed against an environment that already holds an active acceptance relationship | converge on the existing relationship instead of requiring a first run | CLOSED — the gate now reads the existing relationship back and requires it to be active between exactly the two reserved organisations before continuing; proved by three consecutive runs against one environment |
| `SPEC-007` | P0 | Historical architecture/product/UI detail was fragmented across docs/code | authoritative `ARCHITECTURE.md` + CI synchronization rule | CLOSED in #110 |

Every confirmed gap discovered during audit is added here before or with its implementation fix. Closed gaps remain in the table/change history or are moved to the closed section; they are not silently deleted.

---

## 18. Detailed screen/workspace specification rule

For every screen or workspace touched from this point forward, maintain a subsection using this exact schema:

```text
Screen ID / route / navigation entry
Purpose and personas
Upstream context and required IDs
Header / breadcrumb / primary action
Filters and defaults
KPI/metric cards
Main table/list: every column, source, formatting, sorting, filtering
Inspector/detail: every block and field
Forms/dialogs: every field, type, required/default/validation/source
Buttons/actions: icon, label RU/EN, visibility, capability, handler, API, state transition
Statuses: values, semantic tone, transitions
Empty/loading/error/permission/conflict states
Pagination/search/keyboard behavior
Responsive behavior
Data/API dependencies
Persistence/effects
Downstream effects and invariants
Automated evidence
Status/gaps/change reference
```

The existing shell/ODS sections above are already governed at this level. Individual business workspaces will be progressively brought to the same field-level inventory as they are audited or changed; an unaudited detail must be marked as such rather than invented.

---

## 19. Data dictionary rule

Every canonical entity changed from this point forward must list its business key/ID, organisation scope, mutable vs immutable nature, important fields, upstream foreign/version references, downstream consumers and lifecycle. Exact SQL remains in migrations, but this document must explain the business meaning of every material field introduced or changed.

Minimum frozen lineage fields for the current commercial spine include:

| Entity | Mandatory lineage meaning |
|---|---|
| StyleVersion | exact ProductStyle + predecessor/version/content identity + governed refs; category is exact current/effective/active/approved `assortment.category` entry version at write time and is frozen thereafter |
| Colorway | exact StyleVersion + brand + colour identity/ref |
| ProductSku | exact StyleVersion + Colorway + SizeValue; insert materializes one same-brand canonical zero InventoryBalance identity (available=0, reserved=0, version=1) without creating a movement or stock fact |
| CanonicalMeasurementChart | exact same-brand StyleVersion + Colorway + SizeScaleVersion; exact ordered SizeValues; exact current/effective/active governed `measurement.unit` and `measurement.point` versions pinned into chart evidence; runtime point names come from immutable snapshot `translations.ru/en`, localized descriptions/semantics from the same snapshot `attributes`, never source JSON aliases; DRAFT/PUBLISHED revision history is immutable across publication replacement |
| ProductReadinessSnapshot | exact StyleVersion + exact source/evidence versions + readiness result |
| CommercialProductProjectionVersion | exact READY ProductReadinessSnapshot + immutable projection version/content hash; current executable state is immutable `published` |
| CommercialPublication | exact CommercialProductProjectionVersion id/version/hash + ProductReadinessSnapshot + StyleVersion + exact Collection assignment + frozen ProductSku hierarchy/terms; current V2 executable record is immutable `published`, while staged lifecycle remains `COMM-LC-008` GAP |
| PriceListVersion | exact projection-backed CommercialPublication + projection/readiness/StyleVersion + brand/shop + currency + exact ProductSku wholesale/RRP/MOQ/delivery/availability snapshot; optional buyer price override is exact `productSkuId + wholesalePriceMinor`, with server-derived `unitPrice`; current gap: no canonical market/effective_from/effective_to and remaining pricing-policy depth |
| BuyerCatalogVersion | exact projection-backed CommercialPublication + exact ProductSku PriceListVersion + projection/readiness/StyleVersion + brand/shop + open Showroom + accepted/unexpired access grant + frozen ordered variant/ProductSku/price hierarchy; fresh V1 sources are forbidden while historical rows remain readable |
| Selection | exact buyer/catalog version and selected SKU/price context |
| OrderCommitSnapshot | exact agreed order lines/terms/currency/commercial facts |
| SupplyCommitment | exact order commit and physical line allocations |
| ShipmentLine | exact committed order line + ProductSku for canonical lineage |
| ReceiptClaim issue line | exact shipment line facts; when canonical ProductSku lineage exists, preserves `orderLineNo + productSkuId + sku(display)` for downstream recovery |
| ActualCostLedgerEntry | order/commit/supply; exact shipment/orderLine/ProductSku when SKU-specific physical v2; textual SKU is display/legacy compatibility only |
| CostAllocationRunSnapshot | exact order commit + landed snapshot + policy + active cost entry IDs; `lineageMode`; canonical allocation/economics rows preserve `orderLineNo + productSkuId + sku(display)`, legacy rows keep exact IDs null |
| MarginActualizationSnapshot | exact committed revenue + exact LandedCostSnapshot; canonical ProductSku lineage additionally freezes allocation status + exact CostAllocationRunSnapshot id/hash/policy/mode; legacy uses explicit `legacy-not-applicable`; canonical late-cost post-close uses `pending-post-close` until exact reconciliation, after which a new immutable margin on the same aggregate basis becomes `current` |
| CostCloseReadinessSnapshot | exact landed + margin basis and, for canonical ProductSku economics, the same immutable allocation run id/hash/policy/mode |
| CostCloseSnapshot | exact readiness + landed + margin basis and the same canonical allocation pin frozen at close |
| PostCloseAdjustment | immutable close baseline + new ActualCost/Landed/Margin chain; preserves closed allocation provenance and records resulting allocation status without inventing ProductSku allocation |
| PostCloseAllocationReconciliationSnapshot | immutable exact link from latest PostCloseAdjustment/pending margin/adjusted landed basis to an explicit new CostAllocationRunSnapshot and new current MarginActualizationSnapshot; freezes allocation id/hash/policy/mode, pending/current status transition, timestamp and content hash; one per adjustment |
| OrderEconomicsPosition | effective read model over one immutable OrderCommit: exposes close/latest-adjustment/latest-reconciliation IDs, effective landed/margin IDs and current/pending/legacy allocation provenance; an older reconciliation never overrides a newer adjustment |

---

## 20. Change register

| Date | PR / commit | Change | Master sections affected | Evidence/status |
|---|---|---|---|---|
| 2026-08 | #106 | Non-destructive live Campaign → Collection acceptance | 7.2, 15 | merged; public HTTP + PostgreSQL acceptance |
| 2026-08 | #107 | Repeatable owner bootstrap + isolated dev/test PostgreSQL clean-clone path | 2.3, 2.5 | merged; CI verified |
| 2026-08 | #108 | Order currency frozen to submitted Selection lineage | 7.5 | merged; Verify/PostgreSQL CI |
| 2026-08 | #109 / `4f5c452dede1ce9c8ffe518eddb8b6632cc89ad0` | Real supported runtime process smoke added to `verify:postgres` | 2.2, 15.2 | merged; Verify/MDM/PostgreSQL CI green |
| 2026-08-28 | #110 | Establish one authoritative platform/UI/data/architecture specification and CI synchronization contract | entire document | merged; `ARCHITECTURE.md` authoritative |
| 2026-08-29 | #112 | Close ActualCost textual-SKU bypass; require exact ProductSku physical and supplier-recovery identity; keep legacy pre-ProductSku physical/recovery fixtures aggregate-only; expose exact fields in authoritative OpenAPI 1.17.0 without version drift; add forward-only PostgreSQL guard and compatibility-safe tests | 1.2, 8, 9, 12, 13, 15, 16, 17, 19 | merged; required Verify/PostgreSQL CI green; supersedes draft #111 |
| 2026-08-29 | #114 / `47b39f5930540f27fe6bebfbe3bceb0e2b114fdf` | Preserve exact `orderLineNo + productSkuId` through Cost Allocation; prohibit canonical textual-SKU direct/custom resolution; retain explicit legacy mode; expose exact HTTP/OpenAPI output and persist through existing JSONB snapshots | 9.4, 12.3, 13, 15, 16, 17, 19 | MERGED; Verify run `33249799746` success; Syntha V2 CI run `33249799764` success; supersedes closed draft #113; `ECON-003` remains OPEN/PARTIAL |
| 2026-08-31 | #115 / `283c058eba5a83f39718e43aca3c9cc31205a120` | Bind immutable CostAllocationRunSnapshot into canonical MarginActualization → CostCloseReadiness → CostClose; preserve explicit legacy semantics; mark canonical post-close late-cost margin `pending-post-close`; expose allocation provenance in events/OpenAPI/store lookup | 1.2, 3.3, 9.4, 12.3, 13, 15, 16, 17, 19, 20 | MERGED; Verify run `33344377731` success; Syntha V2 CI run `33344377707` success; `ECON-003` remained OPEN/PARTIAL pending exact post-close reconciliation at that merge |
| 2026-08-31 | #116 / `fix/econ003-post-close-reallocation` | Add immutable latest-adjustment post-close allocation reconciliation; bind exact new CostAllocationRunSnapshot to a new current margin without changing aggregate economics or rewriting CostClose/adjustment; persist via migration 074; expose effective reconciliation/allocation provenance in economics-position and OpenAPI | 1.2, 3.3, 3.7, 9.4, 12.3, 13, 15, 16, 17, 19, 20, 21 | implementation-head Verify run `33345573039` success; Syntha V2 CI run `33345572980` success including PostgreSQL verification; `ECON-003` code/runtime lineage gap CLOSED; live Product → Margin proof remains OPEN under `ACC-004` |
| 2026-09-01 | #117 / `feat/acc004-product-readiness-live` (implementation head before authoritative sync: `d5477aa417ee11fc5e6a241e1a2fb848775fdc0d`) | Add guarded Product Identity → ProductReadiness live acceptance harness and exact blocked Readiness→Projection negative gate; add governed source-controlled `assortment.category` bootstrap with `APPAREL`; register `syntha_operational_master` impact; correct MDM validation so mandatory RU size systems/units are enforced globally across modular `mdm/reference` datasets while local semantic references remain same-dataset; explicitly preserve the negative fixture’s category+measurements blockers and document the positive exact-category + Measurement Chart prerequisite | 5.1, 5.3, 6.1, 6.2, 15, 16, 17, 19, 20, 22 | IMPLEMENTED harness + MDM bootstrap/validator contract; authoritative documentation synchronization completed in this branch; actual live acceptance environment evidence is still required before any `PROD-PROVEN` claim; positive downstream commercialization and remaining `ACC-004` slices stay OPEN |
| 2026-09-03 | #117 / `9a8490a` + `c6f1662` | Resolve canonical Measurement runtime↔OpenAPI divergence: document exact Product Identity/MDM request-response contract and supported POST/GET/PATCH/publish routes, preserve legacy SKU routes only as readiness compatibility, and add anti-drift regression coverage | 5.2, 6.1, 15, 16, 17, 19, 20 | IMPLEMENTED contract; composed OpenAPI remains 1.17.0; no `PROD-PROVEN` claim |
| 2026-09-03 | #117 / `8745eab` + `07350a6` + `4c420dc` + `26cce39` | Add separate canonical positive Product Readiness scenario without weakening the negative fixture: bootstrap source-controlled operational MDM through canonical infrastructure, create exact APPAREL/INT_ALPHA/INT_M Product Identity, create+publish CM/CHEST_CIRC canonical Measurement Chart, require READY with zero blockers, prove exact same-environment PostgreSQL lineage, preserve downstream isolation and bound idempotency keys | 5.2, 5.3, 6.1, 6.2, 15, 16, 17, 20 | IMPLEMENTED harness and automated contract; actual intended live acceptance execution still required before `PROD-PROVEN` |
| 2026-09-04 | #117 / `a960486c653666c7cd7da5dcb4f9d21c4a674d8e` | Final squash merge of governed assortment-category MDM, modular validation correction, canonical Measurement/OpenAPI synchronization and dual Product Readiness acceptance scenarios | 5.1–5.3, 6.1–6.2, 15–17, 19–20 | MERGED; exact pre-merge head `d92fd1e96ffd4b0a139cef82f23ce061af2d6c46`: Verify `33781485567` success, MDM Reference Data `33781485572` success, Syntha V2 CI `33781485600` success; no intended-live `PROD-PROVEN` claim |
| 2026-09-05 | #118 / `eb86a04b7ac9c6a0ba743882f59a6b33dfbfd113` → `main@0048d10c410231055d13436d61d0cf33db3e95b7` | Add P0.3 public-runtime READY→Projection→projection-native CommercialPublication→PriceListVersion→BuyerCatalogVersion acceptance with exact Collection assignment, Showroom, relationship/invitation, separate brand/shop actors and same-environment PostgreSQL proof; fix canonical Measurement persisted MDM snapshot consumption and deterministic acceptance reference command timestamps. | 2.6, 3.5, 5.1–5.2, 6.1–6.6, 15–17, 19–20, 22 | MERGED; exact head: Verify `33979864431` success, Syntha V2 CI `33979864438` success, MDM Reference Data `33979865154` success, Product Commercialization Acceptance `33979864435` success; no intended-live `PROD-PROVEN` claim; `COMM-LC-008`, `PRICE-009`, `PUB-005` remain open |
| 2026-09-08 | #119 / `fix/pub005-canonical-commercial-writes` | P0.4 convergence slice: forbid historical V1 CommercialPublication/PriceList from originating fresh buyer commercial truth; make price override exact `productSkuId + wholesalePriceMinor` with server-derived major price; validate exact frozen ProductSku line/hierarchy/terms; add migration 075 DB bypass guards and forward-only V1-history preservation tests; extend Product commercialization acceptance to exact ProductSku pricing. Legacy CatalogSku workspace writes and Selection fallback remain explicitly open rather than hidden. | 2.6, 3.1, 6.4–6.6, 7.4, 12.4, 13, 15–17, 19–20 | IMPLEMENTED locally; `npm run verify` green; PostgreSQL/PR exact-head CI pending; no `PROD-PROVEN` claim; `PUB-005` remains OPEN/PARTIAL |
| 2026-09-17 | `fix/omnidata-observer-feedback-loop` | Stop the Omnidata visual layers from reacting to their own DOM writes: apply with the observer detached, discard self-inflicted records before reconnecting, and schedule through `requestAnimationFrame` instead of `queueMicrotask` | 11, 17, 20 | IMPLEMENTED; the workspace shell was previously unusable — screenshots timed out in two browsers — and now renders and responds; no observed option set was narrowed |

| 2026-09-17 | `fix/notification-projection-outbox-key` | Key the notification projection on the `outbox_events` row primary key instead of a field inside the event envelope, and carry the relational `event_type` with it; this restores projection, claim accounting, checkpointing, dead-lettering and failure diagnostics for every event whose JSON envelope uses `eventId`/`eventType` | 14, 17, 20 | IMPLEMENTED; live proof: before the fix 61 of 63 outbox events failed every worker run with `23502` and flipped readiness to 503; after it 105 of 105 project, 23 consecutive worker runs succeed, and no notifications are created because only three domain event types have recipients |
| 2026-09-17 | `fix/outbox-publication-outbox-key` | Identify outbox publication records by the `outbox_events` row key instead of the event envelope, so an envelope the webhook wire contract rejects becomes a bounded per-event dead-letter rather than an exception that fails the whole batch and blocks every well-formed event behind it | 14, 17, 20 | IMPLEMENTED; live proof against a local webhook receiver — before: 63 pending, 0 delivered, worker failing in runWorker; after: 2 domain events delivered, 61 dead-lettered, queue drained |

| 2026-09-17 | `fix/runtime-db-timeouts-and-dead-ui-layers` | Bound request-path PostgreSQL work with pool-level `statement_timeout`/`lock_timeout`/`idle_in_transaction_session_timeout` so a single stuck query can no longer hold a pooled connection indefinitely; explicitly exempt the two workloads that legitimately run long (startup migrations via session `SET` plus destroy-on-release, retention maintenance via transaction-scoped `SET LOCAL`); validate all timeout inputs as integers | 2.1, 13, 14, 20 | IMPLEMENTED; `npm run verify` green; `npm run verify:postgres` green against the dedicated verification database; no `PROD-PROVEN` claim |
| 2026-09-17 | `refactor/http-transport-independent-pipeline` | Extract the duplicated node:http/Web Fetch request pipeline into one transport-independent `src/http/pipeline.mjs` and move status mapping to `src/http/error-status.mjs`; fix the resulting divergence where the Fetch adapter buffered an entire body before checking the size, so a request without `Content-Length` could exceed the limit in memory | 12, 20 | IMPLEMENTED; `npm run verify` green; `npm run verify:postgres` green; regression test fails against the pre-fix buffering behaviour |
| 2026-09-17 | `fix/http-server-fault-status` | Answer platform-side `DomainError`s (reader contract violations, failed clock/RNG) with HTTP 500 instead of a 4xx that blames the caller; classify by suffix so new reader/result/clock codes are covered on arrival; preserve the contractual 422 default for unclassified domain validation | 12, 20 | IMPLEMENTED; `npm run verify` green; `npm run verify:postgres` green; depends on the pipeline extraction PR |
| 2026-09-17 | `fix/acceptance-script-pg-import` | Make the live acceptance ladder actually executable: fix the load-time `pg` named-import crash in the Collection acceptance and production-reference bootstrap entry points, share the single zero ProductSku inventory identity delta between both Product Readiness scenarios so the BLOCKED scenario can pass live, correct the harness test fixture that modelled no balance-row delta, add a structural guard on the script import contract, and run Collection plus Product Readiness acceptance in the live-stack CI workflow | 15.3, 17, 20 | IMPLEMENTED; all three gates executed end to end against a live runtime and PostgreSQL: Collection passed, Product Readiness passed with `inventoryBalanceIdentityDelta: 1`, Product commercialization passed through READY → projection → publication → PriceListVersion → BuyerCatalogVersion |
| 2026-09-17 | `fix/acceptance-replay-relationship` | Let the commercialization acceptance gate run repeatedly against one environment: when the domain correctly refuses to renew an already-active relationship, read it back and require it to be active between exactly the reserved brand and shop before continuing; attach the domain error code to acceptance request failures so callers converge on state instead of parsing messages | 15.3, 17, 20 | IMPLEMENTED; consecutive runs against one live environment passed; `ACC-REPLAY-010` CLOSED |
| 2026-09-17 | `feat/static-asset-compression` | Serve static workspace assets with negotiated Brotli/gzip encoding, per-representation ETags, `Vary: Accept-Encoding`, an identity fallback when compression does not shrink the asset, and memoisation keyed by the existing content hash | 11, 20 | IMPLEMENTED; measured on the supported runtime: 96 blocking assets fall from 1165 KiB to 288 KiB with gzip and 272 KiB with Brotli; no new dependency, `node:zlib` only |
| 2026-09-17 | `chore/typescript-checkjs-baseline` | Add a type contract gate: TypeScript `checkJs` over the JavaScript sources with a per-file baseline in `ops/type-baseline.json`, wired into `npm run verify`; new type errors fail, and an improved file fails until the baseline is re-recorded, so recorded debt can only decrease | 2.1, 15.1, 20 | IMPLEMENTED; 575 known findings across 177 files recorded; validator self-tested against an introduced error and against an inflated baseline; `npm run verify` green |
| 2026-09-18 | `integration/ui-and-runtime-fixes` | Integrate the open runtime and UI fix branches onto one verified base: PostgreSQL request-path timeouts, the transport-independent HTTP pipeline, platform-fault status mapping, the type ratchet, the executable acceptance ladder, the two outbox key defects, negotiated static compression, the Omnidata observer feedback loops, the PLM form defects, the render/load retry loops, the fabricated controls, the contrast and type floor, the unreachable production and quality workspaces, user-facing error messages, and the ODS composite-wrapper, nested-chrome, topbar and tooltip corrections. Adds the missing render/load guard to `bom.js` and `production-orders.js`, without which every section after Specifications froze the tab | 10, 12, 13, 20 | IMPLEMENTED; `npm run verify` green (1246 passed); all 29 sidebar sections measured to open, where 23 of 27 previously froze the main thread |
| 2026-09-18 | `fix/navigation-paths-and-nested-chrome` | Resolve a measurement publication rule that could not be satisfied: a chart must be published against a published SKU, but publishing the SKU bumps its version, so every chart drafted beforehand was rejected as a stale snapshot. Publication is not a change of definition and a published SKU can never be versioned again, so a snapshot exactly one version behind a published SKU is accepted; anything further behind is still refused. Unblocks tech-pack issue, RFQ allocation and the production order, which all gate on a published measurement chart | 10, 12 | IMPLEMENTED; `npm run verify` green (1247 passed); proved live against a running server during an end-to-end walk of two articles |
| 2026-09-18 | `fix/navigation-paths-and-nested-chrome` | Close the findings of a measured visual audit of all 19 navigable sections: system footer text was white on white (contrast 1.00:1) after one layer repainted the ground and left the colour; the current-page breadcrumb was squeezed to 0px in four sections and to an ellipsis in eight; five sections showed the dashboard's name as their page because a fourth writer overrode the corrected resolvers; an inline label was classified as a metric tile and drawn as a box inside its own parent; a colspan empty-state cell was collapsed to one column by display:grid on a table cell; an always-empty actions slot rendered as a hairline strip | 10, 12 | IMPLEMENTED; `npm run verify` green (1247 passed); every claim measured in headless Chrome before and after |
| 2026-09-18 | `fix/single-language-control-and-counterparty-onboarding` | Keep one language control, in the topbar beside the account, where it stays reachable when the sidebar is collapsed; the second copy in the sidebar footer asked the same question twice. Add `scripts/bootstrap-counterparty-user.mjs`, the operational counterpart of `bootstrap-owner`: it gives an existing organisation a user who can sign in, which is what a buyer needs to accept a trading relationship and what a second reviewer needs to release a quality inspection. Neither `registerOrganisation` nor `grantMembership` is exposed over HTTP, so without it a single-brand database cannot reach either | 10, 20 | IMPLEMENTED; `npm run verify` green with PostgreSQL enabled (1271 passed, 0 skipped); both blockers proved cleared live — two quality inspections released by an independent reviewer and a buyer relationship accepted by the shop |
| 2026-09-18 | `fix/product-sku-inventory-reconciliation` | Close a circular dependency that made the first commercial order impossible to commit. Migration 065 introduced the canonical ProductSku inventory balance and backfilled it from catalog availability so the two counters could not diverge, but the trigger that initialises a balance for a new identity sets zero — correct at that instant, since a ProductSku exists before it is linked to a catalog SKU — and nothing reconciled them afterwards. Committing an order reserves against the canonical balance, and the only route that posts inventory runs through a receipt, which needs a shipment notice, which needs a fulfilment plan, which needs the order commit snapshot the commit itself creates. Migration 076 reconciles at the moment the two identities become one stock, adopting the catalog figure only while the balance is still untouched | 12, 13 | IMPLEMENTED; `npm run verify` green with PostgreSQL enabled (1273 passed, 0 skipped); proved live — the order committed and the deal space opened on a database where both had been refused |
| 2026-09-18 | `feat/attribute-filter-panel` | Replace the Filters control, which moved focus into the status select and called that filtering, with a real attribute filter panel modelled on the reference product's: a searchable attribute list, multi-select, Reset and Apply. Adapted rather than copied — the panel is built from each registry's own columns and rows, so every registry gains it without configuration; each attribute shows its values with the number of rows carrying each, so a choice takes one step instead of two; identifier-like columns are excluded because filtering by a unique code is a search; and applied filters stay visible as removable chips above the table after the panel closes | 10, 12 | IMPLEMENTED; `npm run verify` green with PostgreSQL enabled (1273 passed, 0 skipped); measured open in the Catalog registry with attributes, values and counts |
| 2026-09-18 | `feat/column-chooser` | Add a per-section column chooser beside the filter panel: every registry column can be switched off, the choice is kept per section and remembered between visits in local storage, the last remaining column cannot be switched off, and structural columns without a heading are not offered. Also translate the statuses that reached filter lists untranslated and showed English words inside a Russian interface (closed, submitted, confirmed, rejected, archived, qualified, approved, requested, received, allocated, awarded, issued, acknowledged, released, completed) | 10, 12 | IMPLEMENTED; `npm run verify` green with PostgreSQL enabled (1273 passed, 0 skipped); measured switching a column off and seeing it leave the table |
| 2026-09-18 | `feat/attribute-filter-panel` | Repair controls that render but do nothing, found by two independent agent walks of the running app. Inspector tabs carry their own fields and switch a real panel, and the v6/v7 layers no longer overwrite a module's own tab labels with five generic ones. `toast()` survives the `renderApp()` that ends every mutation, so a failed save is no longer silent. `actionButton()` no longer reloads over a dialog the action just opened and reports success. The topbar search filters registry rows instead of markup that no longer exists. The organisation and account elements carried a dropdown chevron and opened nothing; both open a real menu, with sign-out on the account. UI: inspector tabs, topbar, Модели/Планирование/Материалы. Status: measured in headless Chrome against the demo server. |
| 2026-09-18 | `feat/attribute-filter-panel` | Give the sourcing, calendar and notification tabs distinct content: quotation comparison and production allocations now have their own columns instead of being the RFQ register under another heading, the status filter applies on all three tabs, the calendar timeline has an empty state, and the notification history is a history panel rather than the same register re-sorted. |
| 2026-09-18 | `feat/attribute-filter-panel` | Stop clipping table content. A register declares its column count and the stylesheet gives it the width those columns need; long codes wrap, headers use two lines, badges wrap, and every cell keeps its full value in a title. Measured across all live sections: 0 clipped cells, 0 badges outside their cell. Also: every "Обновить" reports success or failure, Технические пакеты gained its header definition, and the dashboard no longer mounts filter controls it has no registry for. |
| 2026-09-18 | `feat/attribute-filter-panel` | Make Производственные заказы, Производственный календарь and Качество reachable. All three were complete — backend module, UI module and data — but `omnidata-v7-installed.js` ran before the three scripts that register their globals, so the nav shim skipped them and the sections stayed marked as planned. The shim cannot simply move: `measurements.js` and `samples.js` wrap the view and need V7 navigation active before them, which `validate-ui` and two tests enforce. It exports its `activate` handle instead and the three late modules claim their own entry once registered, Final Quality is registered with the V14 module adapters and page headers, and its table takes part in column sizing. UI: sidebar, three sections. Status: measured with 4 live records in each. |
| 2026-09-18 | `feat/attribute-filter-panel` | Put the governed product dimensions to work. The MDM catalogue already declared Возрастная группа, Новизна and Сезонность, and the attribute catalogue already defined `apparel.fit`, but no dictionary carried a single value, so nothing could be classified. Seeds `assortment.gender`, `assortment.age_group`, `assortment.novelty`, `assortment.seasonality` and a new `fit.class` classifier (18 bilingual entries) as one operational dataset; repoints `apparel.fit` from `fit.block`, which is the pattern-making base, to `fit.class`; adds `common.age_group` and `common.novelty` attribute definitions. Migration 077 replaces the `product_master_workspace` view so it resolves category, product type and gender to their bilingual names and projects style-version attribute values under `dimensions`. UI: Модели shows Посадка and Новизна as columns, Пол/Возрастная группа/Сезон as columns switched off by default, and all five in the inspector; the column chooser is keyed on a stable column key instead of a translated heading, so the choice survives a language switch. Status: written onto both demo styles through `POST /v2/product/attributes` and read back through `/v2/workspace`. Gap: gender is a style-version column, so setting it needs a new style version and is not part of this change. |
| 2026-09-18 | `feat/assortment-planning` | Add the planning object the platform was missing. A **Placeholder** is a planned assortment slot created against an open campaign before any style exists, carrying only targets: category, gender, age group, novelty, seasonality and fit as governed MDM references, plus capsule, drop, colourway count, planned quantity, launch date, recommended retail price and planned unit cost. Styles are linked to the slot they were developed for. Migration 078 adds `product_placeholders` and `product_placeholder_style_links` with three DB-enforced invariants: the planned margin is re-derived from price and cost and must agree within one basis point, a style belongs to exactly one slot, and a trigger moves a slot from `planned` to `in_development` the moment its first style is linked. Migration 079 adds the `assortment_plan_workspace` view, which reads the plan against the fact: the linked styles' published BOM totals give the actual unit cost and margin, and only when the BOM currency matches the placeholder currency. API: `POST /v2/assortment/placeholders`, `/transition`, `/styles`. UI: Планирование gained a Линейный план tab with a plan/fact/attributes inspector and a variance column. Status: exercised end to end on the demo server — placeholder planned at 70.0% margin, style linked, slot advanced by the trigger, duplicate link and negative margin both refused, fact read back from a live BOM. Gap: placeholders are created through the API only; there is no create form in the UI yet, and the assortment matrix and season colour palette are not built. |
| 2026-09-18 | `feat/assortment-planning` | Put people on a style. A product carried no responsible person at all, so no register could answer the question every register in this industry is read with: which of these are mine. Migration 080 adds `product_style_responsibilities` with five desks — designer, product manager, buyer, fabric manager, technologist — and a trigger that refuses a desk handed to anyone who is not an active member of the brand that owns the style, because the brand is the unit of access here. The same person is not assigned the same desk twice; two designers on one style are allowed. Migration 081 projects the desks onto `product_master_workspace` resolved to display names, together with the planned slot the style was developed for. API: `POST /v2/product/styles/{id}/responsibilities` and `/v2/product/responsibilities/{id}/release`. UI: Модели gained a Плейсхолдер column, a Байер column and three more desk columns switched off by default, a Команда tab in the inspector, and all of them appear in the attribute filter. Status: four desks assigned on a live style; a duplicate desk, a non-member and an unknown role were each refused. Gap: assignment is API-only, there is no picker in the UI yet. |
| 2026-09-18 | `feat/assortment-planning` | Stop every inspector stating its own name twice. The fidelity layer built the code chip from the heading, so an inspector with no separate code printed its title in capitals directly above the title, and then repeated the subtitle under a "Description" caption — which also called a code a description. The chip is built from the subtitle when that looks like a code, and the description block is only added when there is no subtitle to show. UI: every inspector in the workspace. |
| 2026-09-18 | `feat/category-attributes` | The language switcher's options are sized as standard 34px controls by the design-system layer while an older layer still pinned the container to 24px, so the options stood 5px proud of it and the container's grey track showed as a band around the active one. The container sizes to what it holds and no longer paints a track behind a pair of buttons. UI: topbar. |
| 2026-09-18 | `feat/category-attributes` | Load the governed attribute catalogue into the database and start enforcing it. A product attribute used to be any lowercase string, so a heel height could be written onto a jacket and a typo created an attribute no screen would ever show. Migration 082 loads all 94 definitions and 132 set memberships from `mdm/attributes/attribute-catalog.json`, and a trigger refuses an attribute that is not in the catalogue, or whose declared families do not include the product family the style's category declares in `mdm_entries.attributes ->> 'product_family'`. A style version with no category yet is left alone, because the category is chosen during development. A contract test re-reads the JSON and fails if the two drift. Migration 083 adds `product_category_attribute_workspace`, the field set a style is expected to carry in the order the governed set defines. Migration 084 carries each attribute's own name beside its value and the count the category expects, so a card can render any governed attribute without a hardcoded label. UI: Модели gained an Атрибуты категории inspector tab that reports how many of the category's fields are filled. Status: measured against the running demo — a footwear attribute on an apparel style and an ungoverned attribute are both refused, an apparel attribute is accepted. Gap: no edit form yet, so empty fields are counted but not fillable from the UI. |
| 2026-09-18 | `feat/category-attributes` | Database-enforced invariants now report themselves. `translatePostgresDomainInvariant` recognised only a fixed list of two `P0001` messages, so every other trigger surfaced to the user as `INTERNAL_ERROR`. It now recognises the convention a raised invariant already uses — `CODE: a sentence`, for both `P0001` and `23514` — so each invariant speaks with its own code and message, and anything that is not shaped like one is passed through untouched so a genuine database fault is never dressed up as a business rule. |
| 2026-09-18 | `feat/category-attributes` | Add the descriptive fields a merchandiser fills in on a style — маркетинговое название, капсула, дроп, аналог, в составе комплекта, тип комплекта, сегмент и тип ткани — as governed attributes rather than as new columns, so each arrives with its validation, its place in the category's field set, its bilingual label and its filter, and no screen had to be taught about it: all eight appeared in the Атрибуты категории tab with their Russian labels without a line of UI change. Migration 085 loads them. A boolean `false` was rendered as an em dash, which said "not filled in" about a field that said "no"; it reads Нет now. Gap found and respected: product attribute values are immutable snapshots — a trigger from migration 052 refuses any update — so correcting a field means a new style version. An upsert path was written and then removed rather than weaken that invariant; no UI flow creates a style version yet. |
| 2026-09-18 | `feat/style-lifecycle-card` | Bring the style lifecycle onto the card. The thirteen states and their transitions existed in the domain and were enforced on every write, but no screen showed them, so the only way to move a style was an API call. `allowedStyleTransitions` and `styleLifecycleMap` are exported and served by `GET /v2/product/lifecycle`, so the card offers exactly the steps the domain allows and cannot keep a copy that drifts. UI: Модели gained a Состояние tab with a rail of the main line of development — the states that are exits from it rather than steps along it are stated as the current state instead of drawn as a step — and a button per allowed transition. Six of the thirteen states had no Russian label and reached the rail as raw machine keys (`sample_review`, `technically_approved`, …); all thirteen are translated, and a test now fails if a state is added without a label. Status: measured on the demo jacket — moved черновик → в разработке → на оценке образца through the card, with the registry row following. |
| 2026-09-18 | `feat/colour-model` | Colourways had nowhere to be read: a style reported how many it had and nothing else. A governed palette is seeded as operational MDM reference data — `colour.family` with nine families and `colour.colour` with eight colours, each carrying a Pantone standard, a screen hex, an internal colour code and its family — and migration 086 adds `product_colorway_workspace`, which resolves all of it and derives the colourway article from the style code and the colourway code. The article is never stored: storing it would let it disagree with the two codes it is made of. UI: Модели gained a Цветомодели tab with the swatch, Pantone, family, article and SKU count; the swatch is drawn as SVG with a fill attribute because the loaded UI runtime may not write inline styles. Workspace gained a `colorways` section. Status: measured on the demo jacket — three colourways, two resolving to Pantone 19-1526 TCX / 19-5230 TCX with families Красный and Зелёный, one predating the palette and honestly showing no Pantone. |
| 2026-09-18 | `feat/registry-images` | Put a picture in the register. Media was stored and never projected, so no screen could show any of it. Migrations 087 and 088 add `product_media_workspace`: one image per colourway and one per style version, chosen across everything the style has — an image on the style itself first, then the hero shot, then the technical sketch, then the first gallery frame; a video or a document is never a thumbnail. A style whose pictures all hang off its colourways now shows one of them instead of nothing. UI: Модели gained an Изображение column that falls back to a tile with the style code when the stored URI does not resolve, which the demo data proves it must — every demo hero points at `cdn.example.invalid`. Workspace gained a `media` section. |
| 2026-09-18 | `feat/agent-sweep-fixes` | Defects found by an agent walk of the nine PLM sections and fixed. **The registry search matched the serialised row including its field names** — "tee" sits inside `categoryAttributeExpected`, so searching for a t-shirt returned every row while looking as if it had filtered; it matches values only now, through a bounded walk of the row. **The search filtered only on Enter** while the filter panel's own box filtered on input; it filters as you type, and the caret is restored because the re-render replaces the input. **The dashboard carried a search box with no register to filter**, three hundred pixels from the topbar search that works; a section with no register no longer gets one. **Планирование → Таймлайн re-sorted the portfolio by a date both campaigns share** and rendered byte-identical output; it draws a real timeline. **`shortId` sliced the prefix**, so every style read as the identical `product-…`; it drops the kind first. **A line-plan variance of +30 points was coloured as success** — a plan the fact beats that badly is a wrong plan, and now reads as one. **Publishing a BOM or a measurement chart said nothing**, while the revision action beside it did. **Технические пакеты → Обновить said nothing** and showed a failure banner above a contradictory "no tech packs for the chosen filters". **A transport failure reached the reader as the browser's own "Failed to fetch"**; `api.js` turns it into a sentence for every section at once. **Sample blockers printed `SAMPLE_SUPPLIER_REQUIRED`** where a sentence belongs; the code stays in the element's title for support. |
| 2026-09-20 | `fix/presentation-defects` | A pass over presentation, driven by what the product actually looks like on screen rather than by what the markup intends. **The application appeared to be full of spelling mistakes.** The language audit — a development instrument that marks any node holding a Latin word the interface dictionary does not know — painted a red dotted underline unconditionally, and business data is nothing but such words: every SKU code, style name, supplier name, order number, colourway code and retail-door code in the product carried one, on every screen. «SS27 Paris Showroom DEMO» was underlined as an error. The attribute and the `body.dataset` counter stay, so the audit still works for us; the paint is now opt-in through `data-ods-language-audit-visible` on the document element, and the genuine leaks it had been reporting are translated instead: «Решение: release» was the stored enum printed raw, «Нужен approver», «Закрыт production gate», «Перейти к Final Quality», «Версия execution», and the defect tally «C/M/m» that nobody outside quality can read. **Below about 1100px the header burst.** A rule added to give the breadcrumb room set three column floors totalling 740px and carried no breakpoint, so it overrode the responsive ladder the earlier layers had: the search box landed on top of the page content, and the organisation and user chips were clipped to an empty white rectangle — taking the way out of the application with them. The floors step down, then the header drops to two columns with the search on a row of its own. **The filter bar was a fixed five-track grid** while every view puts a different number of controls in it, so a view with four dropped the last to a second row and a view with two buttons left a hundred-pixel hole between them. It is a row that wraps: the search takes the slack, everything else is its own width. **A metric tile evicted its own caption**: label and value shared a row, and a worded value — «Только чтение», «BER-MITTE», «Отправлена» — took what it needed and squeezed the label down to «Ре…», «R…» and a bare «П». Label above, value below, as every other figure in the product is presented. Also: a command bar holding nothing but a legacy flex spacer painted a bordered 56px rectangle under the title of Рабочий стол, and a Production Order inspector with no action left painted a 74px one — both collapse; six-column tables had an 800px floor inside a 784px pane and so carried a horizontal scrollbar 16px wide for nothing, and the floor now follows the column count; the «Колонки» badge counted columns the section ships hidden, so a freshly opened register announced «−6» about a state nobody chose, and it now counts only what this person put away; and the wholesale order inspector printed `order_93104f24-…` where every other register already shows `ORD-93104F24`, with the full identifier on hover. Then the same walk from the buyer's side, which is the half an investor is shown. **The buyer's own catalogue screen carried six raw database keys** — `buyer-catalog-version_72842dd6-…`, the publication, the price list, the style version, the media file and the colourway — under labels that were class names («BuyerCatalogVersion», «StyleVersion»), and the same class name appeared in the prose above the order grid. They read as «Версия каталога / 72842DD6» now, with the whole key on hover, which is the pattern the rest of the product already follows. **A single look filled two screens.** The role system classifies the look rack as a "layout" and lays it out with auto-fit and 1fr, so with one look in the showroom that card stretched across the full row and its 3/4 figure stood over a thousand pixels tall, pushing the buyer's order grid off the page entirely — a defect introduced with the showroom itself in the previous change. **The order matrix was permanently scrolled sideways**: table minimum widths were assigned from a hand-kept list of class names, the Colour × Size matrix carries no class, and so the one grid a buyer actually writes an order in was held open to 720px inside a 362px card. Every table in the workspace is measured now, by its real column count, down to one. Also: a catalogue image whose link does not resolve showed the browser's broken-image glyph with the alt text spilling out beside it, and now says so in words; the cell said «MOQ 12 · ATS: 600», trade shorthand a shop assistant does not read, and now says «мин. 12 · доступно 600» as the look cards already did; the «from matrix to confirmed order» card had been built with the toolbar class, which the shell right-aligns, so it hugged its right edge with half the card empty; and the buyer's colour swatch was a disabled colour input that the design system paints as a disabled form field, all but hiding a pale colour — there is now one SVG swatch shared by the model register and the catalogue instead of two implementations. | 7.2, 11, 15, 17, 20 | IMPLEMENTED; `npm run verify` green **including PostgreSQL** (1360 passed, 0 failed, 0 skipped); measured in a real browser at 1440 and at 820, as the brand owner and as the retailer's buyer — header overlap gone, zero marked nodes across all 23 sections, no metric label truncated, no raw identifier or class name on any buyer-facing surface, the order matrix fits its card, and every look card is one size whether the showroom holds one or twelve |
| 2026-09-21 | `fix/navigation-and-narrow-shell` | Four more presentation defects, all of them reported from screenshots of the running product. **Thirty sections shared eight icons.** Eight of them — costing, measurement charts, RFQs, quotations, quality, assortments, prices, tasks — were drawn with the identical mark, and seven more shared another. With the sidebar collapsed to its rail the icon is the only thing on screen, so those sections were literally indistinguishable from one another: navigation by sight did not work. Twenty-three new marks in the same 24px stroke language; thirty sections, thirty icons. **The header was two toolbars pushed together.** The language toggle, the bell and the help button stood 34px tall; the organisation and user chips beside them stood 59px — taller than the 50px header itself, so they bled past its rule top and bottom. The cause underneath was that the action cluster carries the filterbar role, so the design system was styling the product's header as a filter bar, wrapping and right-aligning it; the header says it is not one now, and everything in the cluster is 36px. **The filter bar was ragged by eight pixels**: its one captioned control sat 8px lower than the search and the button beside it, because the row was centred on the boxes rather than aligned on the controls. My own regression from the previous change, when the bar became a wrapping row. **Below 700px the shell fell apart.** The base stylesheet turns it into a block and the sidebar into a horizontal strip at phone width; nine later layers pin the sidebar back to a fixed-width column, so what rendered was a 550px-tall rail floating in a full-width block with an empty grey rectangle beside it and the workspace pushed below all of it, the rail's icons cropped down the middle, and the fixed system footer lying on top of the page's last rows. The two-column rail already works between 700 and 980 and works at 360 too. Along the way the header's three parts each take a row of their own below 700 — the path had been collapsing to a nine-pixel white sliver with its own text hidden outside the box — and between 700 and 1180 the chips drop their text, the action cluster stops compressing below its contents (it had been clipping «RU» to «U» and squeezing the path to nothing), and the path drops the group name, which the sidebar shows in full anyway, so the page name survives instead of being the crumb that got cut. | 11, 15, 17, 20 | IMPLEMENTED; `npm run verify` green including PostgreSQL (1360 passed, 0 failed, 0 skipped); measured in a real browser at 1440, 1024, 700 and 390 — thirty distinct nav icons, one control height across the header, the filter bar's controls on one baseline, and at every one of those widths a two-column shell, a readable path, no clipped control and no element past the viewport |
| 2026-09-21 | `feat/order-quantity-grid` | The buyer writes the order (JOOR §64.2). The grid a season's buy is typed into was a plain number box per SKU: no totals, no paste, no feedback until the save was refused whole. It is now the surface the map asks for. **A paste from a spreadsheet is shown before it is applied** — a block pasted one column out of line silently rewrites a buy and the person who did it finds out at the brand's confirmation, so the preview names every cell it would touch, what it was and becomes, which cells the brand's own rules refuse and how much of the block fell off the edge of the style; nothing is written until the reader accepts, and one button puts it back. **A cell is judged where it is typed**, against the frozen price line, with the same codes the matrix core refuses a save with — so what is red in the grid is exactly what the server would reject, and a refused cell no longer takes the rest of the season down with it: it stays on screen in red and everything else saves. **Zero and blank stopped being the same answer**: blank is «not ordered», zero is «none of this», and since the stored matrix has no zero line the grid removes the line and says so instead of calling it an invalid quantity. Also: totals down every column, along every row and for the style in units, lines and money; arrow keys, Enter and Tab between cells; a save state always on screen with a debounced autosave behind it; and a cell nobody can order says whether it is sold out or whether the remaining stock is under the minimum. New `public/modules/order-grid-core.js` holds the arithmetic and the judgement with no DOM in it, under eleven tests. The quantity input became a text field with a numeric mode, because a number input silently swallows what a spreadsheet pastes — «1 200», «12,00» — and reports an empty string for it. The demonstration gained **a season still being written**: every selection in it was submitted, so every grid was read-only and none of this could be touched. A second published collection now carries an open showroom, an accepted invitation, a published buyer catalogue and a draft selection with one line filled and room to type. Five presentation defects from screenshots of the running product, all reproduced and measured first: a single look still filled a 1640px card because the previous override lost on specificity to the role system's layout rule; the pinned-context tiles were 171px wide, too narrow for their own captions; a lone status badge sat inside a bordered, padded, rounded box — two nested containers for one word; and the grid's own footline and its cells were being classified as a definition grid and laid out in columns, because the role heuristics match on words like «summary» and «badge» in a class name. | 7.5, 11, 15, 17, 20 | IMPLEMENTED; `npm run verify` green including PostgreSQL (1371 passed, 0 failed, 0 skipped); walked in a real browser as the retailer's buyer on the draft season — a quantity below the minimum went red in its own cell with «Минимум 12», a two-column paste onto a one-column style reported «Изменится ячеек: 1 · Не поместилось в матрицу: 1» and listed the one cell it would change, accepting it moved the total to 48 and undo put it back to 3; **not verified: a paste across several sizes and colourways**, because the demonstration catalogue still publishes one size and one colourway per style |
| 2026-09-21 | `fix/remaining-screenshot-defects` | The four defects from the reported screenshots that earlier passes had not closed, each reproduced and measured before it was touched. **The tech pack document scrolled sideways.** Its tables were given the workspace register's column-count floor — 780px for six columns — inside a 458px document column, so the list of operations was permanently cut off and a reader had to drag a scrollbar across a document they were about to hand to a factory. A printed page fits its measure and wraps its cells. **The fixed system footer lay across the page's last rows** at every width above 700px; the workspace reserves its height now instead of pretending it is not there. **The brand block stood 58px against a 56px header**, so its dark ground fell two pixels below the header's rule — the notch that appears to cut the corner off the mark. Both are one band and one height. **A counter that counts nothing was painted as a warning.** The tone came from the chip's position in the strip — second green, third amber, fourth blue, on every screen in the product whatever they counted — so «Черновики 0» was an alarm about an empty bucket while a real backlog three chips along was grey. The tone follows the label and the number now, and a count of zero is calm. Also: the buyer's screens said «Retail Door» in nine Russian sentences, once as the label «Торговая точка / Retail Door» carrying both languages at once. | 6.1, 11, 15, 17, 20 | IMPLEMENTED; `npm run verify` green including PostgreSQL (1371 passed, 0 failed, 0 skipped); measured at 820 and 1440 — no horizontal scroller anywhere in the tech pack document, brand block and header both 56px, and every zero-count chip neutral in all three tone systems |
| 2026-09-21 | `feat/attribute-change-history` | The first of the Omnidata gaps: **what changed, not just that something did**. The object history answered “what happened, when and who”; it could not answer “who moved the target price, and what was it before”, which is what an audit, a supplier dispute and a post-season review are made of. Migration 101 adds `object_attribute_history_workspace`: no new table and no second write path, because every outbox event already carries the whole row as it stood, so the change is the difference between one event and the one before it. **Two restrictions carry the whole correctness of that view, and both were learned by reading the output of a wrong draft.** The difference is taken only between consecutive events *of the same type* — the first draft reported «sku: SYN_TEE_R4_OFW_M → null» because a line event was followed by a creation event with no sku field, and nobody had changed a sku. And only events whose payload *is a snapshot of the aggregate* are compared, which is exactly those carrying an `id` equal to the aggregate's — the second draft reported «from: campaign → showroom» on a commercial cycle, which is two different advances read as an edit. Bookkeeping columns and the creating event are excluded: a new object is one event, not a hundred changes. Migration 102 closes what that exposed: **Product Identity emitted its events from a trigger with no metadata at all**, so a style's own history could say a state changed and not say by whom. The row already records the writer — `updated_by`, else `created_by` — and the event now carries it. Events already written keep their missing actor, because history that was not recorded cannot honestly be filled in later. New read surface `GET /v2/history/{subject}/attributes`, filtered by attribute, by person and by window, with an unparseable filter refused rather than ignored — a filter that is silently dropped answers a different question from the one that was asked. The product card gains an «Изменения атрибутов» tab beside «История», and an empty list says which of the two things it means. Two defects found building it: `.od-commandbar` is a reserved class — the fidelity layer moves every one of them into the registry column, so a panel-local filter row teleported out of the inspector; and `attributeValue` was declared twice in one scope, where the later declaration silently wins and every caller of the earlier one gets the wrong function, which showed as a column of dashes where the old and new values belong. | 5.2, 11, 15, 17, 20 | IMPLEMENTED; `npm run verify` green including PostgreSQL (1376 passed, 0 failed, 0 skipped), with a PostgreSQL test proving creation reports no edits, a real edit reports exactly the edited column with both values and its author, and an optimistic-version bump reports nothing; walked in a real browser — a style's tab reads «Состояние · черновик → в разработке», and a transition made through the API immediately after appeared as «в разработке → sample_review · Syntha Owner» |
| 2026-09-21 | `feat/bom-placement-and-main` | The second Omnidata gap: **where a material goes, and which one is the principal**. Both are columns in the printed tech pack a factory works from and neither existed here. Migration 103 adds `placement` and `is_main` to `bom_lines`, projected from the line payload so row and stored line cannot drift. **Placement** is the sentence a cutting room needs — «Полочка, спинка, рукава» — without which a bill is a shopping list that names a shell and a lining and leaves the factory to guess which goes where. **Main** is the one fact a care label, a customs declaration and a composition statement each need, and the rule underneath it is what makes it worth a column: **at most one principal material of each kind in a bill**, held in a partial unique index and again in the domain so the refusal names the two lines that disagree instead of arriving as a constraint violation. A bill may name none — one still being written has not decided — but not two. The inspector reads the bill the way a factory does: grouped by kind with the count on each group, because «three fabrics» is either right or obviously wrong at a glance and a flat list of eleven lines is eleven lines. The editor gains both fields, and ticking «Основной» clears the other lines of that kind rather than letting a person set two and be refused on save. Migration 104 carries both into `tech_pack_document_workspace`, and the printed document prints them. Two things found while building it: the module's own `optionalText` accepts a single character where the column requires two, so the domain would have accepted a placement the database refuses — a clear refusal turned into a constraint violation; and the ten-column materials table made the **document page** scroll sideways, which is the defect fixed two changes ago coming back through a different door, so a document table now scrolls inside its own frame and the page keeps its measure. | 6.1, 11, 15, 17, 20 | IMPLEMENTED; `npm run verify` green including PostgreSQL (1381 passed, 0 failed, 0 skipped); exercised through the real API — a placement and a principal written onto a draft bill, the bill published, two principals of one kind refused as `BOM_MULTIPLE_MAIN_LINES`; read in a real browser — the inspector shows «ТКАНИ 1» with the line marked «основной» and its placement beneath, and the printed document carries «Осн. — да» and «Где применён — Полочка, спинка, рукава»; **not done: consumption per size**, which needs the bill to move from SKU level to style level with a size range and is a separate change |
| 2026-09-21 | `feat/measurement-grading` | **Градация как правило, а не как девяносто шесть набранных чисел.** A twelve-point chart across eight sizes is ninety-six cells, every one of them typed, and a hand-typed grid has no rule inside it: nothing could tell a reader whether 52-54-56 was meant as a uniform two-centimetre grade or whether somebody meant 53 and mistyped. Migration 105 adds `grade_steps` to a point and `source` to a value. **The model is deliberately not the reference's.** Omnidata states grading in two columns (Gr.1 / Gr.2) and the older Syntha repository as one `increments_json` number per point; both are approximations of what a Russian chart actually states, which is «межразмерная разница» per interval, and it is not constant across a range — 44→50 commonly grades at 4 cm in the chest and 50→56 at 6. One number cannot say that and two columns only say it twice. A step per interval says it exactly, and a uniform grade is the ordinary case of it. A step may be zero (a point that does not change) or negative (a point that closes as the size grows). **A typed value beats the rule and is marked an exception.** A person who types a number into a graded chart meant that number, so it is kept — and recorded as `override`, because a factory that cannot see which cells depart from the rule has to re-derive the whole chart to find them. The base size is where the rule starts and is never an exception to it. **What it connects to, in both directions.** Upward: the chart's sizes come from the pinned `size_scale_version_id`, so a rule holds exactly one step per interval between those sizes and cannot outlive them. Downward: migration 107 carries the rule and the list of overridden sizes into `tech_pack_document_workspace`, and the printed document gains a «Градация» column plus a legend for the asterisk — a factory grading a pattern works from the rule, not from a column it has to re-derive. Sideways: the tolerances beside the rule are the ones `final-quality` already measures a sample against (`QUALITY_MEASUREMENT_WITHIN_LIMITS`), and because a graded chart is complete for every size, that check now has a value to test against in sizes nobody typed. Both the free and the MDM-governed chart paths grade identically — a rule that behaved differently depending on which register a chart was written in would be two rules. | 6.1, 11, 15, 17, 20 | IMPLEMENTED; `npm run verify` green including PostgreSQL (1388 passed, 0 failed, 0 skipped), seven new domain tests covering derivation in both directions, uneven steps, zero and negative steps, override marking and every refusal; exercised through the real API — two typed numbers became eight values on a four-size chart, and a ninth typed over the rule came back marked; read in a real browser — the matrix reads «градация: +4 / +4 / +6» with 96 100 104 110, and «градация: +2» with 75 marked «вручную»; two integrity mistakes found and fixed by reading failures — a CHECK may not contain a subquery (the element test is a JSON path predicate) and a JSON null is distinct from an SQL NULL, which refused every ungraded point by its own projection check (migration 106) |
| 2026-09-21 | `feat/pack-size` | **Кратность упаковки**, and with it the last piece of JOOR §64.2 that could not be built. Wholesale goods do not ship as loose units — a style leaves the factory in короба of six or twelve — and an order for fourteen of something packed in sixes is not an order, it is a conversation. The order grid could not give pack feedback because the frozen buyer price line said how few may be ordered and nothing about the box the goods travel in. Migration 108 puts `pack_size` where the minimum already starts, on the catalogue SKU, and it travels the same road: SKU → commercial publication → price list version → buyer catalogue version → the cell, frozen at publication so a buyer is held to the pack the brand published to them rather than to whatever the brand changed it to afterwards. **One rule is enforced where it is set rather than discovered by the person who cannot satisfy it:** the minimum order quantity must itself be a multiple of the pack. A minimum of twelve with a pack of five is not two rules, it is a contradiction — the smallest orderable quantity at or above twelve is fifteen, so the stated minimum is a number nobody can order. The brand hears it at the moment they set it. The rule lives in a CHECK and again in the domain so the refusal carries a sentence. The refusal in the cell **names the next whole box** («Кратно 6 — ближайшее 18») instead of leaving the reader to divide, the price line under the cell reads «мин. 12 · кратно 6», the paste preview explains an off-pack cell the same way, and stock below one box is treated as the dead end it is rather than as an orderable cell. The save is refused independently on the server (`BUYER_CATALOG_PACK_MULTIPLE_NOT_MET`) and in the matrix core, so the grid is a courtesy and not the enforcement. **Not conflated with `packRatio`,** which already exists on the commercial terms and is unused: that is a ростовка — the mix of sizes inside one assorted box, a rule about a whole row. Pack size is a rule about one cell. Enforcing the ratio is the natural follow-up and a different shape of validation. | 7.5, 11, 15, 17, 20 | IMPLEMENTED; `npm run verify` green including PostgreSQL (1393 passed, 0 failed, 0 skipped), five new tests covering the contradiction rule, the keep-on-edit behaviour, the nearest-box arithmetic and the whole frozen path; exercised through the real API — a pack of five against a minimum of twelve refused as `CATALOG_PACK_SIZE_CONFLICTS_MOQ`, a pack of six accepted; exercised in the running client — 12 and 18 accepted, 14 refused as `PACK_MULTIPLE_NOT_MET`, zero still meaning «не заказываем» and a SKU with no pack unaffected |
| 2026-09-18 | `feat/quotation-negotiation` | Quotation becomes a negotiation. A quotation may carry a price ladder — up to ten breaks — and the domain refuses one that rises, because the whole point of a break is that buying more does not cost more per unit; it also refuses two breaks at the same quantity and a smallest break above what the RFQ asks for. `quotedUnitPriceFor` answers which price applies to a quantity. A buyer may answer with a counter-offer naming the quantity and price they are prepared to place, recorded against the quotation revision it answers, and refused if it is above the price already quoted for that quantity — that is not a negotiation, it is a wrong unit or a wrong currency. An RFQ may also state that a sample is requested and name the tech pack it is priced against; both are optional, so a request written before they existed is still valid. API: `POST /v2/rfqs/{code}/counter-offer`, `tiers` on the quote body, `sampleRequested` and `techPackCode` on the RFQ body. Status: driven end to end against the demo — rising ladder refused, 100→5200/500→4800/1000→4400 accepted, counter above the quote refused, 1000 @ 4100 accepted. Gap: the UI does not yet show the ladder or offer the counter action. |
| 2026-09-19 | `feat/tech-pack-document` | The tech pack becomes a document a factory can work from. Migration 089 adds `tech_pack_operations` — the make sequence, with the machine class each step needs and its standard time — and a trigger that closes the sequence once the pack leaves draft, because an issued pack is what a factory quoted and committed against and changing its operations would change the document without changing its revision. A standard time of zero is refused: that is a missing measurement, not a fast operation, and it would quietly shorten the sequence the make cost is derived from. `totalStandardMinutes` is derived, never stored. Migration 090 adds `tech_pack_document_workspace`, which assembles the pack, the published bill of materials, the published measurement chart with its tolerances and the operation sequence in one read, so the four parts cannot contradict each other. API: `POST /v2/tech-packs/{code}/operations`, `GET /v2/tech-packs/{code}/document`. UI: a Документ action renders a print-ready page — cover table, table of contents, materials, measurements, construction/quality/packing notes and the operation sequence with its total — and `@media print` strips the application around it. Status: measured on a draft revision — six operations, 17.2 standard minutes, an operation on an acknowledged pack refused, a repeated sequence number refused. Gap: this is a print-to-PDF document, not a generated PDF file; writing PDF with Cyrillic by hand means embedding a font, which is separate work. |
| 2026-09-19 | `feat/libraries` | Make the governed libraries visible. Fourteen dictionaries and a hundred entries decided what a colour, a size, a point of measurement or a construction node may be, and not one of them could be read from the application: the reference data the whole platform is held to was the only thing nobody could look at. Seeds `design.construction_node` — twelve construction nodes, each naming the machine class it needs — which Omnidata has as a library and we did not. Migration 091 adds `mdm_library_workspace` and `mdm_library_entry_workspace`, the second stripping the governance bookkeeping out of the attributes a reader sees. New read-only surface: `GET /v2/libraries` and `GET /v2/libraries/{code}/entries` with paging and search, gated on an active membership — reference data is global, so there is nothing to scope by brand, but it is still not public. No write route exists: a library changes through the MDM bootstrap, under review, and offering an edit here would put unreviewed data into what every other rule is checked against. UI: a Библиотеки section listing the libraries with their entry counts and the chosen library's entries with their attributes. Status: measured against the running demo — 14 libraries, 102 entries, search and paging. |
| 2026-09-19 | `feat/object-history` | Let an object say what it has been through. Every mutation already recorded what happened, when and under which command; none of it could be read back, so the audit trail existed only for someone with a SQL prompt. Migrations 092–094 add `object_brand_index`, `object_history_subject_index` and `object_history_workspace`. Three decisions worth stating: access is decided by **who owns the object**, not by the event — fewer than six events in ten carry a brand of their own, and gating on the event would have hidden the rest behind a history that looked complete; each event carries a **subject** as well as an aggregate, so setting an attribute, attaching an image or assigning a desk appears on the style rather than only under its own identifier; and the **actor** is taken from the row when a database trigger emitted the event, because reading only the metadata left the who column empty on most of a product's history — the one question an audit trail is kept for. New read-only surface: `GET /v2/history/{subjectId}` with paging, gated on an active membership of the owning brand; an object with no history and one the reader may not see look the same, deliberately. UI: Модели gained an История tab. Status: measured against the running demo — 26 recorded changes on the demo jacket, each with its actor. |
| 2026-09-19 | `feat/supplier-portal` | The counterparty gets a surface of its own. A brand's whole season sits in one database, and the factory it asks for a price had no way to read the part addressed to it. Migration 095 adds `supplier_portal_grants`: one named person, one supplier, revocable, with two rules the database keeps rather than a service — a grant is issued only by an active member of the brand that owns the supplier, and it may not be held by an active member of that same brand. The second rule is the one that matters: if the person who awards the business also answers as the supplier, the portal's promise is still technically true and completely meaningless, which is the separation the quality module already keeps between inspector and approver. A grant names an **account**, resolved from the email the brand types, because every other relation is keyed by user id — matching on an address compared a string nobody else is keyed by and would have let the separation check pass silently. Migration 096 adds `supplier_portal_rfq_workspace` and `supplier_portal_order_workspace`, and the interesting part of them is what they leave out: the invited-supplier list and the quote array are read to decide whether this supplier is addressed and what their own answer was, and neither is projected. A supplier is told whether the business went to them, because they have to plan for it, and never who else was asked or at what price. A draft request does not appear at all. New read-only surface: `GET /v2/supplier-portal/{suppliers,rfqs,orders}`, gated on the grant alone — the caller is a member of nothing. Brand side: `GET/POST /v2/suppliers/{code}/portal-access` and `.../revoke`; revoked grants stay in the list, because an access list that forgets answers only half of "who can read our requests". UI: a ПОРТАЛ ПОСТАВЩИКА navigation group with Запрос на квотирование and Заказ, appended to the rendered sidebar only once the server confirms the account holds access, so a brand's own staff never see it; and a Доступ к порталу panel in the supplier inspector. The destructive sourcing actions (Квалифицировать, В архив, Отправить RFQ) moved off `window.confirm` onto the styled dialog every other destructive action already used. Status: measured live — the invited rep sees 2 requests with their own quote and 2 orders with their own price, sees nothing in the brand's registers, and sees nothing at all the moment access is revoked; a grant to an unknown address and a grant to a brand member are both refused. Gap: the portal is read-only. A supplier answers through the brand today; letting the portal write a quotation means an account outside the brand mutating a brand aggregate, gated on the grant and forced to the granted supplier's code, and it deserves its own pass. There is also no invitation flow: access is granted to an account that already exists, and the error says so. |
| 2026-09-19 | `feat/supplier-portal` (follow-up) | Close what an automated walk of the new portal found. A command's response is the aggregate it changed: a portal grant was being folded into the supplier register, which replaced the supplier with the grant and made the next render throw on a field a grant does not have — so a **successful** invitation broke the screen. A portal-only account no longer sees the brand's navigation: every one of those registers returned an empty list without a membership, so nothing leaked, but the sidebar still showed an outside company the shape of the brand's operation, its vocabulary and its action verbs, on screens that could do nothing; an account belonging to no organisation now gets the portal as its whole navigation and lands on the first request addressed to it rather than on a dashboard of zeroes. Portal refusals are translated instead of surfacing the server's English sentence. A confirmation dialog closes when the command succeeded rather than unconditionally, so a refused action no longer looks accepted. `allocation` stopped being an English word inside Russian sentences on three screens. Pluralisation fixed on the access panel. | 8, 11, 17, 20 | IMPLEMENTED; `npm run verify` green (1340/0); measured live — invitation, refusal, revoke and re-grant all leave the screen intact, the rep's sidebar holds the two portal items and nothing else |
| 2026-09-19 | `feat/placeholder-import` | Read a season plan out of the file it was planned in. A buyer writes the season in a spreadsheet months before anyone opens this application, and the only way in was to retype it slot by slot. What makes this worth building is not the loop over rows: it is that the file is written by a person, in their words, so the import accepts what a person writes — «Одежда» rather than an MDM entry id, «1 250,00» rather than 125000 minor units, an empty cell rather than an explicit null — and refuses the rest **by column and by line**, so a file with four typos is fixed once instead of four times. Three decisions shape it. It runs **twice**: `mode: "validate"` says what would happen and writes nothing, `mode: "commit"` does it, and the screen only offers the second after showing the first. A commit is **all or nothing**: a season half-loaded looks exactly like a season, and the gap is found weeks later by somebody wondering why a category is thin. The single exception is a slot that already exists, which is **skipped rather than refused**, so a corrected file can simply be sent again — measured: re-sending the same file creates nothing and reports two skipped. Dictionary columns are resolved by code, name, translation or alias, and a word matching two entries is refused rather than guessed, because that is a governance problem and picking one would put the wrong entry in a plan. A date is accepted only as `01.09.2027` or `2027-09-01`: reading `03/04` as one or the other moves a launch by a month in silence. An unrecognised column is **reported, not ignored** — a «Бюджет маркетинга» column that quietly does nothing is how a plan arrives with a field missing. New surface: `POST /v2/assortment/placeholders/import` and `GET /v2/assortment/placeholders/import/template`, the latter publishing the column contract so the browser's header matching and the downloadable CSV template are built from the same definition the server validates against — a format described in two places drifts, and it drifts in the file a customer already filled in. UI: an Импорт плейсхолдеров action on the Линейный план tab — file or paste-from-Excel, a verdict table with refused rows first and each problem named in the author's words, and Загрузить disabled while anything is rejected. Status: measured live — a pasted 5-row table with a duplicate code, a malformed code, a missing name, a bad currency, a three-decimal price and a non-numeric cost produced six named problems on four lines and loaded nothing; a clean 3-row file loaded and appeared in the line plan with category and gender resolved from Russian words and the margin derived. Gap: XLSX is not read — the file must be saved as CSV or pasted; and the import creates slots, it does not update ones that already exist. | 3.7, 5.1, 12, 15, 16, 17, 20 | IMPLEMENTED; `npm run verify` green including PostgreSQL |
| 2026-09-19 | `fix/production-readiness-sweep` | Four things an automated walk of the whole application kept catching. **Allocating no longer stops halfway.** «Создать PO и разместить» reported success, left Производственные заказы empty, and required the buyer to retype the same request code by hand into a bare text box in another section. The two commands stay two — they need different standing, and the production order is the factory's copy of the deal — but they are no longer two screens: the allocation opens the order in the same step, and an actor who may allocate but not open orders is told so rather than left with a half-finished chain. When the second step fails the message says the allocation succeeded and which step did not, because reporting the whole thing as a failure would be false. **Identifiers stopped being names.** Orders, deal spaces, selections and the calendar events that mention them showed `order_8c22a11c-e886-4c9d-8078-bebb804f6a65` where a name belongs — unreadable, unquotable, and indistinguishable from the next one. A display rule now renders `ORD-8C22A11C`, applied in the registers, the inspectors and inside server-written sentences; the full identifier stays the key, is shown on hover, and is a field in the inspector. This is deliberately a display rule: a real order number is a domain concept the platform still lacks, and adding one touches every snapshot and lineage check that pins an exact id, so it is named as a gap rather than improvised here. **The path became the control it looked like.** The breadcrumb carried a back chevron, was a plain `div` with `cursor: auto` and no handler, and did nothing; its root is now a button that returns to the workspace, draws the chevron only when there is somewhere to go, and disables itself on the workspace. **The last native dialogs are gone.** `window.confirm` cannot be translated, carries the browser's chrome and blocks the page; seven call sites — publishing a BOM or a measurement chart, starting a revision, a sample action, issuing a tech pack, the generic action button and the unsaved-changes guard — now share one styled confirmation, where Escape and the backdrop both mean no. `tests/ui-form-dirty-guard.test.mjs` pinned the native call; the assertion was rewritten to state the new intended behaviour rather than dropped. | 8, 11, 17, 20 | IMPLEMENTED; `npm run verify` green (1351/0); measured live — the path navigates and disables correctly, `ORD-`/`SEL-`/`DEAL-` references render and a supplier code passes through untouched, and a dirty form closes through a styled modal with zero native dialogs |
| 2026-09-19 | `fix/placeholder-import-sweep` | Twelve defects an automated walk of the import found, worst first. **The check passed rows the load then refused.** The reader tested that a name was present, the domain required two to two hundred characters, and nothing reconciled the two — so a thousand-row file could validate clean and die on commit with a message naming no line and no column, which is the exact failure the module exists to prevent. The reader now enforces the domain's own bounds for names, capsule and drop, the contract publishes them, and a test compares the two sets rather than trusting them. **The application's own English template was rejected by its own import.** `Colourways` and `Launch date` were not among the accepted spellings, so an EN user filled them in and every value was dropped in silence — the agent's bad date was never reported at all, because an unmapped column is not read. Synonyms added, and a test now round-trips every template heading in both languages through the reader. **Server refusals are translated**, including the row limit. **A header-only file** was told its required columns were missing when the header held all four. **An empty paste** did nothing and said nothing; **clearing the paste box** left the previous table loaded and Проверить armed over rows nobody could see — the box now listens on `input`, not `change`. **Two headers mapping to one field** silently dropped the second; it is named. **No busy state:** Проверить stayed live while its request was in flight, so a second press was a silent no-op; it now says «Проверяем…» and the inputs lock. **A phantom № column** appeared non-deterministically because the global `.od-table` decorator prepends a row number to every table it finds and raced the dialog's rebuild — the verdict table is not a register and no longer claims to be one. **A thousand verdict rows** rendered unvirtualised; the table stops at 200 and says so, with refused rows always first. **A 1000-row import showed 200 rows** in the register with nothing to say it was truncated — the line plan now says what it is holding back. **«1 slots loaded.»** | 3.7, 11, 12, 17, 20 | IMPLEMENTED; `npm run verify` green (1351/0); measured live — a one-character name is refused at the check with Загрузить disabled, the EN template round-trips and its bad date is reported, clearing the box clears the table, a repeated column is named, and a header-only file states only the problem it has. Gap: `placeholders` is still not a pageable workspace section, so a season over 200 slots is shown truncated rather than paged |
| 2026-09-20 | `feat/demo-environment` | Make the demonstration environment production-shaped. The wholesale half of the product was the half nobody could see: no counterparty relationship, no invitation, no selection, no order, so five sections rendered an empty state and the retailer's side of the platform was a claim rather than a screen. `scripts/seed-demo.mjs` (`npm run seed:demo`) builds it **through the real services**, never with INSERT statements, so a step that breaks here breaks for a customer too. Three properties: it is **idempotent** — every step looks for its own fixture first, because repeated acceptance runs are what left the database with seven near-identical campaigns named R1 to R5; **every role is a real account** — the platform enforces that a run's inspector cannot approve their own disposition, that a supplier is not the brand and that a buyer is not the seller, and none of that can be shown from one login, so four people sign in because four people have to; and it carries **one coherent season end to end** — retailer, relationship, showroom, invitation, retail door, buyer catalogue published for that shop at those prices, selection, order drafted by the buyer, accepted separately by both sides, committed, deal space opened, and the quality inspections released by a second person in the brand. Alongside it, what a walk of all four accounts then showed. **A retailer saw eleven of the brand's own registers reading zero** — models, materials, bills of materials, suppliers, tech packs, quality — which is not an empty state but somebody else's desk; navigation now follows standing, hiding those views only from an account with no brand membership at all, the same principle the supplier portal already used. **Every notification was in English and could never be anything else**, because the projection stored a finished sentence: a notification now carries the facts as well (`params`), the sentence is composed where the reader's language is known, and a notification written before this still renders through its stored text. `DealSpace`, `Selection`, `submitted` and `attached` stopped appearing as machine words inside Russian screens, and the commercial cycle stopped being offered to a buyer by its generated identifier. | 2.3, 11, 14, 15, 17, 20 | IMPLEMENTED; `npm run verify` green (1353/0); measured live — all four accounts walk every section they can reach with **zero uncaught errors and zero console errors**, the brand keeps 23 sections all with content, the retailer has 9 all with content, and the supplier sees only the portal |
| 2026-09-20 | `fix/visual-consistency-pass` | A measured pass over what the interface actually renders, at 1024, 1280 and 1440, in both languages. **Money looked wrong.** The bill-of-materials section read its locale through `I18N.locale()`, a function that does not exist — the call returned undefined, never matched `'en'`, and every amount in the section stayed Russian-formatted inside the English interface; production orders and sourcing passed `undefined` as the locale and got the machine's, not the reader's. And totals printed four decimals: «Средняя себестоимость 28,0919 €» is a correct figure that looks like a rounding error nobody caught. A total is money and shows two decimals; a **line's rate** is a price per metre and keeps its precision, because those digits are the reason the total is what it is. **Three of twenty-three navigation labels needed a hover to read** — «Модели и цветовые…», «Спецификации и себестои…», «Производственный кален…» — so long labels take a second line instead of an ellipsis, and every entry now fits inside the same 34px row. **Status chips in the registers were being cut, not ellipsised**: `text-overflow` does nothing to an element child, so a cell reading «Артикул не опубли» had told the reader nothing and cost them a hover; chips wrap and the row grows, since a cell's height is a floor. Column headings wrap for the same reason. **The linesheet register's columns were pinned by position** — 37px, then 32px — for a table whose first two columns used to be a checkbox and an icon; the columns are «Публикация» and «Статус» now, and both headings plus a cell reading «Опубликовано» in 46 pixels were cut. The old widths are left in place rather than removed, in case something still renders the shape they were written for; the tables that exist today are told to divide the row evenly, which is what a fixed layout does unaided. | 11, 17, 20 | IMPLEMENTED; `npm run verify` green (1353/0); measured at three widths before and after — clipped cells in Спецификации and Таблицы измерений went 4 and 6 to zero, the linesheet register 6 to 1 (a 64-character content hash, ellipsised), sidebar clipping 3 to 0; money reads «43,58 €» in Russian and «€21,250.00» in English |
| 2026-09-20 | `fix/plm-walk-findings` | What an end-to-end walk of the product-development half found, worst first. **An acknowledged tech pack silently lost its measurement table.** `tech_pack_document_workspace` resolved the chart by SKU and `status = 'published'` while the pack records exactly which chart and version it froze; the moment anyone opened a new revision the published row became a draft, the join matched nothing, and the document rendered with no measurements — status still «Подтверждён фабрикой», допуск still «Допущен», no warning. One ordinary action in the interface caused it. Migration 097 resolves the pack against its own frozen reference: the live chart while it is still that version, otherwise the archived copy in `measurement_chart_revisions`, and the payload now says which of the two it is. The document also states the case it cannot cover — a draft has frozen nothing, and a frozen revision that cannot be found says **do not build from this document** rather than showing an empty table. **Two of the four tabs in Модели could never show anything:** «Готовность» kept only styles that are not ready and «Коммерческая проекция» only those without one, so with a season in good shape both were blank — under the register's own headers, three inches below a KPI strip reading «Готовы 14». They show every style now, ordered worst-first. **An empty register accused a filter that was not set**: «Нет данных по заданным фильтрам» was printed unconditionally, sending a reader to clear a filter that is not there; an empty register and an excluded one are now different sentences. **The column chooser's badge counted hidden columns beside the word «Колонки»**, so «6» read as six columns on a table showing twenty-four, and hiding one more took it to «7»; it carries a minus sign and says so in words. **Five of eight inspector tabs sat outside the panel** — the design-system layer styles an inspector's tab strip with the same no-wrap rule as a workspace-wide filter bar, which is wrong for a 326px column; inspector tabs wrap, and the override lives in the module layer because the ODS stylesheet is deliberately free of module class names. Also: governance vocabulary (`classifier`, `master`, `global`) shown raw in Библиотеки beside a properly translated Статус column; dates in Образцы and Технические пакеты ignoring the EN toggle; «Версия Measurement Chart» as a label; a percentage written «71.4%» with a dot beside «189,00 €»; and money that dropped its second decimal, printing 4.10 as «4,1». | 5.2, 6.1, 11, 15, 17, 20 | IMPLEMENTED; `npm run verify` green (1353/0); measured — all 14 tech pack documents resolve their frozen chart or state why not, all eight inspector tabs sit inside the panel at 1024 and 1440, zero uncaught or console errors across the walk |
| 2026-09-20 | `feat/product-master-writes` | Give the Product Master the controls it was describing. The core register could only be read: the card announced its own gaps — «Ответственные не назначены», «Заполнено 0 из 40 полей» — and offered nothing to press, while every command it needed already existed in the API. Four surfaces now close them: **a desk is assigned and released** on the Команда tab, **a colourway and an image are added** from Цветомодели, and **each expected category field is filled** from Атрибуты категории. Two reads were missing and are added rather than improvised. `GET /v2/product/style-versions/{id}/category-attributes` publishes the field set the chosen category expects, with each field's type, its dictionary and the catalogue version a value must be written against — the register knew only how many fields were expected, and a person cannot fill in a number. `GET /v2/organisations/{id}/members` publishes the roster, readable by the people in it: the workspace returns only the reader's own membership, so the only colleague the assignment form could name was the reader, by their generated user id. Migration 098 projects the responsibility's own identifier, without which a desk could be assigned and never released. Three defects found while walking it: the design system lays a dialog's form out as a two-column grid and forces every direct `div` to span it as a flex row, so wrapping fields in a grid of my own squeezed a number field to 147px — the fields are direct children now, which is the shape every other dialog already has; a hidden error line was given a display by that same layer and showed as an empty pink box above every footer; and **the open inspector tab was lost on every re-render** — it lived only in the DOM, so a save, a refresh or picking another row put the reader back on the first tab, which is what filling in a field did every single time. Also: a search that emptied a register said nothing in three of them, because their wrapper was not in the list the note is hung on; and unversioned assets were served with `max-age=300`, so for five minutes after a deploy a returning browser ran old scripts beside new ones — they revalidate now against the content hash that already backs the ETag. | 5.2, 11, 15, 17, 20 | IMPLEMENTED; `npm run verify` green including PostgreSQL (1353/0/0 skipped); walked in the browser — a category field written and shown as «Заполнено 1 из 40», a desk assigned from a roster naming real people, a colourway added and counted, the open tab surviving a row change and a full refresh |
| 2026-09-20 | `fix/client-walkthrough` | Walked the application as a customer would — create a season, a collection, a SKU, a bill of materials, a measurement chart, a sample, a tech pack — and fixed what that walk hit. **The creation chain was a series of unexplained dead ends.** A new SKU's card offered only «Редактировать»: publishing needs a published collection, publishing a collection needs an open campaign, and none of that was said — the buttons were simply absent. Each missing control now names the step that would bring it back, so the chain reads campaign → collection → SKU → bill without anybody guessing. **The bill-of-materials dialog could only fail**: it offered every published SKU while every one of them already had a bill, so every path ended at «BOM already exists for SKU» in English. It offers the SKUs that can take one, distinguishes «all of them already have one» from «none is published yet» from «there is no SKU», carries `required` on its nine fields, shows the refusal in the form in the reader's language, closes on Escape, and confirms the save — it had been closing in silence. **The measurement editor had no `try` at all**, so a refusal was an unhandled rejection: the dialog stayed open, nothing was written, and the person was told nothing. Same treatment. **The shared create dialog was laid out wrongly by the design system**: it forces every direct `div` of a dialog's form to span both columns as a flex row, so five fields sat on one line with the last wrapping and the save button stretched to half the dialog; the field grid keeps its two columns, and Close and Save sit together in a footer as they do everywhere else. Migration 099 lets a tech pack document name the state of a bill it could not attach, as migration 097 already did for the chart: a draft says it is a draft, and an absent one says **do not build from this document**. | 6.1, 11, 15, 17, 20 | IMPLEMENTED; `npm run verify` green including PostgreSQL (1353/0/0 skipped); walked in the browser — a campaign, collection, SKU, bill of materials, measurement chart, sample and tech pack created end to end through the interface, with every refusal shown in the form and every save confirmed |
| 2026-09-20 | `feat/showroom-presentation` | The showroom becomes something a buyer is shown. Until now it was a permission: it named a collection, opened, and let an invited shop read a table of articles — which is a price list. The JOOR map calls for a showroom builder (§89.3) and this is its first working piece: a brand composes **looks**, each with a photograph, a short text in both languages and the pieces it is made of, and the buyer meets that before the grid. Migration 100 adds `showroom_looks` and `showroom_look_workspace`. Three rules are kept in the database as well as the domain because they are what makes the presentation honest: **a look may only show products from the collection its showroom presents** — a look carrying something the buyer cannot order is a disappointment with a picture on it; **positions are unique within a showroom**, so the order a brand chose is the order a buyer sees rather than whatever the database returned; and a look belongs to one showroom and dies with it. A look holds between one and twenty-four pieces — an empty look is a photograph, and nobody orders a photograph. An image is an `https` link and the rule says so, because there is no file store yet and a relative path or a data URI would break the moment the page is printed or the link reaches a factory. Products are **picked from the collection**, never typed, so the one mistake the rule exists to prevent cannot be made in the form. Reading is governed by the invitation that already decides what a buyer may see, so presentation opens no new door. New surface: `GET/POST /v2/showrooms/{id}/looks`, `PATCH/DELETE /v2/showroom-looks/{id}`. Two tabs beside it that were labels switching nothing — Товары and Доступы — now carry what they are named after. From the Omnidata video: the tech pack document's headings are **bilingual** («Bill of Materials / Список материалов»), because a Russian technologist and a factory abroad read the same printed sheet and a document that picks one language makes the other guess. | 7.2, 11, 15, 17, 20 | IMPLEMENTED; `npm run verify` green including PostgreSQL (1360/0/0 skipped); walked in the browser — a look composed by the brand with two pieces, and the same look met by the retailer's buyer above their order grid, each piece carrying its price and minimum |
| 2026-09-21 | `feat/aql-acceptance-sampling` | **Приёмочный контроль по плану, а не по мнению инспектора.** A Final Quality run carried three numbers a person typed when starting it — how many pieces to inspect, how many major defects were tolerated, how many minor. Nothing tied them to the size of the lot and nothing recorded where they came from, so two inspections of one product could be judged by different criteria and the record could not say which criterion either used. That is the one thing an acceptance decision must be able to say. A run now **states its criterion**. Either it names the standard, the inspection level and two accepted quality limits — a tighter one for major defects, a looser one for minor, which is how a brand actually inspects — and the plan is read off the rows the brand holds; or the plan was agreed with one factory, and it is recorded as **agreed** rather than dressed up as a standard. Migration 109 adds `aql_sampling_plans` and holds the plan set as **data, not code**, and **seeds no coefficients of a published standard**: a brand may have adopted ГОСТ Р ИСО 2859-1 or something else, and the coefficients of a published standard are not something to hard-code from memory into the module that decides whether goods ship. Four rules live in the database because each makes a plan set usable or nonsense: rejection = acceptance + 1 (single sampling leaves no undecided gap); the sample cannot exceed the smallest lot its range covers; ranges of one (brand, standard, level, AQL) may not overlap, or a lot resolves to two criteria and the answer depends on row order; and the payload projection. Three more live in the domain: a lot no row covers is **refused rather than judged by the nearest row**, because "nearest" is a guess about how many defective garments may ship; both limits must be read off **one sample**, since a run draws one; and a major defect may not be tolerated more readily than a minor one. New surface: `GET /v2/aql-sampling-plans` publishes the sets a brand works to, so the inspector **picks** a criterion instead of typing a standard code from memory — the sample size is deliberately not offered as a choice, because it is not one. Two defects found while walking it. The plan notes rendered as adjacent `<small>` elements and **ran together into one line**. And the run history rendered its items **two different ways in the same list**: a run's class carries its status, one status is `in-progress`, and the role system matches component heuristics on class-name tokens — so the token `progress` gave the active run a block layout that completed runs never got. Also closed a dead end reachable from ordinary use, not misuse: a run may be approved neither by the person who started it nor by the person who completed it, and the demonstration brand had **two** members, so one starting and the other completing left a lot nobody in the brand could decide; the seed now has a third member and **chooses the approver per inspection** rather than fixing one in advance. | 6.4, 11, 15, 17, 20 | IMPLEMENTED; `npm run verify` green including PostgreSQL (1403/0/0 skipped); walked in the browser — a lot of 400 pcs re-inspected against the demonstration plan set, resolving to a sample of 50 accepted at 3 major / 5 minor, with the criterion shown on the run and the earlier hand-typed run shown honestly as «Пороги заданы вручную» |
| 2026-09-21 | `feat/inline-quality-control` | **Дефект ловится там, где он сделан.** Production ran through six ordered milestones, each completed with a free-text note, and then Final Quality inspected a sample at the very end. Everything in between was invisible: a fault introduced at cutting was discovered after packing, when the whole lot already carried it. Two things are added, deliberately two. **`defect_types`** — a brand's catalogue of faults, because a defect code was free text and two inspectors writing `SEAM-OPEN` and `OPEN-SEAM` produce two faults that can never be counted together, so no scorecard, no Pareto and no corrective action could ever be built on them. **`inline_quality_checks`** — a check performed *at* a milestone, against that catalogue. The checks hang on the milestones production execution already owns; a parallel list of operations here would have given the project two answers to «на каком этапе эта партия» and guaranteed they drift apart, so `PRODUCTION_MILESTONE_CODES` is imported and a test holds the migration's stage list against it. What makes it a control rather than a diary: **a milestone whose check found defects cannot be completed until they are dispositioned** — reworked, scrapped, or accepted with a written reason, and only `accepted` needs one, because shipping known defects onward is a decision somebody has to own. That gate lives in the domain *and* in a trigger on `production_executions`, because it crosses two aggregates and the module that happens to be writing is not the only writer; the integration test proves it holds against a direct `UPDATE` that goes around the module. **Severity belongs to the catalogue, not to the occurrence**: the line copies it from the registered fault at the moment of recording, so one code cannot be major in one check and minor in the next — and the same rule now binds Final Quality, which reads the same catalogue and refuses a run whose defect severity disagrees with it. A code the brand has not registered is still accepted there, because a brand that has not yet built a catalogue must keep inspecting. **The defective count is derived from the lines by trigger, never asserted beside them**: a total stated separately from what it totals can disagree with itself, and this one decides whether a milestone may close. Types are **retired, never deleted** — deleting one would leave every check that recorded it pointing at nothing, and those checks are the record of why a lot was reworked. New surface: `GET/POST /v2/defect-types`, `POST /v2/defect-types/{code}/retire`, `GET/POST /v2/production-executions/{code}/inline-quality-checks`, `POST /v2/inline-quality-checks/{id}/disposition`; the listing carries its own summary and Pareto so that a defect rate computed three ways in three screens is not three numbers with one name. The demonstration catalogue **adopts the three codes already present in recorded inspections** with the severities those inspections used, so registering it explains the existing records rather than contradicting them. Known gap, stated rather than papered over: the demonstration database has no lot in production — all eight executions were walked to `ready-for-qc` and there is no spare demand — so the record form has nothing to attach to there; the full path is proven end to end against PostgreSQL in `tests/zx-postgres-tech-pack-allocation.test.mjs`. | 6.4, 11, 15, 17, 20 | IMPLEMENTED; `npm run verify` green including PostgreSQL (1410/0/0 skipped); the catalogue, the check, the derived count, the gate (through the module and against a direct SQL writer) and the disposition exercised end to end on PostgreSQL; catalogue and check-listing endpoints and the execution-screen panel confirmed live |
| 2026-09-21 | `feat/supplier-inline-scorecard` | **Карточку поставщика было не посмотреть.** The scorecard was already computed in full — orders placed, on-time delivery to QC, first-pass yield, rework incidence, rejection rate, defect counts and failure economics by currency, all measured from data the platform itself recorded — and **had no screen anywhere**. Rather than build a second scorecard beside it, this extends the one that exists and gives it a place to be read: beside the supplier in the sourcing inspector, for the same reason portal access lives there, because it is a fact about this counterparty. What is newly computable is **whether the factory finds its own faults**. Measuring a factory only at the final gate says whether problems arrived and nothing about whether it caught them: one supplier inspects at the operation, finds a cutting fault on three pieces and recuts them; another presents a clean-looking lot that fails at the gate and costs a rework cycle on the whole batch. Until inline control existed there was nothing to tell them apart with. Migration 111 extends `supplier_operational_performance` with an inline lateral — checks, units checked, defective units, defects by severity, dispositions, lots covered and **open checks**, which is not history but an alarm, since each one is a stage that cannot close. Two deliberate refusals. **No blended score:** an inline defect rate is measured against pieces checked during production and a final-gate count against a sample drawn from a finished lot, so they are reported side by side, each naming its own denominator, rather than averaged into one number that would hide the difference worth looking at. **A share with nothing to divide is «—», not 0 %:** nought per cent claims we looked and found nothing, and we did not look — coverage, by contrast, genuinely is 0 % when no lot was inspected, and says so. The checks are attributed to a supplier through the execution rather than the brand, or one factory's figures would land in another's card. One trap closed in passing: the new fields are asserted rather than defaulted, because a missing column would otherwise report «no inline control at this supplier» — a claim about the factory made from a fault in our own read model. | 6.4, 11, 15, 17, 20 | IMPLEMENTED; `npm run verify` green including PostgreSQL (1412/0/0 skipped); read live for a supplier with real history — two orders, 1600 units, 100 % on time, 100 % first-pass, 0 % inline coverage with the defect rate shown as «—» — and rendered in the supplier inspector |
| 2026-09-21 | `feat/demo-lot-in-production` | **Партия в середине производства.** Every lot in the demonstration had been walked to the end, so the middle of production — the part the platform actually spends months in — was never on screen, inline quality control had nothing to attach to, and the inline half of the supplier scorecard could only show zeros. The seed now takes one awarded RFQ the whole way and **stops it in the middle on purpose**: measurement chart published, tech pack issued and acknowledged by the factory, RFQ allocated, order created and confirmed, execution created and started, then three inline checks — materials inspected and clean, cutting with two panels off grain dispositioned as rework, and assembly with eight findings **left undecided**. The lot sits at assembly with the stage visibly held shut by the rule rather than by a missing button, and the disposition controls have something real to act on. It runs **through the same services a person uses, not through INSERTs**: a seed that writes rows demonstrates the database, a seed that calls the services demonstrates the product — and it fails loudly when a rule it forgot about says no, which happened three times and each time the rule was right. Allocation refused without a supplier-acknowledged tech pack; issue refused without an approved pre-production sample (there was one) and then without a published measurement chart (there was not, so the seed publishes it, through the service). Two display corrections found by reading the result. **Веха и этап — не одно и то же слово:** a milestone is named after the event that ends it, «Раскрой завершён», which is right in the timeline where the reader looks at what has been signed off — but an inline check happens *during* that work, so the same label read as «проверка на этапе „Раскрой завершён“», saying the check came after the stage it belongs to; stages now have names of their own. And a share is shown to one decimal, because the contract's four are computation precision while «5,5556 %» on screen says nothing beyond «5,6 %». The seed is idempotent: a second run added no rows. | 6.4, 11, 15, 17, 20 | IMPLEMENTED; `npm run verify` green including PostgreSQL (1412/0/0 skipped); seeded and re-seeded without duplication; live — the gate refuses to close assembly and names the blocking check, the execution screen shows all three checks with their defect lines, Pareto by origin stage and live disposition controls, and SUP-ATM-R3's scorecard reads 100 % inline coverage, 5,6 % defect rate in production, one rework |
| 2026-09-21 | `feat/supplier-payment-milestones` | **Платёжные вехи: что мы должны фабрике и когда.** The platform knew what a lot costs — the confirmed order carries a frozen commercial snapshot — knew the supplier's terms, and knew the moment goods were released. It could not answer the question a finance department asks every morning. Checked first that nothing already did: the money contour here is **cost accounting** (`actual_cost_ledger_entries`, landed cost, cost close) — what a lot *cost us* — which is a different question from what we *owe*, so nothing was duplicated, and no new capability was added either: `COST_MANAGE` writes and `MARGIN_READ` reads. Two decisions shape it. **Ничего не набирается руками:** amount and currency come from the order's own snapshot, the term in days from the supplier as they stood; a schedule that restates the order's money is a second copy, and two copies of one number disagree the moment either changes. **Наступление срока не хранится, а вычисляется:** a milestone falls due when its trigger happened — the factory confirmed, or Final Quality released — and the date is that event plus the term. There is no `due_at` column and no status column, and a test asserts there is not, because a stored copy of a derived fact drifts: a lot rejected after its balance was marked due would leave money owed for goods that never shipped. It shows on real data without anyone marking anything — released lots read «причитается», the lot still in production reads «не наступил» with no date at all, since offering one invites paying against it. Only the payment is stored, because only the payment is a new fact. The split is the brand's (30/70 is ordinary, 100 % on release happens, thirds happen) so it is data; what is not negotiable is that the parts add up — shares total exactly 10 000 basis points and amounts total exactly the order, **with the rounding remainder carried on the last milestone**, because a fraction lost every order is a supplier underpaid forever. Both sums are enforced by a `DEFERRABLE INITIALLY DEFERRED` constraint trigger, since a rule about a sum cannot be checked one row at a time, and a second trigger refuses a payment whose event has not happened or that predates it — money out for goods that never shipped is the one mistake here that editing a row afterwards does not undo. | 6.5, 11, 15, 17, 20 | IMPLEMENTED; `npm run verify` green including PostgreSQL (1417/0/0 skipped); the whole chain — schedule drawn from a confirmed order, both sum rules refusing a writer that goes around the module, a back-dated balance refused, deposit paid — exercised end to end on PostgreSQL; seeded nine schedules and walked a balance payment through the browser, after which the order read 21 250,00 € paid and nothing due |
| 2026-09-21 | `feat/material-lot-traceability` | **Из какого рулона сшита эта партия.** Checked first what the existing inventory contour covers: `inventory_movement_ledger_entries` is **finished goods** — SKU, warehouse, shop, receipts against a supply commitment — a different branch entirely, so nothing is duplicated. The platform knew a material and knew how much of it a garment takes, because the bill already states consumption and waste; it could not say **which roll**. Two questions depend on that and have no other answer: **разнооттеночность** — fabric is dyed in batches, a lot regularly needs more than one, each roll passes its own incoming inspection, and the fault is in the *pairing*, which only a record of what went where can see; and **отзыв** — when a mill reports a fault in a dye batch, «which production orders carry it» is otherwise «probably all of them». Two tables: a lot is a received batch of one material, an issue is that lot going into one execution. Everything else is derived, consistently with the two previous increments: `issued_quantity` is maintained by trigger from the issues rather than asserted beside them, and **«израсходован» is not a status** — it is received minus issued, and a stored copy of that comparison would drift the first time an issue was corrected. The statuses are only the ones somebody decides: quarantine on arrival, released after incoming inspection, rejected. Материал в карантине в производство не уходит — held in the domain *and* in a trigger, proven against a writer that inserts an issue directly. A lot **already in garments cannot be rejected**: that is no longer a status but a claim against the mill and a decision about goods already cut, and letting a status stand in for it would quietly detach the record from the clothes. The requirement comes from the published bill rather than being restated, and two findings are computed at read time — недостача and mixed dye lots. Rights are the existing ones, split by who actually does the work: `INVENTORY_MANAGE` receives and issues, `QUALITY_MANAGE` releases from quarantine, quarantines and rejects, because the verdict «годится» is not taken by whoever signed for the delivery. One extension rather than a parallel structure: `inline_quality_defects` gains an optional `material_lot_id`, so a materials-stage defect can name the roll — which is what turns «нашли разнооттеночность» into a sentence somebody can act on. Both directions are on screen: what is in this lot of garments (on the execution, placed **above** inline control because a roll arrives before anything is checked on it), and where a roll went (in the material's own inspector, which is the recall answer). | 6.4, 11, 15, 17, 20 | IMPLEMENTED; `npm run verify` green including PostgreSQL (1423/0/0 skipped); the whole path — receive, refuse to issue from quarantine (through the module *and* against a direct INSERT), release, issue, derived count matching column and payload, refuse to over-issue, refuse to reject what is already in garments, two dye lots detected — exercised end to end on PostgreSQL; seeded three rolls and read live: 898,8 m required by the bill, 900 m issued from two dye lots, warning raised, and the material card listing all three rolls with where each went |
| 2026-09-21 | `feat/cutting-spreads` | **Раскрой: настил, карта раскроя и выход из полотна.** Traceability reaches the roll and the bill states consumption per garment; between them lies the one place where cloth becomes panels and that figure is either confirmed or not. Three numbers, **none of them stored**: настелено = длина × слои, раскроено = слои × изделий в слое, расход на изделие = длина ÷ изделий в слое. The last is independent of the number of plies — lay twice as much and you get twice the garments from twice the cloth — and **that independence is what makes it comparable with the bill**, which is the whole point. A marker is written against production executions rather than sizes, because in this schema a SKU *is* one colour and one size, so an execution is a size; one spread therefore serves several lots at once, which is exactly how sizes are nested to tighten a marker. Four rules, each in the domain and in the database. **Снято с рулонов = настелено** — not a reconciliation but an identity: cloth comes from nowhere and vanishes nowhere; held by a `DEFERRABLE INITIALLY DEFERRED` constraint trigger because it is a rule about a sum. **Настил делается только из рулонов, выданных в те партии, которые он раскраивает** — otherwise the record of what a lot is made from and the record of what it was cut from disagree, and yesterday's traceability stops meaning anything; proven against a direct INSERT. **Раскладка не бывает пустой** — a spread with nothing in its marker is spoiled cloth. And, added as migration 115 because migrations only go forward, **с рулона нельзя настелить больше, чем в партию выдано**: without it three 400-metre spreads off a 600-metre roll would record twelve hundred metres, and a cancelled spread is excluded because it did not happen. Отменённый настил не считается ни в ткани, ни в изделиях; раскроенный настил отменить нельзя — детали уже вырезаны. The summary puts the bill's norm and the actual side by side and names the direction: перерасход or экономия. Three display corrections found by reading the result: «Экономия −0,047» said the same thing twice, so the magnitude now goes without a sign where the word already carries the direction; three decimals on a percentage is noise; and the norm 2,247 rounded to 2,25, erasing precisely the third decimal the comparison exists for. | 6.4, 11, 15, 17, 20 | IMPLEMENTED; `npm run verify` green including PostgreSQL (1430/0/0 skipped); the whole path exercised end to end on PostgreSQL — spread laid, cloth balance refused when broken, an unissued roll refused both in the module and against a direct INSERT, an empty marker refused, spread marked cut and then refused cancellation; seeded a spread on the lot in production and read live: 440 m in 200 plies, 200 garments, 2,2 m per garment against the bill's 2,247 — economy of 0,047 m (2,1 %) |
| 2026-09-21 | `feat/operation-sequences` | **Технологическая последовательность (BOL).** Three things were waiting on one missing object. The printed tech pack promises a «Технологическая последовательность» section and had nothing to show. Inline control could say «нашли на пошиве» but not «нашли на притачивании воротника» — and between those two sentences lies the whole difference between «что-то не так» and «знаем, где чинить». The construction-node dictionary had existed from the start, attached to nothing. All three are the same missing thing: **an ordered list of a product's operations**. **Одна форма, два владельца:** a category template and a product's own sequence are identical in shape — same operations, same order, same standard times — so they share one table, with CHECKs making the owner exactly one and never both. Describing them twice would have produced two descriptions that diverge on the first edit. A product sequence takes the template's operations **as a copy, not a reference**: editing the template next season must not change what is already being sewn by. **Этапы взяты у производства, узлы — у справочника**, not re-declared: `PRODUCTION_MILESTONE_CODES` is imported and a test holds the migration's list against it, and a construction node must exist in the MDM library or «воротник» and «воротник втачной» become two different nodes and nothing aggregates. **Номер операции — её место в списке**, assigned on write, so a hole in the numbering is impossible by construction rather than by validation; the database still refuses one with a deferred trigger, because a printed sequence reading «12, 14, 15» sends a machinist looking for the thirteenth. **Трудоёмкость не хранится** — it is the sum of the standard times, split by stage in production order. **Стоимости труда здесь нет вовсе**: it needs a rate, a rate is a separate agreement with the factory, and inventing one would print a number in the tech pack that nobody agreed. The payoff for quality: an inline check may now name its operation, and the operation must belong to that product and that stage — otherwise «нашли на пошиве при упаковке» would be recordable, and an operation of another product could land in this lot's check; held in the domain and in a trigger, proven against a direct UPDATE. The form offers only the current stage's operations, with «Этап целиком» first, because checking a whole stage remains legitimate. One wording correction: «Раскрой: 2 × 10,5 мин» read as «twice ten and a half» when it means two operations totalling that. | 6.2, 6.4, 11, 15, 17, 20 | IMPLEMENTED; `npm run verify` green including PostgreSQL (1437/0/0 skipped); the whole path exercised end to end on PostgreSQL — template, operations, publish, product copy, a second sequence refused, workload summed, a numbering hole refused by the database, a check naming its operation, and a wrong-stage operation refused both in the module and against a direct UPDATE; seeded a ten-operation sequence and read live: 49,3 min per garment across four stages, shown on the tech pack and offered in the inline-check form |
| 2026-09-21 | `feat/target-pricing` | **Целевое ценообразование: сколько нам можно потратить.** The platform could always answer what a garment cost — the bill gives materials, cutting the actual yield, payment milestones what we owe, the cost ledger what was spent. It had never once been asked the **reverse**: what price at the factory still squares with the retail price and the planned markup. The chain is short and entirely derived: розница ÷ наценка = допустимая себестоимость на складе (DDP), and DDP ÷ (коэффициент страны × коэффициент категории) = допустимая цена у ворот фабрики (FOB). **Одно правило добавлено против источника, и оно существенно.** Reproducing Omnidata's own numbers — RRP 600 000 ₽, наценка 2,70, коэффициенты 1,30 и 0,30 — gives their FOB of 7 342,79 USD exactly, but that is 569 801 ₽ against a DDP of 222 222 ₽: **the price at the factory gate comes out higher than the landed cost**, which cannot happen, because freight, duty and handling only add. The coefficients are multipliers along the road from FOB to DDP, so each is required to be **not less than one**, in the domain and in a CHECK — and that rule refuses exactly the configuration the source ships with. Курс берётся из **курсов сезона** — a dated table per campaign, resolved as the latest rate effective on or before the date, with a missing rate **refused rather than guessed** — and is **frozen into the plan together with its date**, as the payment terms are: a target that quietly follows the rate is useless in a negotiation, because it cannot be cited tomorrow. A trigger requires the plan's rate to match a season row that really existed, so «курс 92,1» is checkable rather than taken on trust. Neither target figure is stored: both are quotients of already-recorded numbers. What makes this ours rather than a copy: **the source only shows the target, while we hold the actual** — the quoted FOB comes from the confirmed production order and the material cost from the published bill, read in one snapshot, so the screen says «цель 84,73 €, фабрика запросила 52,00 €, укладываемся, и сверх материалов остаётся 41,15 €». A quote in another currency is **not converted by a second rate**: that would compare against a number nobody named. Cost Breakdown deliberately **not** copied — a hand-typed cost sheet beside the bill of materials is a second opinion about one number, and the bill already computes it. | 6.5, 7.1, 11, 15, 17, 20 | IMPLEMENTED; `npm run verify` green including PostgreSQL (1443/0/0 skipped); exercised end to end on PostgreSQL — a duplicate season rate refused, a plan before any rate refused, the source's own 1,30 × 0,30 refused, the frozen rate and date proven against a direct UPDATE in both directions; seeded and read live: 24 900 ₽ ÷ 2,6 → 9 576,92 ₽ landed ÷ 1,2272 ÷ 92,1 → 84,73 € target FOB against 52,00 € quoted |
| 2026-09-21 | `feat/season-economics` | **Плановая экономика плейсхолдера: план, цель и факт об одной единице.** Три документа уже отвечали на вопрос «сколько стоит вещь», каждый со своей стороны — слот линейного плана (что заложил планировщик), целевая цена (сколько нам можно заплатить) и подтверждённый заказ (сколько мы заплатили) — и **ни один запрос не клал их рядом**, отчего маржа сезона ниоткуда не бралась. **Строилось не с нуля: плейсхолдеры, связь со стилями и сверка плана с фактом уже существовали** (миграции 078–079), поэтому работа была достроить, а не завести второе. Существующая сверка меряет факт **себестоимостью по ведомости** — во что нам обходится пошив; маржа сезона считается от другого числа — **от цены, о которой договорились**. Новое здесь ровно одно вычисление: **котировка FOB поднимается до ввезённой себестоимости в розничной валюте тем же курсом и теми же коэффициентами, которыми считается цель** — цель умела считаться сверху вниз, факт снизу вверх не поднимался никогда; посчитать его другой линейкой значило бы показать расхождение, которого в деньгах нет. Миграция 118 добавляет представление `placeholder_realisation_workspace` **рядом с существующим, а не вместо него**: `assortment_plan_workspace` уже читается клиентом. Представление — только соединение; приведение валют, взвешивание и отказы живут в домене, потому что правило, записанное дважды, расходится. **Наценка выводится, а не хранится** — вопреки источнику, где она отдельное поле: она частное розничной цены и себестоимости, а четвёртое число рядом с тремя связанными разошлось бы при первой правке. **Одно правило я сначала написал слишком строгим и исправил до внедрения:** свод отказывался объявлять факт, пока не подтверждён каждый слот — но сезон в работе не бывает подтверждён целиком, и поле пустовало бы ровно тогда, когда нужно; хуже, частичную себестоимость я собирался делить на выручку **всего** сезона, что занизило бы затраты. Факт считается по подтверждённым слотам и сравнивается с планом **по тем же слотам**, рядом с долей охвата. Сезон **отказывается складывать разные розничные валюты**, а целевой план в валюте, отличной от валюты слота, отказывается сводиться: два документа назначили вещи разную цену на полке, и какой верен — решает человек. Найдено по дороге и исправлено: **демо-сид вовсе не заводил плейсхолдеров**, поэтому экран линейного плана на свежей базе был пуст, а связей «слот ↔ стиль» не было ни одной — сверка плана с фактом стояла тёмной с самой постройки. | 5.3, 6.5, 7.1, 11, 15, 17, 20 | IMPLEMENTED; `npm run verify` green including PostgreSQL (1455/0/0 skipped, +12); PostgreSQL-тест доказывает, что база держит плановую маржу связанной с ценой и себестоимостью при записи мимо модуля в обе стороны; прочитано живьём — жакет закрыт целиком (цель 9 576,92 ₽ против факта 5 877,31 ₽, маржа 76,4 % против плановых 63,0 %), футболка нацелена без закупки, платье только запланировано, сезон: выручка 59 700 000 ₽, план 63,8 %, закуплено 50,1 % выручки, отклонение +13,4 п.п.
| 2026-09-21 | `feat/material-specification` | **Материал как измеримая вещь, а не как строка названия.** О полотне было известно то, что кто-то напечатал: плотность жила **внутри имени** («Recycled ripstop shell 60gsm») и потому не искалась и не проверялась, состав был одной строкой «100% recycled polyester», ширины раскроя не было вовсе. **Несущая находка:** у настила ширина записывалась всегда, но сверять её было не с чем — и в демонстрации стояло `fabric_width = 150` при `unit = 'm'`, то есть «полотно шириной 150 метров». Ширина брала единицу от единицы расхода материала, хотя это разные величины. Миграция 119 даёт ширине **свою** единицу, заводит ширину раскроя у материала и триггер `assert_spread_fits_cloth()`, приводящий обе к миллиметрам; то же правило стоит в домене раскроя. Существующие строки **не переписываются и не обнуляются**: парность ширины и единицы требуется через `NOT VALID`, потому что записанная ширина — настоящее измерение, пусть и без единицы, а стереть её значило бы уничтожить данные ради чистоты ограничения. Состав переведён на строки по **governed-справочнику**: в `mdm/catalog` `material.fibre` и `material.fibre_origin_class` были **объявлены, но не инстанцированы**, поэтому добавлен эталонный набор `russia-fashion-fibre-core.json` (13 волокон, 3 класса происхождения, двуязычные) — приватный текстовый словарь дал бы «cotton», «Cotton» и «хлопок» как три разных волокна. Сумма процентов держится **отложенной** проверкой базы (состав правят целиком, промежуточная сумма ни во что не складывается) и в домене считается **целыми тысячными**: «33,333 три раза» честно отвергается — остаток несёт одно из волокон. **Дубль устранён, а не заведён:** старое свободное поле `composition` не удалено, но когда строки есть, текст **выводится из них**, поэтому разойтись им нечем; читатель говорит, что перед вами — `lines` или `legacy-text`. Добавлен коэффициент пересчёта (закупаем в кг, расходуем в метрах) с правилом, что **единица, пересчитанная в себя с коэффициентом ≠ 1, — опечатка**, молча перемасштабирующая каждую строку ведомости. Цена DDP намеренно **не** заведена столбцом: она частное уже записанных чисел, а логистические коэффициенты живут в целевом ценообразовании, и второй их источник разошёлся бы с первым. Физические свойства правятся и после публикации отдельной командой: полезная ширина рулона выясняется, когда рулон приезжает, а посчитанные ведомости от этого не меняются — цена в строке снята снимком. | 6.1, 6.3, 11, 15, 17, 20 | IMPLEMENTED; `npm run verify` green including PostgreSQL (1471/0/0 skipped, +16); оба правила доказаны живыми запросами мимо модуля — настил 1600 мм на полотне 150 см отвергнут триггером, состав в 60 % отвергнут отложенной проверкой на COMMIT; прочитано живьём: состав «95% Хлопок, 5% Эластан» выведен из строк, волокна несут класс происхождения из справочника, 1 kg → 3,2 m
| 2026-09-21 | `fix/double-pill-border` | **Плашка статуса в двойной рамке.** В шапке инспектора вокруг «опубликовано» рисовалась вторая пилюля — та же рамка, тот же радиус 999px, тот же фон. Причина — знакомый класс дефекта: **роли сопоставляются по токенам имени класса**, и `od-inspector-**status**-line` был прочитан как ещё один статус, хотя это флекс-строка, которая статус лишь содержит. Тот же механизм когда-то сделал `.ls9-metric-label` вторым бордюрным тайлом внутри тайла, который он подписывает, и был закрыт правилом `inlineCannotBeBox`; это его пара. Существующее правило «роль не вкладывается сама в себя» здесь не помогает: оно снимает роль с **внутреннего** элемента, что верно для контейнеров (инспектор внутри инспектора), а у листовой отделки лишний **внешний**. Добавлено `heuristicPillWrapsElements`: **эвристическое** совпадение на `status` снимается, когда внутри есть элементы, потому что пилюля обводит текст, а не разметку. Правило узкое намеренно — плашки из явного списка (`.badge` и родня) не затрагиваются даже с разметкой внутри: их назначал человек, а не совпадение подстроки. **Диагноз пришлось исправить по дороге:** сначала то же правило было поставлено во **втором** слое ролей (`omnidata-v14-components.js`), но обёртка там вообще не классифицировалась, а правило задевало бы карточки-счётчики `od-status-card`, которые законно содержат точку `od-status-dot`; та правка откачена, осталась одна точная. | 11, 17, 20 | IMPLEMENTED; `npm run verify` green including PostgreSQL (1471/0/0 skipped); проверено не на глаз: написан геометрический детектор «рамка внутри рамки, совпадающая по обеим сторонам» и прогнан по всем 12 разделам с открытым инспектором и перебором его вкладок — до правки находил обёртку статуса, после правки чисто везде; карточки-счётчики сохранили пилюлю
| 2026-09-21 | `feat/material-colours-and-lab-dips` | **Цвет материала и его утверждение перед закупкой.** У полотна был один цвет строкой — «Midnight Navy»; это не цвет, а подпись к нему, по ней нельзя ни найти, ни сверить, ни сказать, утверждён ли оттенок к производству. У партии материала цвета не было **вовсе** — только номер крашения `dye_lot`, который ни на что не ссылается. Добавлены палитра полотна по governed-справочнику `colour.colour` (его записи уже несли Pantone, hex, семейство — то есть **пробел 24 каталога пробелом не был**, справочник богаче скрина Omnidata) и **лабораторные образцы** с семью состояниями источника, сведёнными в машину состояний: запрошен → прислан → решение, где «отклонён, пересдача» возвращает образец на присылку **следующим раундом**, а «отклонён окончательно», «утверждён», «утверждён условно» и «отменён» окончательны. **Раунд — не украшение:** он говорит, сколько раз фабрика не попала в цвет, и это тот же факт о поставщике, что и срыв срока, поэтому рядом считается доля принятых с первого раза. Три правила держит база: решение выносится только по **присланному** образцу; условное утверждение несёт условие, а отказ — причину (иначе статус ничего не сообщает приёмке, а читает его именно она); и **действующий эталон в цвете ровно один** — проверкой непересечения сроков, а не уникальным индексом, потому что «одно утверждение на цвет навсегда» неверно: сезон сменился — красят по новому. **Цепочка замкнута:** партия не выпускается из карантина в цвете без действующего утверждённого образца — перекрасить принятую партию невозможно, её можно только не принять. Партия без названного цвета проходит: фурнитура и уже принятые записи цвета не несут. Срок действия задан датами, а не ссылкой на сезон: партия своей кампании не знает, и до неё пришлось бы идти через исполнение, заказ, SKU и коллекцию — проверка при выпуске зависела бы от половины схемы. Запрета «решает не тот, кто прислал» намеренно нет: присылает фабрика, а фиксирует её присылку человек бренда, и в маленькой команде это один человек — записаны оба, что оставляет это видимым, не останавливая работу. **Попутно исправлен отдельный дефект сида:** отозванную связь с байером он пытался принять и падал, из-за чего демонстрация переставала засеваться навсегда после одного отзыва; теперь связь сначала запрашивается заново. | 6.1, 6.3, 6.6, 11, 15, 17, 20 | IMPLEMENTED; `npm run verify` green including PostgreSQL (1483/0/0 skipped, +12); четыре правила базы доказаны запросами мимо модуля (решение без присылки, условное без условия, пересечение сроков, выпуск в неутверждённом цвете); цепочка пройдена целиком через HTTP на живом рантайме — цвет не из палитры отвергнут, приёмка в утверждённом цвете прошла в карантин, выпуск записал эталон `LD-MATSHELLR3-MIDNIGHTNAVY`, а партия в цвете с нерешённым образцом отвергнута; экран прочитан живьём: «Утверждён · 1 раунд» против «Прислан, ждёт решения», предупреждение называет непринятый цвет
| 2026-09-21 | `fix/audit-first-tier` | **Первая очередь по аудиту: то, что течёт, обманывает или ломает экран.** (1) **Внутренний склад не уходит контрагенту.** Магазин, связанный с брендом коммерческим циклом, намеренно видит опубликованные позиции его коллекции — это витрина обнаружения, и тесты закрепляют её как решение, — но вместе со строкой уходили `availableQuantity`, `reservedQuantity` и `availableToSell`. **Резерв — это чужой спрос:** он складывается из обязательств перед другими покупателями, и по нему читается, кто и сколько у бренда забрал. Сначала я снёс саму видимость и получил пять красных тестов; они и показали, что ломаю чужой замысел. Откатил и сделал узко: строки видны как прежде, внутренние складские поля проецируются прочь для всех, кроме владельца. (2) **Материал, которого нет в ведомости изделия, в производство не уходит** — проверялись статус партии, бренд, активность исполнения, остаток и дата, но не принадлежность материала изделию; ведомость читается тем же снимком, а при её отсутствии выдача проходит, потому что судить не по чему. (3) **Заведены роли `production` и `quality`**, а у `sales` отобрано право подписывать качество: тот, кто продаёт партию, не решает, годна ли она. Тесты финального контроля гоняли инспекцию **от имени продаж** и тем закрепляли отсутствие разделения обязанностей — переведены на роль качества, кроме одной проверки отказа по правам, где актёр без подписи нужен по существу. (4) **Мой дефект: целевая цена и маржа сезона считались без постоянной части цены закупки** — фабрика берёт цену за штуку плюс оснастку и приладку, и на живом заказе это 5312,5 против 5200. Итог снимка уже содержит обе части, поэтому делится он. Мой же сквозной тест закреплял занижение. (5) **Мой дефект: слот без плановой себестоимости давал сезону выручку с нулевыми затратами** — теперь в деньги не входит вовсе, а разница `slotCount` и `quantifiedSlotCount` сама говорит, что сезон посчитан не целиком. (6) Четыре экранных дефекта: рекурсия, съедавшая два действия с образцами (локальная функция затеняла глобальную и звала саму себя); белый экран в `odPage` ровно на пути ошибки; `ReferenceError` в кнопке «Обновить»; публикация ведомости и обмерной таблицы **без перехвата ошибок**. (7) Сид теперь **называет расхождение роли** вместо «уже admin» — на этой тишине демонстрация показывала инспектора с правами администратора. | 6.1, 6.4, 7.4, 11, 15, 17, 20 | IMPLEMENTED; `npm run verify` green including PostgreSQL (1501/0/0 skipped); утечка проверена живьём до и после: у байера в сыром каталоге склад скрыт, у бренда свой виден; витрина байера отвечает прежним содержимым и хешем; замок на ведомость и разделение обязанностей закреплены тестами. **Не сделано намеренно:** два перечня операций не слиты — это не дубль, а два несвязанных понятия, и слияние снесло бы правило класса оборудования; смена роли через API не реализована, поэтому демо-аккаунты остаются на `admin` до пересева.
| 2026-09-21 | `feat/bom-size-line` | **Размерный ряд ведомости и выход полотна.** Разведка сузила блок G сильнее, чем говорил каталог: **группировка по типу со счётчиком уже была сделана целиком** (`lineGroups` рисует «Ткани · 1» ровно как на скрине), `is_main` и `placement` на строке — тоже, как и `waste_percent`, `gross_quantity`, построчная валюта и курс. Настоящим пробелом был **ряд**: спецификация ведётся на каталожный SKU, то есть на цвет и размер сразу — это верно по сути, расход на 48-й больше, чем на 40-й, и одна цифра на все размеры означала бы закупку либо с запасом, либо с недостачей, — но автор заполняет пять отдельных ведомостей и **не может сравнить их между собой**, а опечатку показывает именно сравнение. Представление отдаёт по плоской строке на (размер, материал), сетку собирает домен: пивот в SQL пришлось бы переписывать при каждом новом столбце, а правило «чем больше размер, тем больше расход» в запросе не выражается вовсе. Порядок берётся из **размерной шкалы бренда** — она и есть решение о том, как идёт ростовка, — а не из общего справочника, где рядом лежат буквенные и обувные системы. Сетка называет три расхождения и **ни одному не отказывает**: размер без опубликованной ведомости, материал, пропавший в размере, и расход, не растущий с размером. Отказ уместен там, где запись бессмысленна; здесь она осмысленна, но подозрительна — раскладка на разных размерах ложится по-разному, и запрет закрыл бы законный случай. Пропуск в середине ряда объявляется **один раз**: «материал пропал» и «расход упал» про один и тот же пропуск были бы сообщением об одной ошибке дважды. **Выход полотна выводится, а не хранится:** он уже задан процентом отходов, и 7 % дают 93,46 %, а не 93 %. **Ошибка, пойманная до внедрения:** общий выход по ведомости я сначала считал суммой нетто и брутто по всем строкам — то есть складывал метры ткани со штуками фурнитуры; число ни к чему не относилось. Взвешен деньгами: стоимость общая для всех строк, и дорогая ткань влияет на итог сильнее дешёвой подкладки. Cost Breakdown источника намеренно **не** скопирован — ручная калькуляция рядом с ведомостью есть второе мнение об одном числе, которое ведомость уже считает. | 6.2, 6.3, 11, 15, 17, 20 | IMPLEMENTED; `npm run verify` green including PostgreSQL (1495/0/0 skipped, +12); конечная точка пройдена на живом рантайме — сетка собирается, выход 93,46 % выведен из 7 % отходов, `graded: false` честно сообщает, что по одному размеру градацию не показать; экраны прочитаны живьём, двойных рамок нет. **Открытым осталось:** демо односоставно по размеру — существующий размер «M» заведён с `sort_order = 0`, поэтому добавление S=200 и L=400 отрисовало бы ряд как «M, S, L», а операции изменить порядок у заведённого значения нет; правильная ростовка требует новой размерной шкалы и переноса SKU, на которых висят опубликованные каталог, прайс-лист и заказ. Это отдельный слайс, а не правка.
| 2026-09-21 | `feat/registry-view-and-hierarchy` | **Реестр как инструмент чтения: Freeze Line и структура иерархии.** Разведка по пяти снимкам сузила блок: **фильтр по любому атрибуту был сделан целиком** — `odFilterPanel` с поиском «Найти атрибут», группами значений и счётчиками по каждому, и проверен живьём до того, как писать хоть строку. Не было двух вещей. **Первая — Freeze Line.** Видимость колонок была, порядка и закрепления — нет, а реестр моделей несёт двадцать колонок: прокрутив до «Технолога», человек видит значения и не видит, чьи они. Граница хранится **числом, а не списком**: закрепить можно только первые колонки подряд — закреплённая посреди прокручиваемых оставляла бы разрыв в строке. Смещения заданы **таблицей стилей, а не скриптом**: первый черновик считал их через `cell.style.left`, что запрещено границей ODS — и запрет верный: оформление, разложенное между CSS и обработчиком, расходится при первой правке. По той же причине селектор `:has()` заменён классом `od-table-frozen`, выставляемым из рантайма: запрещена стилизация, не разметка. Кнопка переименована с «Колонки»: панель делает три работы, и подпись по одной из них обещала меньше, чем там есть. **Вторая — навигатор по иерархии**, его не было вовсе. Фильтр отвечает на «покажи только это»; иерархия — на другой вопрос: **из чего вообще состоит раздел**. Уровни собираются из тех же атрибутов, что и фильтр — второй список «по чему можно группировать» разошёлся бы с первым в тот же день. **Дерево строится без учёта открытой ветви**, и это главное решение: если считать его по уже суженным строкам, соседние ветви исчезнут после первого же выбора и из ветви некуда будет вернуться. Остальные сужения — поиск, статус, выбранные значения — дерево учитывает, иначе его числа не сошлись бы с тем, что человек увидит в таблице, если перейдёт. Строка без значения — тоже ветвь («—»): пропустить её значило бы, что сумма ветвей не сошлась бы с «Всего экземпляров», а часть строк стала бы недостижима обходом. **Уровни переживают визит, открытая ветвь — нет**: вернуться в раздел и увидеть его пустым из-за выбора, сделанного месяц назад, — худший из возможных приёмов; по той же причине пересборка уровней сбрасывает ветвь — в другом дереве прежняя ничего не значит. Открытая ветвь видна и снимается **снаружи панели**, как и выбранные значения фильтра: пустой реестр без видимой причины — тот же дефект. **Два дефекта найдены измерением, а не глазом:** кнопки порядка обеих панелей заявлены 22×22, а считались 7,5×34 — ODS задаёт кнопке `min-height` и `width:auto` селекторами по ролевым атрибутам, и один класс им проигрывает; при этом «×» опознаётся эвристикой как `icon-button`, а не `button`, и первая же правка его пропустила. Три панели теперь взаимно исключающие: открытые разом они оставляли от реестра полосу. | 11, 17, 20 | IMPLEMENTED; `npm run verify` green including PostgreSQL; оба губернатора границ пройдены (`validate-ods-boundaries`, `validate-design-system`); пройдено живьём в браузере на реестре моделей: закреплённые колонки держат позицию при `scrollLeft = 500`, а незакреплённые уезжают; два уровня иерархии собраны и переставлены, выбор ветви сузил таблицу 14 → 1 и «Всего экземпляров» с ней же, при этом соседняя ветвь осталась видна со своими 13; уровни пережили перезагрузку, ветвь — нет. **Не сделано намеренно:** перетаскивание мышью — порядок меняется кнопками, потому что перетаскивание недоступно с клавиатуры, а реестром пользуются и те, кто мышью не работает. |
| 2026-09-21 | `fix/editor-dialogs-and-grade-carryover` | **Вторая очередь по аудиту: что теряется и что молчит в диалогах редакторов.** (1) **Ревизия обмерной таблицы стирала правило градации.** Посылка PATCH собиралась из точек без `gradeSteps`, домен читал отсутствие поля как «правила нет» — и градуированная таблица после первой же ревизии становилась набранной вручную. **Потерю не видно глазом**: числа те же, их шлёт та же посылка; исчезает только межразмерная разница, из которой они выведены, а вместе с ней — пометки исключений, по которым фабрика видит, где человек отступил от правила. Правило переносится вместе со всеми значениями, и это **воспроизводит таблицу точно**: домен помечает значение, равное выведенному, как `derived`, а отличающееся — как `override`, поэтому исключения остаются исключениями, а не становятся новой нормой. (2) **Слушатель клавиатуры утекал с каждым открытием редактора.** Диалоги ведомости и обмерной таблицы написаны руками, поэтому Escape вешается на `document` — а снимал себя только он сам. Крестик, «Отмена», щелчок мимо и успешное сохранение оставляли его навсегда вместе со ссылкой на оторванный диалог. Закрытие сведено в одну `closeEditor()`. (3) **Проверка формы бросалась до try-блока**, то есть в асинхронном обработчике события: дублирующиеся коды размеров и отсутствие базового размера уходили в необработанное отклонение, и кнопка «Сохранить» просто не делала ничего. Сообщение показывается там же, где отказы сервера. (4) **Отказ при открытии редактора терялся в трёх местах** — загрузка справочника материалов и каталога может отказать, а `onclick` возвращённый промис не ждёт. (5) **Находка аудита A9 снята проверкой, а не исправлена.** «`omnidata-fidelity.css` не подключён» — это решение, закреплённое с двух сторон: `omnidata-fidelity-contract` требует отсутствия ссылки и называет файл «retired reference asset», а `validate-ods-boundaries` отказал бы в подключении — таблицы вне замороженного перечня из 21 запрещены. «Исправление» уронило бы governed-валидатор; документ аудита исправлен. **Попутно сломал и починил чужой тест:** `bom-ui-pagination` вырезает тело загрузчика по якорю «сразу за ним идёт `openEditor`», и новая функция между ними его уронила. Перенесёна обёртка, а не ослаблен тест. | 6.1, 11, 17, 20 | IMPLEMENTED; `npm run verify` green including PostgreSQL; проверено живьём: счётчик слушателей `keydown` ходит 0 → 1 → 0 на трёх подряд открытиях ведомости и двух обмерной таблицы (раньше рос 1, 2, 3); дублирующийся код размера теперь даёт сообщение в диалоге и ни одного необработанного отклонения; перенос градации и её потеря закреплены двумя тестами домена, снятие слушателя — тестом на оба модуля. |
| 2026-09-22 | `fix/outbox-queue-visibility` | **Аудит A4: очередь не молчит.** Аудит нашёл 1067 событий в `pending`, старшему 34 дня. **Само по себе это не дефект**: публикатор создаётся только при заданном адресе вебхука, и работа без внешнего потребителя событий — законная настройка, решать за эксплуатацию платформа не вправе, а выдуманный адрес привёл бы только к тому, что тысяча событий ушла бы в dead-letter. **Дефект в другом: система не видела собственного остатка.** `/ready` отвечал 200 и не упоминал очередь вовсе, потому что проверяет живость **зарегистрированных** работников, а этот при отсутствии адреса не регистрировался; журнал молчал; уборщик удаляет только доставленные и похороненные (и правильно делает), поэтому ждущие — единственное, что растёт без предела. **У таблицы не было ни одной своей отметки времени, кроме `published_at`**, который заполняется после доставки: возраст неотправленного события читался только разбором `event->>'occurredAt'`. Миграция 124 добавляет `queued_at` с частичным индексом по ждущим. **Засев точный, а не `now()`**: у всех 1067 строк `occurredAt` есть, и это то же время — outbox пишется в одной транзакции с самой записью; `now()` объявил бы месячную очередь новорождённой и стёр бы ровно тот факт, ради которого колонка заводится. **Хранилище очереди теперь создаётся всегда**, а служба публикации — по-прежнему только при подписчике: раньше вместе со службой исчезала и возможность посмотреть на очередь. На старте и на каждом цикле обслуживания снимается остаток, и проверка готовности сообщает его вместе с возрастом самого снимка — устаревшие цифры должны быть видны как устаревшие. Статус остаётся `ready`: **отчёт, а не приговор**. **Попутно найдено и исправлено:** отбор событий на публикацию сортировался по **тексту из jsonb**. Строковое сравнение ISO-меток совпадает с хронологическим только пока формат один, а в этой таблице уже соседствуют «…Z» и «…+00:00»; смещение, отличное от нуля, сломало бы порядок совсем, и индекс выражение не брало. Оба порядка в этом запросе — отбора и выдачи партии — переведены на `queued_at`. | 11, 15, 17, 20 | IMPLEMENTED; `npm run verify` green including PostgreSQL (1510/0/0 skipped, +5); миграция применена на живой демо-базе и засев проверен запросом: 1067 ждущих, старшему 33 дня 21 час — точно то, что назвал аудит; запущен реальный узел — журнал на старте пишет «1067 event(s) pending and no subscriber is configured… oldest queued 33 day(s) ago», а `/ready` несёт `outbox-publication: {status: ready, publisher: not-configured, pending: 1067, oldestQueuedAt: 2026-08-19}`. |
| 2026-09-22 | `fix/membership-names-an-identity` | **Аудит A10: идентификатор, за которым закреплены роли, нельзя занять заново.** У `memberships.user_id` не было внешнего ключа, и аудит нашёл пять активных членств, включая два `owner`, выданных идентификаторам, для которых пользователей не существует. Войти под ними нельзя — **опасно другое: идентификатор оставался свободен**, и тот, кто позже завёл бы пользователя с таким же идентификатором, молча получил бы владельца чужой организации, не через выдачу прав, а через совпадение строки. **Внешний ключ был написан, применён и снят — проверкой, а не спором.** Он выглядел прямым ответом, и в обоих настоящих путях заведения учётная запись действительно создаётся раньше членства. Но полный прогон показал, что система так не считает: **девятнадцать наборов тестов** заводят членства с самостоятельными идентификаторами актёров, потому что проверяют права и представления, а не личности. Ограничение, которое нарушают девятнадцать фикстур, — это утверждение, которого система не делает; `memberships.user_id` — это **актёр**, и актёром может быть не только тот, кто входит (`system` тоже никогда не был строкой в `auth_users`). Миграция 125 переписана **до фиксации**, а снятое ограничение убрано из локальных баз вместе с его строкой в `schema_migrations`. Правило сформулировано ровно по опасности: триггер `BEFORE INSERT` на `auth_users` отказывает, если за идентификатором уже числятся членства, и называет роли — `AUTH_ACTOR_IDENTIFIER_TAKEN`. Захват прав невозможен, свобода актёра сохранена. Правило перекрёстное, поэтому живёт в базе: домен `auth` о членствах не знает и знать не должен, а писатель мимо модуля всё равно упрётся в триггер — что и проверено вставкой сырым SQL. **Приёмочный бутстрап приведён в порядок и без внешнего ключа**, потому что был единственным местом, где роли выдавались раньше личностей: теперь он заводит личности своих актёров первыми. Трём актёрам, которые никогда не входят, — **отключёнными**, на зарезервированном домене `.invalid`, со случайным паролем, который никуда не возвращается. Для этого `bootstrapUser` принимает `status`, где допустимы только `active` и `disabled`: понятие не новое — колонка и её CHECK уже были в схеме, а вход и проверка сессии и раньше отказывали всем, кроме `active`; не было лишь способа завести такую сразу. **Два приёмочных набора идут по одной базе**, а идентификаторы актёров фиксированы, поэтому учётные данные владельцев сведены в один модуль: разная почта на один идентификатор даёт отказ «identity does not match». Раньше это не всплывало лишь потому, что владельца магазина заводил ровно один набор. Одна фикстура (`zu-postgres-supplier-portal`) сеяла членство раньше личности — порядок исправлен на настоящий. **Демо-база убрана по решению владельца:** две приёмочные организации стояли рядом с «Syntha Brand» и «Nordhaus Retail» и были бы видны на показе. Удалению предшествовала проверка по **всем** текстовым колонкам со ссылками на организацию или актёра: нашлось ровно пять мест и ни одной предметной записи. Из 93 остальных внешних ключей на `organisations` все — `NO ACTION`, то есть любая забытая ссылка дала бы отказ, а не тихое удаление. Удалено одной транзакцией: 1 заявка на связь, 5 членств, 2 организации; журнал команд (7 строк) **оставлен** — это запись о случившемся, журналы называют тех, кого больше нет, и в интерфейсе он не виден. **Попутно найдено и НЕ исправлено (новая находка A11):** `npm run test:postgres` **не входит в `verify`** и не зелёный — на чистой базе 7 из 8, а `READY Product … BuyerCatalog` падает на HTTP 500. Проверено на нетронутом дереве через `git stash -u`: дефект был до этих правок. | 2.2, 11, 15, 17, 20 | IMPLEMENTED; `npm run verify` green including PostgreSQL на чистой базе; правило доказано вставкой мимо модуля — новая личность проходит, личность на занятый идентификатор отвергнута с именем ролей; приёмочные наборы на чистой базе дают два владельца `active` со своей почтой и три актёра `disabled` на `@acceptance.invalid`; демо-база после уборки — две организации, четыре членства, ноль висячих; рабочее пространство прочитано живьём: те же 8 коллекций и 16 SKU, шоурум на месте, слова «Acceptance» на экранах больше нет. |
| 2026-09-22 | `fix/failures-say-what-they-are` | **Аудит A5: отказ называет себя, а пятисотка попадает в журнал.** HTTP-слой не писал **ничего**, включая пятисотые: на сервере не оставалось ни стека, ни SQLSTATE, ни идентификатора запроса. Идентификатор уже уходил в ответ — и нигде не сохранялся, то есть жалоба «получил 500, вот requestId» ни с чем не сопоставлялась, и отладить пятисотку было нечем, кроме как воспроизвести её руками. **Пишется только 5xx.** Четырёхсотые — обычный разговор с клиентом, и журнал из них состоял бы на девяносто девять процентов, после чего перестал бы читаться. **Тело, заголовки и строка запроса не пишутся**: там пароли, токены и персональные данные, а журнал живёт дольше и читается шире, чем сам запрос. Из ошибки PostgreSQL берутся SQLSTATE, `constraint`, `table`, `column` — этого хватает, чтобы найти правило, — но не `detail`, который содержит значения самой строки. Сбой самого журнала не превращает один отказ в два. **Классификация отказов хранилища живёт на границе HTTP, а не в обёртке транзакции, и это существенно.** Первый черновик переводил их в `withPostgresTransaction` и уронил три набора тестов — потому что ловить такой отказ можно только **последним**: у каждого писателя и у каждой службы уже был свой шанс сказать точнее (перехватить 23505 своим сообщением, разобрать `detail` поднятого инварианта), и ранний перевод отнимал у них эту возможность. На границе разбирается ровно то, что не разобрал никто. **Классы:** 23503 → 409 (ссылка на то, чего нет, или на то, что ещё используется), 23505 → 409, 40001/40P01/57014/53300/08006/08003 → 503. А **23502 и безымянный CHECK → 500**, и это не мелочь: правило, записанное в схеме, должно быть записано и в домене — тогда отказ приходит с точным кодом, не дойдя до базы. Если сработал сам CHECK, значит домен правила не знает, то есть дыра **у нас**, и 422 свалило бы её на того, кто прислал безупречный по нашему же договору запрос, да ещё и убрало бы отказ из журнала. Использован действующий суффикс `_RESULT_INVALID` вместо нового соглашения. **Имена таблиц, колонок и ограничений наружу не идут** — наружу класс и фраза, внутренности в журнал: схема не часть договора с клиентом, а имя колонки читателю API ничего не говорит, он посылал поля запроса. **Все 41 код `*_SERVICE_REQUIRED` отдают 503 вместо 422.** Служба, которой в этой сборке нет, — не ошибка позвавшего: запрос был безупречен, а узел собран без этой части, и 503 оставляет клиенту верный вывод — менять запрос незачем, то же самое может сработать в другом месте или позже. **Соглашение расширено на голый код.** Часть триггеров поднимает только код, без фразы за двоеточием; правило `CODE: фраза` их не узнавало, и названный базой отказ доезжал как `INTERNAL_ERROR`. Теперь узнаётся и голый код, а фраза не выдумывается: код и есть всё, что база сказала. **Этим немедленно названа находка A11.** `POST /v2/commercial-publications/{id}/buyer-catalogs` отвечал `HTTP 500 (INTERNAL_ERROR)`, теперь отвечает `HTTP 422 (BUYER_CATALOG_PRICE_HIERARCHY_MISMATCH)`, а журнал показал остальное: SQLSTATE 23514, поднято из `insertPriceListVersion`. Сам дефект — расхождение прайс-листа и каталога байера в иерархии цен — **остаётся открытым**: это отдельный разбор, а не правка транспортного слоя. | 11, 17, 20 | IMPLEMENTED; `npm run verify` green including PostgreSQL на чистой базе (1528/0/0 skipped, +11); за весь прогон в журнал попала ровно одна пятисотка — та, которую тест поднимает намеренно; `npm run test:postgres` даёт прежние 7 из 8, но восьмой теперь называет своё правило вместо `INTERNAL_ERROR`. |
| 2026-09-22 | `fix/buyer-pack-size-reaches-the-buyer` | **Аудит A11: публикация каталога байера была сломана целиком, и никто не мог этого увидеть.** Миграция 075 заморозила договор: SKU в прайс-листе равен SKU публикации **плюс ровно три** байерские надстройки — цена, валюта, минимальный заказ. Миграция 108 завела кратность упаковки, `canonical-source.mjs` стал добавлять четвёртую — `buyerPackSize`, — а проверка о ней не узнала. С того дня база отвергала **любую** публикацию канонического каталога байера: сравнение «SKU прайс-листа без трёх надстроек равен SKU публикации» не сходилось на лишнем поле. **Это не дефект теста, а сломанный рабочий путь**, пролежавший незамеченным ровно потому, что наружу он выглядел как `HTTP 500 INTERNAL_ERROR` без единой строки в журнале. Назвать его удалось в первом же прогоне после A5, а найти конкретное из пяти мест проверки — временной копией функции в отдельной базе, где каждое место подняло свой код: правило раскладывается на условия, пока не останется одно. **Исправлена проверка, а не писатель.** Кратность **обязана** дойти до байера: по ней сетка заказа отказывает в количестве не по коробке, и заморожена она вместе с ценой — байер держится той упаковки, которую ему опубликовали, а не той, на которую бренд сменил её потом. Миграция 126 переопределяет `assert_canonical_commercial_price` (075 не правится). **Заодно закрыт пробел, оставленный той же невнимательностью:** четвёртая надстройка не только не снималась перед сравнением — она **ни с чем не сверялась**, в отличие от остальных трёх, то есть байеру можно было показать одну упаковку в карточке и другую в строке цены. Теперь сверяется. **Тест закрывает не случай, а причину:** он сам читает список надстроек из каталога и список снимаемых полей из последней миграции и требует их совпадения — то есть следующая надстройка, добавленная без правки проверки, роняет тест, а не рабочий путь. Проверено обратным опытом: добавленная пятая надстройка тест роняет. | 7.1, 7.2, 11, 15, 17, 20 | IMPLEMENTED; `npm run verify` green including PostgreSQL на чистой базе (1530/0/0 skipped, +2); **`npm run test:postgres` зелёный впервые: 8 из 8** — до этого 7 из 8 с `READY Product … BuyerCatalog` в красном. |
| 2026-09-22 | `fix/one-rounding-rule` | **Аудит A7: округление платформы — это округление PostgreSQL.** `Math.round` округляет половину к плюс бесконечности: у положительных это «от нуля», у отрицательных — «к нулю». PostgreSQL у `numeric` округляет половину **от нуля при любом знаке**. Пока числа положительные, разницы нет, и её не было видно годами; она появляется на убыточной марже, стоимостной коррекции и кредите поставщика — `ACTUAL_COST_AMOUNT_INVALID` требует лишь **ненулевой** суммы, так что отрицательная законна. Там, где то же число пересчитывает триггер целостности, это не расхождение в копейке, а отказ записи — и срабатывало бы оно ровно на убыточном заказе, то есть в том случае, ради которого маржу и считают. **Причина оказалась не в правиле, а во втором мнении.** Доля маржи считалась своей арифметикой на числах с плавающей точкой, хотя **в том же модуле** построчная аллокация уже считала её общим помощником `calculateMoneyPercentage`, который делит в целых BigInt и округляет половину от нуля. Актуализация маржи переведена на него; собственный `roundMetric` удалён. **Правило округления сумм сведено в один `roundAwayFromZero`** в `core/money.mjs`. До этого в проекте жили два разных `roundMoney`, и второй прибавлял `Number.EPSILON` **к значению**, а не к его модулю, то есть чинил представление ценой той самой симметрии, ради которой поправка и нужна. Минус ноль приводится к нулю: в JSON он неотличим от нуля, а в сравнении — отличим. **Названа и граница того, что этим не лечится.** Десятичная половина вида `0,00005` в double не существует: ближайшее число лежит чуть ниже, и любой язык округлит его вниз, а PostgreSQL, читающий десятичную строку точно, — вверх. Правилом округления это не чинится, только целочисленной арифметикой — и именно поэтому доля считается в BigInt. Тест проверяет правило на половинах, **точно представимых** в двоичной плавающей точке (иначе он проверял бы удачу), а точность доли — сравнением с самим PostgreSQL на том же выражении, которым её пересчитывает триггер `035`. | 7.1, 11, 15, 17, 20 | IMPLEMENTED; `npm run verify` green including PostgreSQL на чистой базе (1532/0/0 skipped, +2); `npm run test:postgres` 8 из 8; обе проверки округления идут против настоящей базы, а не против переписанного правила. |
| 2026-09-22 | `fix/money-chain-runs-on-a-real-order` | **Аудит B4: денежный контур был не «не засеян», а сломан — и сломан именем поля.** Одиннадцать таблиц фактических денег стояли пустыми при трёх десятках файлов тестов. Причина: снимок заказа кладёт номер строки как `lineNo`, а экономика заказа читала `orderLineNo` — имя, которого нет ни у одной строки в базе. Поэтому **любой** канонический заказ выглядел для контура как смесь двух моделей идентичности и отвергался (`ORDER_COMMIT_ECONOMICS_LINEAGE_MIXED`), а рукописные фикстуры тестов подавали `orderLineNo` напрямую и проходили. Код был написан и **ни разу не выполнялся по настоящему заказу** — ровно та форма дефекта, которую зелёные тесты не ловят по построению. То же расхождение сидело во втором месте — в основе разнесения затрат (`COST_ALLOCATION_ORDER_LINE_IDENTITY_INCOMPLETE`). Имя разрешается теперь в одном месте, `commitLineNo`, чтобы «как называется номер строки» не разошлось снова. **Контур пройден по живым заказам через те же службы, что и прод**, а не вставками в таблицы: обязательство поставки → курс → строки затрат → landed cost → разнесение по строкам → актуализация маржи. Разделение обязанностей показано, а не обойдено: обязуется поставить владелец (`supply.manage` есть у него и у продаж), деньги ведёт заведённый для этого финансист (`cost.manage` есть у финансов и у владельца). **Два заказа намеренно кончаются по-разному.** 4320 € дают маржу 2156 € (49,9074 %); 1440 € дают **убыток** −270 € (−18,75 %), потому что та же оснастка и та же перевозка на втрое меньшей партии съедают маржу. Система, которая умеет показывать только прибыль, — это брошюра. И это **единственный способ увидеть на живых данных отрицательную маржу**: убыточный заказ прошёл триггер `035`, пересчитывающий долю уже в PostgreSQL, то есть исправление A7 проверено не тестом, а работой. Шаги сида идемпотентны **поштучно**: записи контура неизменяемы, и вторая попытка завести обязательство на тот же подтверждённый заказ — не повтор, а отказ; прерванный сид обязан дойти оттуда, где остановился. **Открытым осталось:** экрана у этих чисел нет. Во всём `public/` три упоминания слова margin, и ни одно не про фактическую маржу заказа. Данные есть — показать их пока негде. | 7.1, 7.3, 11, 15, 17, 20 | IMPLEMENTED; `npm run verify` green including PostgreSQL на чистой базе (1535/0/0 skipped, +3); контур пройден на демо дважды подряд — второй прогон ничего не создаёт и говорит «маржа уже актуализирована»; в базе 2 обязательства поставки, 2 курса, 6 строк затрат, 2 свода landed cost, 1 политика и 2 прогона разнесения, 2 актуализации маржи. |
| 2026-09-22 | `fix/order-economics-is-reachable` | **Экран фактической экономики заказа был написан целиком и недостижим.** Диалог «Экономика заказа» лежит в `views-4.js` со статусом, фиксацией заказа, себестоимостью, маржой, процентом и причинами блокировки закрытия — а кнопка, которая его открывает, осталась на **прежней карточке заказа**, которую омнидата-реестр заменил. Искать «чего не хватает в интерфейсе» пришлось не в отсутствующем коде, а в коде, до которого не доходит нажатие. **Вторая причина пустого экрана была в чтении.** Позиция заказа отдавала одни `null`, пока по заказу не оценена готовность к закрытию, — то есть связывала вопрос «сколько заработано» с вопросом «можно ли закрывать». Это разные вопросы: маржа существует с того мгновения, как её посчитали. Теперь она отдаётся вместе с честной причиной `readiness_not_evaluated` — цифра есть, окончательной её ещё никто не объявлял. **Диалог переиспользован, а не переписан**, и тест это закрепляет: два экрана об одном числе расходятся ровно тогда, когда один из них поправят. Действия заказа переопределяются в `order-lifecycle-actions.js` — первая правка легла в `omnidata-workspace.js` и оказалась мёртвым кодом, потому что живой список действий собирает другой модуль; правка перенесена туда, где выполняется. | 7.1, 11, 17, 20 | IMPLEMENTED; `npm run verify` green including PostgreSQL на чистой базе (1538/0/0 skipped, +3); прочитано живьём в браузере: у заказа на 1440 € экран показывает 1 710,00 EUR себестоимости, −270,00 EUR маржи и −18,75 %, у заказа на 4320 € — 2 164,00 EUR, 2 156,00 EUR и 49,91 %. |
| 2026-09-22 | `feat/shipment-tail-walks-on-the-demo` | **Аудит B3: хвост за обязательством поставки пройден целиком по живым заказам.** Семь таблиц — план отгрузки, уведомление, приёмка, расхождение, претензия, её решение, складской журнал — стояли пустыми, и на вопрос «а где товар сейчас», первый после маржи, показать было нечего. Цепочка пройдена **через те же службы, что и прод**, а не вставками: Porto → Hamburg, DHL, номер отслеживания, приёмка на складе ретейлера. **Стороны разделены по-настоящему, а не одним аккаунтом.** План и отгрузку заводит бренд (`fulfillment.manage`), приёмку записывает магазин (`receipt.manage`) — служба прямо отказывает тому, кто не состоит в принимающей организации, — претензию подаёт магазин (`claim.manage`), а решает её бренд. Демонстрация обязана показывать это правило, а не обходить его. **Одна поставка приходит целой, вторая — с недостачей.** Приёмка «всё сошлось» доказывает только счастливый путь; расхождение — то, ради чего приёмку и ведут. На партии в 60 штук: отгружено 60, принято 57, повреждено 3, **принято к учёту 54**, недостача 3. Магазин подал претензию на возмещение, бренд принял её кредит-нотой. **Повреждённое на склад не попало** — в журнале лежит только принятое, потому что на повреждённое пишут претензию, а не ставят его в продажу. Шаги идемпотентны поштучно: прогон, прерванный на середине, доходит оттуда, где остановился, а третий подряд прогон не создаёт ничего. Первая версия шага делала `continue` сразу после «приёмка уже записана» и тем пропускала оприходование и претензию на повторном прогоне — поймано повторным запуском, а не рассуждением. **Открытым осталось:** экрана у хвоста нет. Пункт «Логистика» в меню — заглушка со статусом `planned`, нажатие никуда не ведёт. | 7.3, 7.4, 11, 17, 20 | IMPLEMENTED; `npm run verify` green including PostgreSQL на чистой базе (1538/0/0 skipped); в демо-базе 2 плана отгрузки, 2 уведомления, 2 приёмки, 2 снимка расхождения, 1 претензия, 1 решение, 2 записи складского журнала; третий прогон сида подряд — ноль ошибок и ноль новых записей. |
| 2026-09-22 | `feat/where-the-goods-are` | **Экран отгрузочного хвоста: «где товар сейчас».** Пустым его держала не вёрстка, а **чтение**: записи хвоста читались только поштучно, по идентификатору (`GET /v2/fulfillment-plans/{id}`, `/shipment-notices/{id}`, `/receipts/{id}`), и спросить «что с этим заказом» было нечем — показывать было нечего, потому что нечего было запросить. Искать «чего не хватает в интерфейсе» пришлось в отсутствующем read-model, а не в отсутствующей разметке. Добавлены читатель, служба и `GET /v2/orders/{id}/fulfillment`: план → отгрузка → приёмка → расхождение → претензия → решение **одним запросом**, дочерние уровни читаются пачкой через `= ANY($1)`, а не по одному — иначе это N+1 там, где заведомо нужен весь список. Расхождение пересчитывается после каждой приёмки, поэтому берётся последнее: показывать первое значило бы показывать позавчерашнюю недостачу. **Право спрашивается у той организации, в которой состоит спрашивающий.** Цепочку вправе видеть обе стороны сделки — бренд отгружает, магазин принимает, — и собирать один факт из двух разных прав было бы неверно. Чужому обеим организациям отказывают. **Экран показывает не только счастливый путь.** В порядке событий: маршрут, плановые даты, отгрузка с перевозчиком и номером отслеживания, приёмка с номером GRN, принято к учёту, повреждено, недостача, претензия с причиной и её решение. Сошедшаяся поставка говорит об этом прямо — «нет, поставка сошлась», — а не молчит. **Диагноз дефекта занял один запрос благодаря A5:** первая версия читателя сортировала по `created_at` там, где колонка называется `submitted_at` и `resolved_at`; журнал назвал таблицу, колонку и строку сразу. | 7.3, 7.4, 11, 17, 20 | IMPLEMENTED; `npm run verify` green including PostgreSQL на чистой базе (1542/0/0 skipped, +4); прочитано живьём: у заказа на 60 шт. экран показывает 60 отгружено, 57 принято, 54 к учёту, 3 повреждено, 3 недостача, претензию на возмещение и её принятие; у заказа на 180 шт. — «нет, поставка сошлась». |
| 2026-09-22 | `feat/release-requires-material-trace` | **Аудит B1: отгрузка выпускается только тогда, когда известно, из чего она сшита.** Шесть отгрузок на 4800 штук были выпущены при **нуле** выдач материала: учёт партий вёлся и ни на что не влиял, потому что выпуск не спрашивал о материале вообще. Это был дефект **правила**, а не витрины — в отличие от остального раздела B, где данных просто не было. Партия материала ведётся ровно ради одного вопроса: если в носке вылезет дефект полотна, какие рулоны в нём были и куда ещё они ушли. Ответ собирается **до** отгрузки, потому что после неё собирать не из чего — товар у покупателя, а связи «эта отгрузка — эти рулоны» нет нигде. Выпуск на отгрузку и есть последний момент, когда её ещё можно записать. **Ведомость, которой нет, судить не может** — то же исключение, что и при самой выдаче (`MATERIAL_LOT_NOT_IN_BILL`), и по той же причине: без опубликованной спецификации неизвестно даже, из чего изделие должно состоять, и отказ остановил бы отгрузку по причине, к прослеживаемости отношения не имеющей. Есть ведомость — это заявление «вещь сшита из материалов», и тогда отгрузить, не назвав ни одного рулона, значит потерять прослеживаемость навсегда. Правило держат **и домен, и база** (миграция 127): проверка стоит **до** записи инспекции, чтобы отказ не оставлял наполовину выпущенную отгрузку, а триггер ловит писателя, обходящего модуль, — что и проверено вставкой сырым SQL: `QUALITY_RELEASE_WITHOUT_MATERIAL_TRACE` называет и партию, и недостающий материал из ведомости. **Шесть уже выпущенных отгрузок остаются как есть, и это не недоделка.** Выдать материал в завершённое исполнение нельзя (`MATERIAL_LOT_EXECUTION_NOT_ACTIVE`), а дописать связь мимо домена значило бы подделать прослеживаемость — ровно то, против чего правило вводится. Прошлое не переписывается; правило связывает с этого дня. **Фикстура чужого теста оказалась устаревшей, а не мешающей:** поддельное хранилище финального качества не знало новых читателей, и правильным было научить фикстуру модели настоящего хранилища, а не сделать правило пропускаемым через `?.`. | 6.4, 11, 15, 17, 20 | IMPLEMENTED; `npm run verify` green including PostgreSQL на чистой базе (1548/0/0 skipped, +6); отказ проверен и через службу (выпуск отвергнут, инспекция осталась в `review-pending`, ни одной записи выпуска), и сырым SQL мимо модуля; после записи рулона тот же выпуск проходит. |
| 2026-09-22 | `feat/agreed-sampling-states-its-reason` | **Аудит B5: у выборки должно быть происхождение, и одна половина находки оказалась историей, а не дефектом.** Аудит назвал «8 из 10 прогонов без `source` и `standardCode`». Разведка показала: модель прогона **давно** требует происхождения — либо `standard` с кодом стандарта, уровнем и строкой таблицы под размер партии, либо `agreed`. Восемь прогонов без `source` написаны **до** этой модели, и живой путь такую запись сегодня создать не может. Чинить было нечего; сказать об этом — обязательно. **Живой дефект был в другом, и он тот же по форме, что и B1: запись не могла объяснить собственное число.** У договорённого плана причина была **необязательной** (`optionalText`), то есть единственное место, где число, решающее судьбу партии, берётся не из таблицы, а из головы, не требовало ни слова объяснения. На демонстрационных данных так и вышло: партия в 400 штук проверена выборкой 80, тогда как таблица бренда для 281–500 даёт 50 — и почему не 50, запись не говорит. Теперь договорённый план обязан назвать причину: `QUALITY_SAMPLING_NOTE_REQUIRED`, пустая строка и пробелы не считаются. У стандартного плана требования нет — у него происхождение записано само. **Правило стоит там, где план формируется, а не в расчёте пределов.** Первая попытка поставила его в `agreedSamplingPlan`, и он немедленно отверг собственный внутренний вызов: эта функция зовётся дважды, по разу на каждый предел, и второй раз причина заведомо `null`. Причина у плана одна, а не по одной на предел. **Фикстуры чужих тестов пришлось научить называть причину** — пять мест в трёх файлах. Это и есть содержание правила: договорённый план без объяснения перестал быть записываемым нигде, включая тесты. | 6.4, 11, 17, 20 | IMPLEMENTED; `npm run verify` green including PostgreSQL на чистой базе (1550/0/0 skipped, +2); отказ проверен на `undefined`, `null`, пустой строке, пробелах и слишком коротком тексте; стандартный план проходит без причины и несёт `source: standard` со своим кодом. |
| 2026-09-22 | `feat/approved-demand-chain-runs`| **Аудит B2: контур «потребность → заказ» пройден, и проверка происхождения включилась сама.** Снимки потребности стояли пустыми, и из-за этого **вторая ветвь проверки не исполнялась ни разу**: правило написано как «либо унаследованный заказ первой версии, у которого происхождения нет, либо заказ второй версии — и тогда обязаны быть снимок потребности, номер строки, хеш содержимого и ProductSku», а все девять заказов оставались первой версии. Проверка была и была выключена — не потому, что её отключили, а потому, что путь, который её включает, никто ни разу не прошёл. Цепочка пройдена по живым данным через те же службы, что и прод: обязательство поставки → снимок потребности → запрос цен **из строки потребности** → котировка → выбор поставщика → размещение → производственный заказ `PO-DEMAND-001` со второй версией происхождения. Шаг добавлен в сид и идемпотентен: повторный прогон находит уже выросший заказ и ничего не делает. **Первая версия остаётся законной, и это решение, а не недоделка.** Бренд шьёт и без подтверждённого оптового заказа — на склад, на образцы, на допоставку, — и запрещать это значило бы запретить половину работы отрасли. Разница в том, что теперь видно, какой производственный заказ вырос из чужого обязательства, а какой — из решения бренда. **Найдено попутно и НЕ исправлено:** `seed-demo` не собирает демонстрацию с нуля — на пустой базе он падает на «No published collection to demonstrate», то есть коллекцию создаёт какой-то другой путь, а какой, скрипт не называет. Демонстрация воспроизводима только поверх уже существующей базы. | 6.5, 7.3, 11, 17, 20 | IMPLEMENTED; `npm run verify` green including PostgreSQL на чистой базе (1550/0/0 skipped); каждый шаг цепочки пройден через HTTP на живом рантайме и записан: `production_requirement_snapshots` 0 → 1, строк потребности 1, RFQ с `lineage_version = 2` и ссылкой на потребность, производственный заказ `PO-DEMAND-001` с версией 2, номером строки 1 и ProductSku; прочитано в браузере — заказ виден в разделе «Производственные заказы» на 180 шт. |
| 2026-09-22 | `feat/season-palette-and-shell-verdicts` | **Аудит B6: три «пустые скорлупы» оказались тремя разными вещами, и одинаковое решение по ним было бы неверным.** **`season_colour_palettes` — достроена.** Таблица была моей и стояла без единой строки кода во всём репозитории: вопрос «что вообще в этом сезоне» задать было некому. Появились домен, хранилище, служба и маршруты `POST/GET /v2/campaigns/{id}/palette`. Палитра закрепляет **версию** записи справочника, а не только её идентификатор: палитра сезона — решение, принятое один раз, и переименование оттенка в справочнике не должно менять утверждённый сезон. Правила: цвет берётся из словаря цветов и только из него (запись семейства цвета — не цвет), отставленный цвет нового сезона не открывает, чужую кампанию нельзя покрасить даже случайно, позиция — часть решения, потому что палитра читается сверху вниз и первым идёт главный цвет. Демонстрация собирает палитру из тех же governed-цветов, которыми покрашено полотно: палитра, не совпадающая со складом, — пожелание, а не решение. **`tech_pack_operations` — вытеснена, а не продублирована.** 089 привязывала операции к техпакету, 116 завела последовательность `bol_sequences` с этапом и узлом конструкции; работа идёт через неё — 20 строк против нуля. Слить нельзя: разные агрегаты с разными ключами, и слияние снесло бы класс оборудования, которого во второй модели нет. **`catalog_outbox_events` — отставной путь записи.** Вставок нет **нигде** — ни в `src/`, ни в миграциях; каталог пишет прямо в единую `outbox_events`. Триггер-зеркало, счётчик метрики и уборка оставлены совместимостью, и ноль строк здесь — ожидаемое состояние, а не потерянные события. **Ни одна таблица не удалена, и ни одна больше не молчит:** миграция 128 даёт каждой `COMMENT`, который читается прямо в `\\d+`, — таблица без строк и без объяснения читается как недоделка независимо от того, чем она является. | 5.1, 6.1, 11, 15, 17, 20 | IMPLEMENTED; `npm run verify` green including PostgreSQL на чистой базе (1555/0/0 skipped, +5); палитра проверена на живом рантайме: цвет добавлен с закреплённой версией, повтор цвета отвергнут (`SEASON_PALETTE_ENTRY_EXISTS`), несуществующий цвет отвергнут (`SEASON_PALETTE_COLOUR_UNKNOWN`), чтение возвращает палитру по позициям. |
| 2026-09-22 | `feat/mdm-coverage-says-the-truth` | **Аудит B7: разрыв 1060 против 16 назван числом, и внутри него найден настоящий дефект.** **Покрытие 1,5 % само по себе не дефект**: каталог описывает предметную область целиком, а не список задач, и требовать 100 % значило бы требовать тысячу пустых справочников вместо восемнадцати работающих. Какие из объявленных нужны платформе — решение владельца, а не валидатора. **Дефектом было молчание:** `validate:mdm` отвечал `ok`, и на этом чтение заканчивалось — он проверял, что заведённое хорошо устроено, и не имел мнения о том, что заведено не всё. Теперь печатает `operationalCoverage`, `notInstantiated` и список «тонких» справочников, а покрытие держит **храповиком**: расти не обязано, потерять молча нельзя (тот же приём, что и базовая линия типов, и по той же причине — он запрещает откат, не требуя совершенства). **«Товарная категория из одной записи» — снято: это намеренно.** Описание самого справочника говорит: «минимальный управляемый корень; более детальная структура расширяется только отдельными управляемыми версиями MDM». Валидатор всё равно называет его тонким, чтобы решение оставалось видимым, но не считает ошибкой. **Настоящий дефект нашёлся рядом, и он объективен:** служба идентичности продукта резолвит `assortment.product_type` и `size.accessory_size`, которых в операционном наборе **не существовало**. Поле типа товара нельзя было заполнить никогда и никому: все 14 версий моделей стоят с категорией и с пустым типом при живой колонке `product_type_entry_id`. Оба справочника заведены (8 типов одежды, 5 размеров аксессуаров), и добавлено правило: **справочник, который называет код, обязан существовать** — единственная часть покрытия с объективным ответом. **Связь типа с категорией записана атрибутом, а не родителем:** первая версия поставила `parent_code: APPAREL` и была отвергнута бутстрапом (`MDM_BOOTSTRAP_PARENT_NOT_FOUND`) — родитель разрешается внутри своего справочника, а APPAREL живёт в справочнике категорий. | 5.1, 11, 17, 20 | IMPLEMENTED; `npm run verify` green including PostgreSQL на чистой базе (1555/0/0 skipped); храповик проверен обратным опытом — завышенная база даёт отказ; новое правило поймало `size.accessory_size` само, без подсказки; 13 записей загружено в демо-базу бутстрапом справочников. |
| 2026-09-22 | `fix/forbidden-reads-as-a-refusal` | **Аудит C: отказ по правам виден пользователю.** Клиентский транспорт знал только `401`: любой `403` доходил до экрана красным баннером «Ошибка запроса» — ровно тем же, что и обрыв сети. Под покупателем `buyer@nordhaus.example` это подтверждено живьём: `ACTIVE_MEMBERSHIP_REQUIRED` выглядел сбоем платформы, а не её решением. **Отказ отделён от сбоя в одной точке.** `public/modules/api.js` помечает `403` как `error.forbidden` и заменяет диагностический код предложением «У вас нет прав на это действие. Обратитесь к владельцу организации.»; повтор не включается — `isRetryableTransportError` и раньше повторял только `REQUEST_TIMEOUT` и разрыв соединения. **Экранное состояние стоило одного изменения, а не сорока:** разделы хранят только текст сообщения, поэтому `notice()` в `public/modules/dom-1.js` сам распознаёт это предложение и рисует `notice denied` — нейтральный серый блок с замком; стиль лежит в `public/omnidata-v14-role-system.css` под обоими корнями ОДС. **То же сообщение нашлось в виде действия:** два раздела предлагали при ошибке кнопку «Повторить», бесполезную на отказе, — она больше не показывается, а «Торговые точки» перестали префиксовать отказ словами «Не удалось загрузить». Это измеренные, а не предположенные два места. Помощника «весь раздел закрыт» намеренно нет: честного вызова у него сегодня не существует, и тест закрепляет его отсутствие, чтобы правка про недостижимое сама не оставила недостижимого. **Управляемый ключ кеша сдвинут согласованно** — `visual-20260806-14-role-system-1` → `visual-20260922-14-role-system-2` — в оболочке, в рантайме, в валидаторе и в шести закрепляющих его тестах: пара «стиль + рантайм» обязана двигаться вместе, ради этого ключ и управляется. **Граница названа, потому что измерена:** на защищённых маршрутах отказ приходит как надо (`GET /v2/orders/{id}/economics-position` → `403`), а те ~55 читающих маршрутов, что не проверяют роль, отвечают `200` с пустым телом — раздел выглядит пустым, а не закрытым. Утечки нет, отказа тоже; состояние «нет прав» там появится только вместе с проверкой роли на чтении. | 13, 14, 16 | `tests/forbidden-reads-as-a-refusal.test.mjs` (403-ветка, отсутствие повтора, вид `denied`, отсутствие «попробуйте ещё раз» в обоих языках); verify 1559/1559 на чистой базе |
| 2026-09-22 | `fix/every-pageable-section-can-be-finished` | **Аудит C: реестры дочитываются, а граница видна.** Аудит считал разделы по клиенту (четырнадцать) — на сервере их **семнадцать**, и `reset()` молча отбрасывает всё, чего нет в клиентском списке: `placeholders`, `colorways` и `media` сервер объявлял обрезанными и выдавал курсор, клиент выбрасывал его без ошибки. **`media` нельзя было дочитать вообще, и это дефект сервера:** курсор keyset-чтения продолжается по последнему полю сортировки, поэтому оно обязано быть уникальным и непустым; у шестнадцати разделов последним стоит `id` или `sku`, а у `media` — пара `(styleVersionId, colorwayId)`, которая ключом не является и у строки уровня стиля пуста наполовину (14 строк из 28 в демо). Живьём `GET /v2/workspace/media/page` отвечал `400 WORKSPACE_CURSOR_INVALID` — сервер отвергал собственный курсор на первом продолжении. Ключ уже существовал: вьюха строит `id` = `styleVersionId[:colorwayId]` по одной строке на пару, он уникален по построению и просто не попадал в payload, а клиент видит только payload; миграция 129 кладёт его туда. **«Загрузить ещё» существовало и было недостижимо:** механизм `sectionCard()` подключён в одиннадцати разделах, а на экране их ноль — `.card.section` не рендерится нигде, весь интерфейс ОДС. Полоса продолжения поэтому добавлена в `odRegistry` — одну точку, через которую рисуется каждый реестр, — с одной таблицей соответствий рядом; собрана из готовых элементов системы, без нового слоя стилей и без сдвига ключа кеша. Устаревшая плашка «сузьте фильтр» в линейном плане убрана как дублирующая. | 13, 14, 16 | `tests/every-pageable-section-can-be-finished.test.mjs` (списки разделов в ядре/службе/читателе/клиенте совпадают; сортировка каждого раздела кончается уникальным ключом; проверено, что тест падает при возврате обоих дефектов); живьём: media 6 страниц и 27 записей без дублей, три раздела дочитываются с клиента, SKU 1 → 16; verify 1566/1566, test:postgres 8/8 |
| 2026-09-22 | `feat/measurement-tables-hold-their-own-rules` | **Аудит C: таблицы обмеров держат свои правила сами.** Ноль триггеров на всех пяти таблицах при живых данных (29 таблиц, 57 точек, 71 значение). Добавлено девять — три правила, все `CONSTRAINT TRIGGER ... DEFERRABLE INITIALLY DEFERRED`, потому что проекции переписываются несколькими запросами в одной транзакции. Правила не новые: они уже сформулированы в домене (`MEASUREMENT_MATRIX_INCOMPLETE`, `MEASUREMENT_REVISION_ALREADY_ARCHIVED`, `replaceMatrix`) — база держит их тоже, потому что межагрегатное правило, живущее в одном приложении, действует лишь до первого пути, который через это приложение не идёт. Перед добавлением измерено, что все 29 таблиц им удовлетворяют вплоть до отдельных ячеек, поэтому `NOT VALID` не нужен. **Главное нашлось проверкой, а не чтением:** первая версия сверяла только состав ячеек, и `UPDATE measurement_values SET value = value + 7` проходил — реестр показывал обхват груди 119 см там, где payload держал 112; экран читает payload, сетка градации проекцию, изделие кроят по одному из двух. Теперь сверяется само число в обе стороны. Полнота требуется от опубликованной таблицы, а не от черновика — ровно как в домене. **Аудит был не совсем прав в слове «не защищена»:** публикационное состояние уже охранял check-constraint, а обязательные поля NOT NULL. **И открылось свойство сильнее заявленного:** внешний ключ требует снять архив раньше таблицы, а правило — чтобы архив был на месте, поэтому опубликованную таблицу обмеров удалить нельзя вовсе; это желаемо, но любая уборка обязана идти путём приложения. | 4, 7.3, 15 | `tests/postgres/measurement-tables-hold-their-own-rules.test.mjs` (сырым SQL, в обход домена: неполная матрица, расхождение payload и проекций в обе стороны и по числу, замена и удаление без архива, неделимость опубликованной, разрешённые черновик и ревизия с архивом); живьём: демо-сид 50 шагов, публикация через приложение v1 → v2; verify 1566/1566, test:postgres 9/9 |
| 2026-09-22 | `fix/attribute-coverage-cannot-outrun-the-register` | **Аудит C: подтверждение покрытия атрибутов больше не обгоняет реестр.** Противоречие лежало внутри одной записи: живой запрос на версию модели без единого атрибута возвращал вердикт `ready`, а рядом, в его же доказательстве, — `{coverageAttestation: true, styleAttributeCount: 0, skuAttributeCount: 0}`. Это было единственное измерение из восемнадцати, чей вердикт приходил утверждением из тела запроса. Теперь вердикт требует и подтверждения, и хотя бы одного управляемого значения у версии модели или её SKU, а причина отказа называет расхождение. **Порог скромный намеренно:** каталог объявляет обязательность полем `required_when`, но оно заполнено прозой у 4 определений из 102 и пусто у остальных — посчитать «все обязательные заполнены» нечем, и придумывать политику за домен здесь неуместно; это следующий настоящий шаг. **Аудит был не прав в одном:** сами значения уже управляемы базой (миграция 082) — живьём `banana.split` → `PRODUCT_ATTRIBUTE_NOT_IN_CATALOGUE`, `footwear.shaft_height` на одежде → `PRODUCT_ATTRIBUTE_CATEGORY_MISMATCH`, — поэтому «есть значения» это управляемый факт. **Одна строка значений была дефектом демо:** сид заводит настоящие атрибуты управляемого набора одежды, стало 92 строки по 14 моделям, шаг идемпотентен; живая приёмка готовых изделий тоже заводит настоящее значение вместо утверждения на слово. | 7.2, 11, 15 | `tests/product-readiness-domain.test.mjs` (подтверждение при пустом реестре — blocked с причиной о расхождении; при заполненном — ready; без подтверждения — blocked); живьём: ready опирается на 7 значений; verify 1567/1567, test:postgres 9/9 |
| 2026-09-23 | `feat/payment-milestones-know-four-events` | **Аудит C: график платежей знает четыре события, а не два.** Аудит нашёл девять одинаковых графиков 30/70 и назвал это «не проверено данными». Причина оказалась иной: `PAYMENT_TRIGGERS` знал подтверждение заказа и допуск к отгрузке, поэтому третью веху было **не на что вешать** — «до двенадцати вех» оставалось словами. События при этом платформа уже записывает по тому же заказу и с датой; измерено на живых данных: запуск в работу есть у девяти заказов из девяти, готовность к контролю — у восьми, допуск к отгрузке — у шести. Словарь расширен до четырёх в порядке наступления, график их не копирует, а читает там, где они живут. **Правило живёт и в базе — и потому расхождение нашлось живой оплатой:** ветка `ELSE` триггера 112 считала допуском к отгрузке всё, что не подтверждение, поэтому веха «запуск в работу» искала дату в выпусках отгрузки; чтение говорило «причитается», запись — «не наступило». Миграция 131 выбирает событие явно по каждому из четырёх и отвергает неизвестное кодом `PAYMENT_TRIGGER_UNKNOWN`, а не приравнивает его молча к отгрузке. Демонстрация показывает настоящий график 20/30/30/20 на заказе из потребности, не переписывая прежние девять. Заодно исправлен дефект сида: цикл оплаты шёл по снимку и не перечитывал график, что на двух вехах не проявлялось. | 8.4, 11, 15 | `tests/supplier-payments.test.mjs` (четыре вехи, деление долей без потери копейки, каждое событие смотрит в своё доказательство, запрет платить за ненаступившее); живьём: две вехи оплачены по разным событиям, суммы сходятся 100 700 + 100 700 = 201 400; verify 1569/1569, test:postgres 9/9 |
| 2026-09-23 | `fix/bom-exchange-rate-is-a-rate` | **Аудит C: кросс-валютная ведомость исполнена, и исполнение нашло дефект в счёте.** Данные подтвердились — 15 строк в одной валюте, все 14 материалов в евро, — но стоило путь **исполнить**, как платформа не дала записать настоящий курс. Курс хранился как деньги, `numeric(20,4)`: на паре RUB→EUR это округляет 0,01085776 до 0,0109 и завышает строку на 0,39 % — тихо, систематически и всегда в одну сторону (живьём 8,0050 € вместо 7,9739 €). Правило о восьми знаках платформа уже знала: оба реестра курсов хранят `numeric(…,8)`, а экономика заказа требует восьми знаков кодом `FX_RATE_SCALE_INVALID`; ведомость была единственным местом, где курс считался деньгами, и единственным, где курс применяется к деньгам построчно. Правило перенесено в денежное ядро, экономика заказа перестала держать свою копию. **Округляли три места, и найти их удалось только исполнением:** проверка значения, сама арифметика (`toScaled` масштабировал курс денежным множителем до умножения) и писатель, приводивший курс к `numeric(20,4)` в запросе — отчего payload и проекция расходились. Миграция 132 расширяет колонку. Демонстрация теперь исполняет путь: сид заводит российскую фурнитуру в рублях и ставит её в ведомость в евро по курсу из реестра сезона. **Остаётся названным:** ведомость реестр сезона не спрашивает — курс приходит числом из тела запроса, и решение о том, обязан ли счёт следовать курсу сезона, принадлежит владельцу домена. | 8.3, 11, 15 | `tests/bom-cross-currency.test.mjs` (восемь знаков доживают до стоимости строки; строка без курса отвергается; строка в валюте ведомости не выдумывает курс; правило в одном месте; писатель и колонка хранят все знаки); живьём `RUB по курсу 0.01085776 → 7.9739 EUR`, шаг сида идемпотентен; verify 1572/1572, test:postgres 9/9 |
| 2026-09-23 | `feat/canonical-measurements-are-reachable` | **Аудит C: авторитетный реестр обмеров стал достижим, а мост между двумя системами остался намеренно отсутствующим.** Половина находки неверна: §20/§356 объявляет каноническую таблицу repository-authoritative, говорит, что она никогда не выводит идентичность из текстового SKU, и называет её единственным семейством, годным для гейта готовности; измерено, что у двух систем и словари разные — каноническая говорит управляемыми записями MDM и Product SizeValue, система по SKU свободным текстом, общих ячеек ноль. Вторая половина верна целиком: живьём `GET /v2/measurements` отдавал 15 таблиц по SKU и **ноль** канонических, ответ по стилю обмеров не нёс, в рабочем пространстве раздела нет — 14 опубликованных канонических таблиц читались только по заранее известному идентификатору. Добавлен список `GET /v2/measurements/canonical` формы соседнего: те же страницы и ограничение по членству, фильтры по статусу, единице, бренду, версии стиля и колорвею; позиция страницы по идентификатору, курсор один на оба списка. **Два теста закрепляли саму недостижимость** — договор OpenAPI требовал `collection.get === undefined`, а тест «canonical route does not get swallowed» фиксировал ровно обратное своему названию; оба приведены к ожидаемому. Ответ по стилю обмеры по-прежнему не несёт намеренно: вопрос решает фильтр `styleVersionId`, второй путь к тому же факту не заводится. | 7.3, 13, 15 | `tests/canonical-measurements-are-reachable.test.mjs` (чтение без идентификатора, страницы без дублей, фильтр по версии стиля, курсор не переносится между наборами фильтров, списки раздельны, читатель без канонической страницы отвергается); живьём 14 записей за 4 страницы, список по SKU остался 15/0; verify 1577/1577, test:postgres 9/9 |
| 2026-09-23 | `fix/money-and-units-read-as-language` | **Аудит C: деньги и единицы читаются на языке пользователя, а форматтер один.** Счёт «более 250 точек» неверен — это счёт всех мест, где выводится число; машинерия была в порядке (`Intl` с локалью). Сломано было три конкретных вещи, все видны на одном живом экране: валюта печаталась кодом («4,10 EUR» вместо «4,10 €»), единицы — латинским кодом («100 m» вместо «100 м»), количества без форматтера теряли разрядность («5000» вместо «5 000»). **Шесть модулей держали по своему форматтеру денег с двумя разными договорённостями о копейках** — четыре делили на сто, два нет. Проверено поимённо: ошибки в сто раз сегодня нет, но ничем она не удерживалась; теперь собственных форматтеров ноль, есть `I18N.formatMoney` и `I18N.formatUnit`. **Находка по дороге:** `i18n-v7.js` переэкспортирует поверхность `SynthaI18n` поимённо, поэтому добавленное в базу и не названное там исчезает молча — функция есть в файле и в его экспорте, а в окне её нет, и экран продолжает показывать старое; ни ошибки, ни предупреждения. | 13, 14 | `tests/money-and-units-read-as-language.test.mjs` (форматирование в одном месте; ни один экран не держит своего форматтера и не дописывает код валюты или единицы; поверхность v7 называет обе функции; неизвестные код и единица деградируют читаемо); живьём `4,10 EUR · 100 m · 5000 m` → `4,10 € · 100 м · 5 000 м`, англ. `€4.10 · 100 m · 5,000 m`; verify 1582/1582 |
| 2026-09-23 | `fix/material-cost-is-guarded-like-cost` | **Аудит C: себестоимость материала охраняется как деньги везде, где она есть.** Счёт «около 55 читающих маршрутов не проверяют роль» неверен: семнадцать читателей фильтруют по роли прямо в SQL, и наборы содержательно разные — деньги закрыты для продаж и наблюдателя, обмеры и образцы для финансов, — а ещё четыре отдают членство службе, которая проверяет способность. Итого роль спрашивают двадцать один читатель из тридцати двух. Утверждение «viewer читает то же, что finance» для них неверно: проверено живьём заведённым наблюдателем — из `/v2/boms`, `/v2/measurements`, `/v2/production-orders` он получает ноль строк. **Настоящая находка одна и объективна:** наблюдатель получал из `GET /v2/materials` строку с `unitCost` — закупочной ценой у поставщика, — и ноль строк из `GET /v2/boms`, где та же цена лежит в составе изделия; при этом `COST_MANAGE` есть у владельца, администратора и финансов, а у роли `viewer` денежных способностей нет ни одной. Изымается **поле**, а не строка (закрывать материал целиком нельзя: нужны название, цвет, единица, состав, остаток), тем же приёмом, каким каталог изымает внутренние складские числа; валюта уходит вместе с ценой, потому что половина факта хуже его отсутствия. Набор ролей не придуман заново — тест требует совпадения с держателями `COST_MANAGE`. Роль читается тем же запросом, что и материал, внутри одного снимка. В демонстрацию добавлен `viewer@syntha.local` — роли, о границах которой пункт, там не было вовсе. | 5, 11, 15 | `tests/material-cost-is-guarded-like-cost.test.mjs` (изъятие поля и сохранение остального, набор ролей против модели способностей, оба пути чтения проектируют, роль не читается отдельным запросом, приём совпадает с каталожным); живьём владелец и финансы `4.1 EUR`, наблюдатель «изъята»; verify 1587/1587, test:postgres 9/9 |
| 2026-09-23 | `fix/partners-screen-and-forms-that-keep-their-promise` | **Живой обход экранов: экран контрагентов вернулся, матрица ролей стала матрицей, форма перестала обещать невозможное.** Обход всех пунктов меню под владельцем, с чтением того, что появилось на экране. **Экран «Контрагенты и доступы» был оторван уровнем выше, чем я предполагал:** `omnidata-v7.js:28` объявляет пункт с `view: 'partners'`, а `omnidata-v7-installed.js` переназначает его на `suppliers` — это был единственный вход, поэтому `renderPartners()` с четырьмя вкладками не достигался ниоткуда. Возвращён своим пунктом через незанятый запланированный слот, без отмены сорсинга. **Матрица ролей показывала одну строку — читателя:** она строилась на `workspace.memberships`, который несёт только своё членство (живьём: маршрут членов отдаёт пять записей, рабочее пространство одну). Теперь читается состав организации, человек назван именем, идентификатор в подсказке. Загрузчик состава не продублирован — вынесен из `styles.js` в `brand-roster.js`, один на оба экрана. **Форма предлагала два значения из четырёх, которых домен не знает:** `sketch` и `flat` всегда отвечали `PRODUCT_MEDIA_ROLE_INVALID` — проверено по каждому варианту живьём. | 13, 14 | `tests/forms-offer-only-what-the-domain-accepts.test.mjs` (предложенное обязано совпадать с принимаемым, для роли изображения и для единиц материала); живьём: пять строк с именами и ролями вместо одной, «Технические пакеты» переходят без ложного уведомления, запланированные пункты честно сообщают о себе; verify 1589/1589 |
| 2026-09-24 | `feat/cutting-defects-trace-to-a-roll` | **Дефект раскроя нельзя было свести к рулону, хотя связь для этого уже существовала в данных.** Перед этим поправил свой же прошлый отчёт: то, что я называл открытым пунктом «A1» (байер видит сырой каталог), на самом деле закрыто 21 сентября и закреплено тестом (`tests/postgres-catalog-reader.test.mjs`) — я сказал «не закрыто», не проверив git-историю файла. Настоящий открытый пункт из реестра требований — `PUB-005`: `Selection` теоретически может собраться без привязки к `BuyerCatalogVersion`, если рантайм соберут без `commercialPublicationReader`. В демо-базе таких подборок нет (проверено запросом), и в единственной продакшен-сборке (`postgres-base-runtime.mjs`) читатель передаётся всегда; сужение на уровне конструктора сломало бы 5+ тестов, намеренно использующих облегчённую сборку сервиса для другого. Оставлено как есть, не тронуто. Вместо этого — пункт D6: настил связан с исполнением через `cutting_spread_outputs` и с рулонами через `cutting_spread_lots`, инлайн-проверка привязана к тому же `execution_id` — цепочка «дефект → исполнение → настил → рулон» строится JOIN'ом, но нигде не собиралась в один ответ. `checksForExecution` для проверок на вехе `cutting-complete` теперь возвращает `culpableLots` — рулоны настила, обслужившего это исполнение; на прочих вехах список честно пуст, а не гадателен. | 15 | Добавлена проверка к уже существующему сквозному тесту (`tests/zx-postgres-tech-pack-allocation.test.mjs`), а не отдельный файл с фикстурой с нуля: сцена там уже раскраивает настил из `ROLL-A-001`, фиксирует дефект на вехе раскроя и решает «перекроены из того же рулона» — не хватало только прочитать эту связь. Живьём на копии демо: `EXEC-PO-SYN_JKT_R3_MID_M` резолвит настоящие `ROLL-R3-A-001` и `ROLL-R3-B-002` (те же, что выдавались в партию раньше в этой же сессии), `EXEC-PO-DEMAND-001` — честно пустой список, потому что для него настил никогда не заводился; строка видна в интерфейсе под записью проверки. verify 1661/1661 |
| 2026-09-24 | `feat/material-cost-reconciliation` | **Ведомость материалов и фактическая себестоимость по заказу нигде не сверялись, хотя обе уже существовали.** Пункт D раздела аудита: `bom.materialCost` — план по SKU, `costType: 'material'` в реестре фактических затрат — сумма на заказ, и одно с другим не встречалось ни разу. Свод только сравнивает, ничего не решает: для каждой строки заказа берётся опубликованная ведомость **в валюте заказа** (иначе — вне покрытия, а не пересчёт по случайному курсу), план — `materialCost × количество`, факт — `componentTotals.material` уже посчитанного `landed cost`. Отдаётся на лету в `GET /v2/orders/{id}/economics-position` полем `materialCostReconciliation` (плюс `coverageBasisPoints` и `uncoveredSkus`, тем же честным паттерном частичного факта, что у сезонной экономики) — ничего нового не хранится, четвёртое число рядом с тремя связанными разошлось бы при первой правке. **Попутно найдено при сверке этого же раздела аудита с кодом:** пункт про базу плановой/фактической маржи (первая от РРЦ, вторая от опта) был закрыт в коде ещё 21 сентября (`feat/season-economics`), но ревизия аудита этого не отражала — сверено по `git log` на файл, а не по формулировке документа; отмечен закрытым задним числом вместе с находкой про раскрой ↔ рулон из строки выше, которая тоже осталась неотмеченной в прошлый раз. | 6.5, 11, 15 | `tests/order-economics-position.test.mjs`: TSHIRT-1 с опубликованной ведомостью в валюте заказа входит в план и покрытие, JACKET-1 без ведомости — честно в `uncoveredSkus`, а не по нулевой цене. Живьём на копии демо: заказ `order_46542907…` (SKU SYN_TEE_R4_OFW_M, 120 шт. по 5.7031 €) даёт `plannedMaterialCost` 684.372 €, покрытие 100 %, `actualMaterialCost: null` — фактические затраты этого заказа записаны только категорией `factory`, материального факта ещё нет, и поле честно об этом говорит, а не подставляет ноль. verify 1662/1662, test:postgres 12/12 на чистой базе |
| 2026-09-24 | `feat/readiness-can-be-assessed-by-a-person` | **Оценка готовности — последний из четырёх коммерческих шагов, который делал только скрипт: `POST /v2/product/style-versions/{id}/readiness` в клиенте не вызывался ни разу.** Форма «Оценить готовность» на вкладке готовности модели. Внешних подтверждений с хешами из чужих систем она не спрашивает — человек их не набирает; такие измерения остаются заблокированными, и панель называет каждое. Предзаполняется прежним снимком (коммерческая подготовка меняется редко, а набирать шестнадцать полей заново ради поправки цены — способ ошибиться в цене); без снимка — названиями модели. Изображения берёт из самой модели, правило «главное фото и покрытие каждого варианта» проверяет сервер. Деньги набираются в основных единицах, минорные считает форма: живьём 129/329 → 12900/32900. Обязательные поля тест читает из самого маршрута, чтобы форма не отстала от него молча. | 13, 14 | `tests/readiness-can-be-assessed-by-a-person.test.mjs` (ровно поля маршрута и никаких внешних подтверждений; деньги основные → минорные; предзаполнение снимком и моделью; изображения без дублей и без документов; отказ без изображений и без кода страны — до отправки; право `product.manage`); живьём: снимок записан, честно `blocked` по шести измерениям с русскими причинами; verify 1661/1661, test:postgres 12/12 |
| 2026-09-23 | `feat/readiness-tells-the-reader-what-is-missing` | **Готовность модели говорила «заблокировано · 56 %» и не говорила, чего именно не хватает; коммерческую проекцию по-прежнему публиковал только скрипт.** Рабочее пространство несёт по каждой модели статус, четыре счётчика измерений и идентификатор снимка — и ничего из этого на экране не было: сам снимок `GET /v2/product/readiness/{id}` в клиенте не читался ни разу, хотя у каждого заблокированного измерения в нём лежит `evidence.reason` — предложение о том, чего недостаёт. Вкладка «Готовность» получила разбор по восемнадцати измерениям, заблокированные сверху. Следом — действие «Опубликовать коммерческую проекцию»: строка `commercial-projection` в `public/` не встречалась вовсе, а публикация коллекции выбирает именно из проекций, поэтому без этого шага цепочка до байера не замыкалась. Номер последней проекции берётся из рабочего пространства, а при расхождении повторяется **один раз** с тем номером, который служба назвала в деталях отказа; любой другой отказ не повторяется и доходит до читателя. **Два дефекта языка найдены на этом же экране.** В колонке «Проверка» одна строка говорила `blocked` по-английски рядом с «готов»: `statusLabel`, не найдя фразы, показывает сырое слово из базы, а `blocked`, `not_applicable` и `quarantine` в словаре отсутствовали — вместе с ними добавлены ещё семнадцать значений, которые домен умеет отдавать, и снят дубль ключа `in_development`, где вторая строка молча побеждала первую. Причины блокировки — замкнутый набор из двадцати девяти предложений оценщика — переводит тот модуль, который их показывает, как отказы форм переводит модуль-владелец; незнакомое предложение остаётся словами службы. | 13, 14, 15 | `tests/readiness-tells-the-reader-what-is-missing.test.mjs` (у каждого из восемнадцати измерений имя на двух языках и они различны; заблокированные идут первыми; причина переводится, незнакомая — нет; английский читатель видит слова службы; неоценённая модель говорит об этом; проекция предлагается только по готовой оценке и объясняет себя иначе; конфликт версий повторяется ровно раз и именно названным номером, прочий отказ — не повторяется) и `tests/every-status-the-server-emits-has-a-phrase.test.mjs` (перечисления статусов читаются с диска, поэтому новое попадёт под проверку без правки теста; дубль ключа отвергается; обе фразы непусты и русская не равна коду); живьём на копии демо: 18 измерений по-русски с причинами, проекция SYN.JKT.DEMO опубликована человеком до v2; verify 1653/1653 на чистой базе |
| 2026-09-23 | `feat/material-lots-reach-the-person-who-holds-them` | **Материальный контур был недостижим человеком целиком, и из-за этого недостижим был конец производственного дня.** Из шести маршрутов партий в клиенте вызывалось одно чтение (`grep -rn "v2/material-lots" public/` — одно попадание); пять мутаций — приёмка, выпуск из карантина, возврат, отклонение и выдача — не вызывались нигде. Это не отчётная мелочь: выпуск партии к отгрузке требует непустого списка выдач (`QUALITY_RELEASE_WITHOUT_MATERIAL_TRACE`), а выдать материал было нечем. Вкладка «Партии» получила приёмку и решения по каждой партии — ровно те, что домен разрешает по её статусу, с объяснением вместо отсутствующей кнопки. Выдача предлагает только **работающие** исполнения, у которых этот материал есть в опубликованной ведомости: живьём форма верно отказала на джерси (в ведомости единственного активного исполнения `MAT-SHELL-R3`) и верно прошла на плащёвке. **Три дефекта найдены тем, что я прошёл форму сам.** Первый мой собственный: `placeholder: undefined` писался в атрибут строкой «undefined» — `el()` ставит `title`/`placeholder` до общей защиты от отсутствия, и это касалось полей **всех** форм приложения. Второй: единица приписывалась к подписи сырым кодом («Количество, m»); теперь подпись без кода, а подсказка называет остаток через слой единиц («не больше 200 м»). Третий и главный: домен трактует выдачу как **уточнение** (`issuedQuantity - alreadyIssuedToExecution + amount`), то есть повторная выдача переписывает, а не прибавляет — за исполнением стояло 300 м, я ввёл 50, и стало 50. Модель законна и удобна для исправлений, но поле называлось «Количество»; теперь — «Всего выдано в это исполнение», в подписи выбора стоит уже выданное, а потолок считается как остаток плюс своя же прежняя запись. Состояние копии демо восстановлено до 300 м. | 13, 14 | `tests/material-lots-reach-the-person-who-holds-them.test.mjs` (решения по статусу; отклонение спрашивает и исчезает, когда партия в изделиях; два разных права на приговор и на выдачу; предлагаются только активные исполнения со своей ведомостью; отказ называет, чего не хватает; поле названо итогом и посылает итог; и отдельно — что **домен действительно переписывает**, вызовом самого домена, а не предположением о нём) и `tests/an-absent-placeholder-is-absent.test.mjs` (отсутствующая подсказка остаётся отсутствующей, пустая строка сохраняется как сказанное); verify 1640/1640 на чистой базе |
| 2026-09-23 | `fix/order-grid-totals-are-not-off-by-a-hundred` | **Сумма в подвале сетки заказа завышалась ровно в сто раз на любой цене с копейками.** Две ошибки гасили друг друга на целых ценах и складывались на дробных. Первая: сетка угадывала единицу цены по её виду — `Number.isSafeInteger(raw) ? raw : Math.round(raw * 100)`, то есть «24» считалось 24 центами, а «24,50» — 24,50 евро. Вторая: подвал печатал минорный итог основным форматтером денег. В демо все цены целые, поэтому на экране всё сходилось, и дефект ждал первой цены вида 24,99. Измерено на прежнем коде из git: 24,00 → 1.00×, **24,50 → 100.00×**, **24,99 → 100.00×**, 25,00 → 1.00×. Угадывать нечего: строка прайс-листа несёт `wholesalePriceMinor` целым числом рядом с ценой — оно теперь едет в ячейке матрицы и читается напрямую, а разбор основного значения остался запасным путём для старых снимков. Подвал получил отдельную функцию `formatMoneyMinor`, а не флаг на месте вызова: перепутать флаг — значит ошибиться в сто раз, и однажды так и вышло. Обе правки сделаны вместе — по отдельности каждая сломала бы то, что до неё работало случайно. **Прежний тест сетки закреплял сам дефект:** его приспособление подавало `unitPrice: 2400`, то есть считало основное поле минорным; приведено к настоящей форме ячейки. Данные при этом не портились — `amountMinor` никуда не отправлялся, ошибка была только на экране, но именно её читает байер, набирая количества. | 13, 14 | `tests/order-grid-totals-are-not-off-by-a-hundred.test.mjs` (целые и дробные цены вперемешку, потому что расходились ровно дробные; единица берётся из замороженного целого, а не из вида числа; запасной путь для снимка без минорного поля; догадка не возвращается — проверяется код, а не комментарий, её описывающий; минорный итог печатает минорный форматтер); verify 1629/1629, test:postgres 12/12 на чистой базе |
| 2026-09-23 | `fix/every-number-is-printed-by-the-language-layer` | **Числа печатались мимо языка читателя — в обе стороны.** Найдено на экране планирования: в одной строке таблицы стояло «-10.7 п.п.» рядом с «56,0 %» и «99,00 EUR». Значок отклонения форматировался `toFixed`, который всегда ставит точку, а соседняя карточка сезона — слоем чисел; тот же дефект уже правили для процентов в этом же модуле и пропустили значок. Обход всего клиента дал восемь мест в шести модулях, и половина ошибалась зеркально: `replace('.', ',')` всегда ставит запятую, то есть врёт в английском интерфейсе. В `production-orders.js` наценка, оба коэффициента и курс печатались в русской ветке через `replace`, а в английской — сырым значением: одно число в двух видах, оба мимо языка. Всё сведено к `I18N.formatNumber`; курс сохраняет восемь знаков — столько же, сколько хранит платформа. Единица «п.п.» была жёстко русской и в английском интерфейсе — теперь переводится («pp»), и печатает её один формат, общий для значка и карточки. | 13, 14 | Расширен `tests/money-and-units-read-as-language.test.mjs`: обход всех модулей клиента отвергает и `toFixed`, и ручную подстановку разделителя — в прошлый раз правку сделали для процентов и пропустили соседнюю колонку, поэтому правило проверяется целиком, а не по месту; отдельно — что единица переводится и формат один. Живьём на копии демо: «-10,7 п.п.» по-русски и «-10.7 pp» по-английски в той же ячейке; verify 1625/1625 |
| 2026-09-23 | `fix/planning-slots-can-be-moved-by-a-person` | **Слот ассортимента не переводился по статусу ни через интерфейс, ни — для двух демо-слотов — вообще никак.** Живой обход показал: из пяти маршрутов слотов в клиенте достижимы только два импортных, `…/transition` не вызывался нигде, то есть слот навсегда оставался в статусе, в каком приехал из импорта. Инспектор линейного плана получил переходы, повторяющие доменную карту (planned → in_development \| dropped, in_development → delivered \| dropped, delivered → dropped), с версией из той же строки, которую видит человек, и подтверждением на необратимое снятие; конечное состояние `dropped` объясняет себя вместо пустого места. **Под этим лежал дефект тяжелее.** Нажатие давало конфликт версий на свежезагруженной странице. Причина — триггер `advance_placeholder_on_style_link`: переводя слот в `in_development` при связывании первой модели, он двигал **колонки** `status` и `version`, не трогая `payload`, а полезная нагрузка в этой системе и есть запись — её читает домен и её версию сверяет `transitionProductPlaceholder`. Измерено в базе: SS27-OUT-001 и SS27-TOP-002 имели колонку `version=2, status=in_development` при `payload` `version=1, status=planned` — и в копии, и **в самом демо**. Такой слот не переводился никогда, а домен считал его всё ещё запланированным. Миграция 133 переписывает функцию так, что состояние идёт в нагрузку (метка времени — в том же формате `toISOString`, что пишет приложение), и чинит разошедшиеся строки по узкому условию, совпадающему с подписью прежнего триггера. | 7.3, 13, 14 | `tests/postgres/linking-a-style-moves-the-slot-itself.test.mjs` (сырым SQL, в обход домена: колонка и нагрузка отвечают одно и то же; сданный слот не возвращается в разработку вторым связыванием; разошедшаяся строка сводится условием миграции) и `tests/planning-slots-can-be-moved-by-a-person.test.mjs` (экран предлагает ровно то, что домен принимает, и ничего сверх; версия и код перехода уходят из видимой строки; снятие спрашивает подтверждение; переходы закрыты правом на кампанию); живьём на копии демо: слот прошёл `in_development → delivered`, на экране «передано» и единственная оставшаяся кнопка; verify 1623/1623, test:postgres 12/12 |
| 2026-09-23 | `fix/revoking-a-relationship-closes-the-showroom` | **Отзыв торговой связи не закрывал ничего: отключённый партнёр продолжал читать образы и прайс-лист бренда.** Найдено живым обходом ролей и воспроизведено лично на копии демо: `POST /v2/relationships/{id}/revoke` → 200 `revoked`, после чего `GET /v2/showrooms/{id}/looks` под магазином отдавал образ с оптовой ценой 185, а `GET /v2/showrooms/{id}/buyer-catalog` — строку прайс-листа с ценой 129. Причина: доступ к показу спрашивал **только** приглашение. Каскада «отозвали связь → отозвали приглашения» быть не должно — это разные решения, и связь восстанавливают, не выписывая приглашения заново, — поэтому связь стала обязательной частью самой проверки: `assertAcceptedShowroomAccess` требует `relationship` и проверяет его существующим `assertActiveRelationship`. Аргумент обязателен намеренно: место вызова, которое его не подаст, упирается в `ACTIVE_RELATIONSHIP_REQUIRED`, а не тихо сохраняет дыру — на этом и вскрылись все пять мест. Образы охраняются собственной проверкой и получили то же условие; в создании подборки связь уже читалась строкой выше и переиспользована, а не прочитана вторично. | 7.3, 13, 15 | `tests/revoking-a-relationship-closes-the-showroom.test.mjs` (принятое приглашение на отозванной, отклонённой и ожидающей связи не открывает показ; неподанная связь — отказ, а не проход; связь других сторон — не эта торговля; ни одно место вызова не обходится без связи); четыре прежних двойника приведены к новому договору; живьём на копии демо: оба маршрута до правки 200 с ценами, после — `SHOWROOM_ACCESS_DENIED` и `ACTIVE_RELATIONSHIP_REQUIRED`, после восстановления связи снова 200; verify 1616/1616 |
| 2026-09-23 | `fix/a-newer-buyer-catalogue-is-not-hidden` | **Магазин не узнавал, что бренд открыл ему новый каталог.** Найдено живьём сразу после того, как публикация каталога стала достижимой: экран листа закреплён за снимком, по которому собрана подборка (`selection.buyerCatalogVersionId`), и это верно — цена, по которой набраны количества, не смеет меняться задним числом. Неверным было молчание: более новая версия не упоминалась нигде, а кнопка «Обновить каталог» перезапрашивала ровно ту же закреплённую версию, то есть имя обещало то, чего она не делает и сделать не может. Перепривязку не ввожу: домен уже ответил правилом `SELECTION_FOR_CYCLE_EXISTS` — одна подборка на цикл, новый каталог покупается новым циклом. Экран теперь читает последнюю версию доступа отдельно от закреплённой и, когда они различны, предупреждает с датой и называет верный путь; кнопка переименована в «Перечитать каталог» и заодно заново спрашивает, какая версия последняя. Отказ чтения последней версии не превращается в утверждение — экран молчит. | 13, 14 | `tests/a-newer-buyer-catalogue-is-not-hidden.test.mjs` (закрепление сохранено, различие версий сравнивается по идентификатору, сообщение на двух языках и с датой, путь назван тот, что домен разрешает, кнопка не обещает лишнего, отказ чтения не порождает утверждения); живьём на копии демо: «Бренд открыл более новый каталог — 23.09.2026, 17:01»; verify 1611/1611 |
| 2026-09-23 | `feat/commercial-publication-reaches-the-buyer` | **Две коммерческие публикации перестали быть шагами скрипта, и ни один снимок старше миграции 126 больше не тупик.** Измерено грепом по `public/modules/*.js`: ни одной мутации на `commercial-publications` и `buyer-catalogs` — человек доходил до цикла и дальше не мог. Форма публикации не собиралась из существующих чтений: ассортимент коллекции на чтение не отдавался вовсе (маршрут `/v2/collections/{id}/style-versions` только POST). Добавлен `GET /v2/collections/{id}/publishable-projections`, отвечающий на вопрос формы целиком: правило «проекция опубликована и её версия модели назначена в коллекцию» осталось в домене, способность та же, что у самой публикации (живьём `viewer` → 403, не пустые 200), список второго читателя проекций не заводит. Действия стоят каждое на своём экране: снимок — у коллекции, каталог — на доступах шоурума, с объяснением вместо отсутствующей кнопки. **Два дефекта найдены тем, что я смотрел на экран, а не на ответ.** Первый: каталог создавался, форма говорила «Изменения сохранены», а строка доступа показывала прежнюю дату — кэш переживал перезагрузку, теперь запись сбрасывает свою пару. Второй и главный: **ни одна публикация, созданная до миграции 126, не могла стать каталогом байера** — в её строках нет ключа `packSize`, а `applyBuyerPrices` ставил `buyerPackSize: null` всегда; в jsonb «ключа нет» и «ключ равен null» различны, и триггер отвергал пару кодом `BUYER_CATALOG_PRICE_HIERARCHY_MISMATCH`. Проверки приложения этого не ловили: они сравнивают два результата одной сборки. Строка прайс-листа обязана равняться замороженной, поэтому исправлено украшение: оно повторяет молчание строки. Голый код в отказе оставлен намеренно: `error-status.mjs` уже решил, что выдумывать фразу за триггер нельзя, и код прописью объяснения не добавляет. | 7.3, 13, 14, 15 | `tests/publishable-projections-are-offered-to-the-publisher.test.mjs` (только опубликованные и только назначенные, чужой бренд не проходит, строка без технического снимка, способность как у публикации); `tests/commercial-publication-reaches-the-screen.test.mjs` (обе формы в песочнице: право, состояние, тело запроса, объяснение вместо кнопки, сброс строки после записи); `tests/buyer-decorations-stay-in-step.test.mjs` теперь спрашивает саму сборку, а не разбирает исходник регуляркой; живьём на копии демо: снимок создан через форму (3 публикации вместо 2), колонка «Каталог байера» появилась в таблице доступов, отказавшая публикация R4 стала каталогом; verify 1606/1606 |
| 2026-09-23 | `fix/form-refusals-speak-the-readers-language` | **Живой обход форм: отказ формы читается на языке пользователя.** Первая попытка создать кампанию с перепутанными датами дала русскому пользователю «Campaign dates: start must be before end». Все двенадцать сообщений `ui-validation.js` были только английскими; таблица фраз их подхватить не могла — она ищет точное совпадение, а строки собираются с подстановкой чисел. Язык выбирает сам валидатор. **Подписи полей были второй половиной беды:** в валидатор передавались английские строки в двадцати четырёх местах, дублировавшие подписи полей, и после первой правки получалось «Название кампании: minimum 2 characters» — половина перевода хуже отсутствия. Теперь передаётся та же строка, которой подписано поле; там, где сообщение однозначно и без названия, подпись убрана вовсе. **Три прежних теста закрепляли английскую прозу** и теперь проверяют код отказа плюс смену языка через двойник рантайма i18n — строже, а не слабее: раньше они прошли бы и при полностью непереведённом интерфейсе. | 13, 14 | `tests/form-refusals-speak-the-readers-language.test.mjs` (каждое сообщение имеет языковую пару; ни одна форма не передаёт подпись, которой интерфейс не показывает; диапазон дат отказывает самостоятельной фразой) плюс переписанные `tests/ui-validation.test.mjs`; живьём `Название: минимум 2 символа` / `Name: minimum 2 characters`; verify 1592/1592 |
| 2026-09-18 | `feat/quotation-negotiation` | Defects from the second agent walk (sourcing, production, wholesale and the top bar). **A Russian user could not enter a price**: the quotation form prints money as "21 250,00 €" and then rejected `52,00` with the bare code `INVALID_MONEY`. A comma is a decimal separator, spaces group thousands, and the message is a sentence. **The top-bar search was inert in eight of thirteen sections** because it only knew `.od-table`, while the sourcing, production, quality and linesheet modules render their own. **Every date stayed Russian in EN** across that same half: `Intl.DateTimeFormat(undefined, …)` follows the browser, not the interface. **`unread` had no label** so the notifications table showed `прочитано` and `unread` side by side; the deal, sharing and notification-type keys are labelled too. **Three more refresh buttons fetched and said nothing** — production orders, linesheets and the sidebar footer — while their siblings reported themselves. |

Future implementation PRs add a row here. The row is not a substitute for updating the affected detailed sections.

---

## 21. Supporting specifications and their authority

These files remain useful deep dives and evidence. They must be reconciled into this master specification when behavior changes:

- `docs/architecture/product-identity-v2.md`
- `docs/architecture/product-readiness-commercial-projection-v2.md`
- `docs/architecture/order-supply-cost-margin-baseline.md`
- `docs/architecture/order-margin-bridge.md`
- `docs/architecture/econ003-allocation-margin-close-lineage.md` — exact pre-close allocation provenance plus post-close pending/reconciliation semantics.
- `docs/architecture/syntha-v2-live-capability-register.md`
- `docs/commercial-execution-spine.md`
- `docs/commercial-publication-linesheets.md`
- `docs/omnidata-design-system-v1.md`
- `docs/fashion-kpi/`
- `docs/observability.md`
- `docs/joor-retailer-cabinet-complete-map.md` — research/reference map, not proof that a Syntha capability is implemented.

A supporting document can be more verbose, but it cannot redefine a canonical entity, lifecycle or visual token without the same change being made here. Supporting documents that contain stale “remaining gate” language must be corrected during the relevant audit pass; stale support text is never allowed to override the current status in this master specification.

---

## 22. Definition of Done for Syntha V2 changes

A change is DONE only when all applicable boxes are true:

- [ ] domain behavior is implemented and fail-closed;
- [ ] exact entity/lineage relations are preserved;
- [ ] API + OpenAPI agree;
- [ ] persistence constraints/transactions agree with domain rules;
- [ ] organisation/RBAC/capabilities are enforced;
- [ ] UI has a real handler and all states, not a decorative control;
- [ ] RU/EN user-facing content is wired through localization;
- [ ] ODS v1 roles/tokens are reused; no local visual dialect is introduced;
- [ ] unit/integration/PostgreSQL tests cover the changed invariant;
- [ ] live acceptance is expanded when the change crosses a previously unproven business boundary;
- [ ] acceptance evidence is classified accurately: a defined or mocked harness is not live proof, a green CI runtime gate is not automatically the intended production-evidence environment, and `PROD-PROVEN` requires the accepted intended environment;
- [ ] `ARCHITECTURE.md` records fields/actions/relations/UI/API/status/gaps affected by the change;
- [ ] change register row is added/updated;
- [ ] `npm run verify` passes;
- [ ] `npm run verify:postgres` passes for release-candidate changes;
- [ ] PR is mergeable and required GitHub CI is green.

This checklist is deliberately stricter than “code compiles”. Syntha V2 is treated as one operating platform, so a locally correct screen or service is not complete if its upstream/downstream contract is broken or undocumented.
